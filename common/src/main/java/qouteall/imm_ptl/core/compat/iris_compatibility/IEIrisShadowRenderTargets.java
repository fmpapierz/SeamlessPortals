package qouteall.imm_ptl.core.compat.iris_compatibility;

// S12-A Iris compile-shell duck (VERBATIM IP). Empty marker — its only member (getShadowMapSwapper) is
// commented out in IP because it references the next-Iris-version ShadowMapSwapper. Held/inert until S13.
public interface IEIrisShadowRenderTargets {
//    ShadowMapSwapper getShadowMapSwapper();
}
