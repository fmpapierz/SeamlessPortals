# Make RenderType.create accessible
public net.minecraft.client.renderer.rendertype.RenderType create(Ljava/lang/String;Lnet/minecraft/client/renderer/rendertype/RenderSetup;)Lnet/minecraft/client/renderer/rendertype/RenderType;

# Make RenderPipelines.register accessible
public-f net.minecraft.client.renderer.RenderPipelines register(Lcom/mojang/blaze3d/pipeline/RenderPipeline;)Lcom/mojang/blaze3d/pipeline/RenderPipeline;

# 26.2: ViewArea.sections is now private final RotatingSectionStorage<RenderSection>.
# Widen for the portal-view section-iteration loops.
public-f net.minecraft.client.renderer.ViewArea sections

# 26.2: the section dirty/rebuild state moved off RenderSection onto the
# LevelExtractor's SectionUpdateTracker. Widen it so the portal-view
# section-compile pumps can read/clear dirty state.
public-f net.minecraft.client.renderer.extract.LevelExtractor sectionUpdateTracker

# Entity-portal migration S4 (U2 duck): 26.2 makes ChunkMap.TrackedEntity a private nested
# class, breaking IEChunkMap.ip_getEntityTrackerMap()'s Int2ObjectMap<ChunkMap.TrackedEntity>
# signature. IP's own accesswidener widens exactly this type; this is the neoforge-side
# faithful port of that entry (parallels the AW entry). Grows the AT as the duck demands.
public net.minecraft.server.level.ChunkMap$TrackedEntity

# Entity-portal migration S4 (U2 held facade RequiemCompat): 26.2 reduces ServerPlayer.server
# to private final MinecraftServer (ServerPlayer.java:232), whereas IP's 1.21.3 field was public
# (no IP AW entry existed). RequiemCompat.java:91 reads player.server verbatim; this is the
# neoforge-side faithful port of the AW growth. Read-only, so plain `public` (no de-final).
public net.minecraft.server.level.ServerPlayer server

# Entity-portal migration S5 (U3 held McHelper.getDoesRegionFileExist): 26.2 keeps
# MinecraftServer.storageSource as protected final LevelStorageSource.LevelStorageAccess
# (MinecraftServer.java:217). McHelper.java:430 reads MiscHelper.getServer().storageSource
# verbatim; IP's imm_ptl.accesswidener widens exactly this field. This is the neoforge-side
# faithful port of the AW growth. Read-only, so plain `public` (no de-final).
public net.minecraft.server.MinecraftServer storageSource

# Entity-portal migration S10-B (U8 held ClientWorldLoader.createSecondaryClientWorld): 26.2
# keeps BiomeManager.biomeZoomSeed as private final long (BiomeManager.java:16) with no getter.
# ClientWorldLoader.java:605 reads CLIENT.level.getBiomeManager().biomeZoomSeed verbatim (the
# shared per-save seed passed to the secondary ClientLevel ctor). IP's imm_ptl.accesswidener
# widens exactly this field; this is the neoforge-side faithful port of the AW growth (the
# compile-time widener on this NeoForge ModDevGradle classpath is the AT, so the AW entry alone
# left it private-access). Read-only, so plain `public` (no de-final).
public net.minecraft.world.level.biome.BiomeManager biomeZoomSeed

# Entity-portal migration S10-C (U8 held MixinServerPlayerGameMode.redirectNewUseOnContext): 26.2
# demotes the 5-arg UseOnContext ctor to protected (UseOnContext.java:24; was public in 1.21.3,
# no IP AW entry existed). MixinServerPlayerGameMode constructs it verbatim; this is the
# neoforge-side compile-time widener paired with the AW entry. Constructor AT form `<init>(desc)V`.
public net.minecraft.world.item.context.UseOnContext <init>(Lnet/minecraft/world/level/Level;Lnet/minecraft/world/entity/player/Player;Lnet/minecraft/world/InteractionHand;Lnet/minecraft/world/item/ItemStack;Lnet/minecraft/world/phys/BlockHitResult;)V
