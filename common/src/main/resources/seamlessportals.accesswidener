accessWidener v2 official

# Make RenderType.create accessible for creating custom render types
accessible method net/minecraft/client/renderer/rendertype/RenderType create (Ljava/lang/String;Lnet/minecraft/client/renderer/rendertype/RenderSetup;)Lnet/minecraft/client/renderer/rendertype/RenderType;

# Make RenderPipelines.register accessible for registering custom pipelines
accessible method net/minecraft/client/renderer/RenderPipelines register (Lcom/mojang/blaze3d/pipeline/RenderPipeline;)Lcom/mojang/blaze3d/pipeline/RenderPipeline;

# 26.2: ViewArea.sections is now private final RotatingSectionStorage<RenderSection>.
# Widen for the portal-view section-iteration loops.
accessible field net/minecraft/client/renderer/ViewArea sections Lnet/minecraft/client/RotatingSectionStorage;

# 26.2: the section dirty/rebuild state moved onto the LevelExtractor's SectionUpdateTracker.
accessible field net/minecraft/client/renderer/extract/LevelExtractor sectionUpdateTracker Lnet/minecraft/client/SectionUpdateTracker;

# Entity-portal migration S4 (U2 duck): 26.2 makes ChunkMap.TrackedEntity a private nested
# class, so IEChunkMap.ip_getEntityTrackerMap()'s Int2ObjectMap<ChunkMap.TrackedEntity>
# signature no longer compiles against vanilla. IP's own accesswidener widens exactly this
# type (imm_ptl.accesswidener: transitive-accessible class ...ChunkMap$TrackedEntity) — this
# is the faithful port of that entry, growing the AW as the duck demands (plan S4(a)).
accessible class net/minecraft/server/level/ChunkMap$TrackedEntity

# Entity-portal migration S4 (U2 held facade RequiemCompat): 26.2 reduces ServerPlayer.server
# to `private final MinecraftServer` (ServerPlayer.java:232); in IP's 1.21.3 it was public, so
# IP's imm_ptl.accesswidener has NO ServerPlayer entry (none was needed). RequiemCompat.java:91
# reads `player.server` verbatim (ServerTeleportationManager.of(player.server)). Faithful fix =
# grow the AW to restore the access IP had for free, keeping RequiemCompat byte-verbatim
# (identical in class to the ChunkMap$TrackedEntity growth above; read-only, so no `mutable`).
# RequiemCompat is HELD until S13 — this is its probe/closure obligation, landed now.
accessible field net/minecraft/server/level/ServerPlayer server Lnet/minecraft/server/MinecraftServer;

# Entity-portal migration S5 (U3 held McHelper.getDoesRegionFileExist): 26.2 keeps
# MinecraftServer.storageSource as `protected final LevelStorageSource.LevelStorageAccess`
# (MinecraftServer.java:217). McHelper.java:430 reads MiscHelper.getServer().storageSource
# verbatim. IP's own imm_ptl.accesswidener widens exactly this field
# (transitive-accessible field ...MinecraftServer storageSource L...LevelStorageAccess;) — this
# is the faithful port of that entry (plain non-transitive `accessible`, matching this mod's AW
# convention; read-only, so no `mutable`).
accessible field net/minecraft/server/MinecraftServer storageSource Lnet/minecraft/world/level/storage/LevelStorageSource$LevelStorageAccess;

# Entity-portal migration S10-B (U8 held ClientWorldLoader.createSecondaryClientWorld): 26.2
# keeps BiomeManager.biomeZoomSeed as `private final long` (BiomeManager.java:16) with no getter.
# ClientWorldLoader.java reads CLIENT.level.getBiomeManager().biomeZoomSeed verbatim (the shared
# per-save seed passed to the secondary ClientLevel ctor). IP's own imm_ptl.accesswidener widens
# exactly this field (transitive-accessible field ...BiomeManager biomeZoomSeed J) — this is the
# faithful port of that entry (plain non-transitive `accessible`, matching this mod's AW
# convention; read-only, so no `mutable`).
accessible field net/minecraft/world/level/biome/BiomeManager biomeZoomSeed J

# Entity-portal migration S10-C (U8 held MixinServerPlayerGameMode.redirectNewUseOnContext): 26.2
# demotes the 5-arg UseOnContext ctor to `protected UseOnContext(Level, @Nullable Player,
# InteractionHand, ItemStack, BlockHitResult)` (UseOnContext.java:24); in IP's 1.21.3 it was
# public, so IP's imm_ptl.accesswidener has NO UseOnContext entry (none was needed).
# MixinServerPlayerGameMode.java constructs it verbatim to route block break/use through the
# actual (cross-portal) destination dimension. Faithful fix = grow the AW to restore the access
# IP had for free (member ctor, so `accessible method ... <init> ...`; no `mutable`).
accessible method net/minecraft/world/item/context/UseOnContext <init> (Lnet/minecraft/world/level/Level;Lnet/minecraft/world/entity/player/Player;Lnet/minecraft/world/InteractionHand;Lnet/minecraft/world/item/ItemStack;Lnet/minecraft/world/phys/BlockHitResult;)V

# Entity-portal migration S13 (alternate_dimension compile shell — NormalSkylandGenerator): 26.2
# makes NoiseBasedChunkGenerator `public final` (NoiseBasedChunkGenerator.java:50). IP subclasses it
# so ChunkMap's `generator instanceof NoiseBasedChunkGenerator` seeds the RandomState / createState
# (NormalSkylandGenerator.java:55-59 javadoc; extending ChunkGenerator instead would lose that
# instanceof seeding = worldgen-randomness fidelity violation, api-map platform-compat-peripheral §2
# NoiseBasedChunkGenerator row). IP's own imm_ptl.accesswidener widens exactly this class
# (transitive-extendable class ...NoiseBasedChunkGenerator) — this is the faithful port of that
# entry, de-finalized so the subclass compiles (plain non-transitive `extendable`, matching this
# mod's AW convention). Paired with the accesstransformer.cfg `public-f` entry (the compile-time
# widener on the NeoForge ModDevGradle :common classpath).
extendable class net/minecraft/world/level/levelgen/NoiseBasedChunkGenerator

# Entity-portal migration S13-A (U11 peripheral — CommandStickItem/PortalWandItem creative-tab wiring):
# 26.2 makes CreativeModeTab.Output a `protected interface` (CreativeModeTab.java:246) — public in IP's
# 1.21.3. CommandStickItem.addIntoCreativeTag(CreativeModeTab.Output) + PortalWandItem's counterpart
# reference it verbatim. Fabric API's own accesswidener widens exactly this nested type; faithful port of
# that access. Paired with the accesstransformer.cfg `public` entry.
accessible class net/minecraft/world/item/CreativeModeTab$Output

# Entity-portal migration S13-A (U11 commands — PortalDebugCommands./portal debug profile set_lag_logging_threshold):
# 26.2 makes ActiveProfiler.WARNING_TIME_NANOS `private static final long` (ActiveProfiler.java:27) —
# public static (non-final) in IP's 1.21.3, written directly by the command. Non-constant initializer
# (Duration.ofMillis(...).toNanos()) so it is not inlined and the write is runtime-effective. Restore IP's
# verbatim write: accessible + mutable (de-final). Paired with the accesstransformer.cfg `public-f` entry.
accessible field net/minecraft/util/profiling/ActiveProfiler WARNING_TIME_NANOS J
mutable field net/minecraft/util/profiling/ActiveProfiler WARNING_TIME_NANOS J

# S14.21 (rung-2 fix 1 — the prepareRendering stencil-clear FBO resolver): GlDevice is
# package-private in the real jar (the mc262-ref decompile normalizes access). The resolver needs
# glDevice.frameBufferCache().getFbo(...) + directStateAccess() — the SAME lookup every main-pass
# createRenderPass performs — to bind the LIVE main-target FBO for the per-frame stencil clear
# (the 26.2 1:1 of IP's mainRenderTarget.bindWrite(false); replaces the stale gameFboId capture
# that produced the 51k/session GL_INVALID_OPERATION flood + the broken stencil reset).
accessible class com/mojang/blaze3d/opengl/GlDevice
# S14.21 companion: RenderSystem.getDevice() returns the GpuDevice FACADE; the GL backend
# (GlDevice implements GpuDeviceBackend) is reachable only through its private `backend` field.
accessible field com/mojang/blaze3d/systems/GpuDevice backend Lcom/mojang/blaze3d/systems/GpuDeviceBackend;
# S19-C: CreateWorldScreen$MoreTab is a private inner class; MixinCreateWorldScreenMoreTab_CVB's
# class-literal @Mixin target needs it accessible (IP precedent: imm_ptl.accesswidener carries
# the same transitive-accessible line for this exact class).
accessible class net/minecraft/client/gui/screens/worldselection/CreateWorldScreen$MoreTab
