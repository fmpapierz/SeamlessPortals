# 26.2 API Map — Common Mixins Triage (`qouteall/imm_ptl/core/mixin/common/**`)

**Inventory source:** `migration/inventory/mixin-common.md` (74 mixin classes; the inventory's original "73" headline was an off-by-one, since corrected — its per-directory counts and a fresh directory listing both give 74).
**26.2 evidence root (all `file:line` below are relative to this unless absolute):** `C:/Users/warwa/ModDev/mc262-ref`
**IP source root:** `C:/Users/warwa/ModDev/ImmersivePortalsMod/src/main/java/qouteall`

**Verdicts:** PORTS-CLEAN = target class + targeted members + @At anchors exist compatibly. NEEDS-RETARGET = target exists but a signature/anchor/shadow moved (exact replacement given). TARGET-GONE = the hook site no longer exists (26.2 mechanism that fills the role given).

**Totals: 50 PORTS-CLEAN / 19 NEEDS-RETARGET / 5 TARGET-GONE.**

---

## 1. Root (`common/`) — 8

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `MixinClipContext` | `ClipContext` | **PORTS-CLEAN** | `private final Vec3 from` / `to` at `net/minecraft/world/level/ClipContext.java:22-23`; `getBlockShape(BlockState, BlockGetter, BlockPos)` `:48` | `@Mutable @Final` re-point of `from`/`to` still valid (fields still final); HEAD-cancellable inject on `getBlockShape` unchanged. |
| `MixinConnection_Debug` | `Connection` | **PORTS-CLEAN** | `exceptionCaught(ChannelHandlerContext, Throwable)` at `net/minecraft/network/Connection.java:105` | — |
| `MixinDedicatedServer` | `DedicatedServer` | **PORTS-CLEAN** | class at `net/minecraft/server/dedicated/DedicatedServer.java:75` | Inert (empty body). |
| `MixinLevel` | `Level` | **NEEDS-RETARGET** | `Level.prepareWeather()` **does not exist** — grep of `Level.java` has no `prepareWeather`; it is now `private void prepareWeather(WeatherData)` on **ServerLevel** at `net/minecraft/server/level/ServerLevel.java:702`, driven from the ctor via `this.prepareWeather(server.getWeatherData())` `:273`. Weather advance is per-level gated by `Level.canHaveWeather()` (`net/minecraft/world/level/Level.java:857`; used `ServerLevel.java:713`). Duck targets survive: `thread` `Level.java:113`, `oRainLevel/rainLevel/oThunderLevel/thunderLevel` `:118-121`, `levelData` (`WritableLevelData`) `:126`, `protected abstract LevelEntityGetter<Entity> getEntities()` `:1000` | The `prepareWeather` TAIL inject (zero nether rain gradient) must re-anchor: server side is now gated by `canHaveWeather()` (`ServerLevel.java:713`) so a nether ServerLevel never raises `rainLevel`, and rain-level packets are dimension-scoped via `broadcastAll(packet, this.dimension())` `ServerLevel.java:774-783` — **but** the rain-state-flip branch broadcasts `RAIN_LEVEL_CHANGE`/`THUNDER_LEVEL_CHANGE` to ALL players with NO dimension (`ServerLevel.java:785-795`), which is exactly the cross-dimension bleed IP guards against. Ducks (`ip_getLevelData`, `portal_setWeather`, `portal_getEntityLookup`, `portal_getThread`) port clean (`getEntities()` is now `protected abstract` — shadow still binds). |
| `MixinLivingEntity` | `LivingEntity` | **NEEDS-RETARGET** | `tick()` `net/minecraft/world/entity/LivingEntity.java:2743`; `lastHurtByMob` is now `EntityReference<LivingEntity>` `:256`; `getLastHurtByMob()` resolves via `EntityReference.getLivingEntity(this.lastHurtByMob, this.level())` `:616`; `setLastHurtByMob(@Nullable LivingEntity)` `:645`; `getLastHurtMob()` `:650`; the 1-arg `setLastHurtByPlayer(Player)` is gone — overloads are `(Player,int)` `:632` / `(UUID,int)` `:636` | Inject point (`tick` RETURN) fine. The clear call `setLastHurtByPlayer(null)` must become the 2-arg form. Semantics note: `EntityReference.getEntity(Level, Class)` resolves via `level::getEntityInAnyDimension` (`net/minecraft/world/entity/EntityReference.java:78-81`; `Level.java:785`) — the attacker DOES resolve across dimensions in 26.2, so the cross-dimension aggro-clear is still meaningful. Preserve the IP asymmetry (checks `getLastHurtMob()`, clears via `setLastHurtByPlayer`) per port-risk note 7 of the inventory. |
| `MixinMinecraftServer` | `MinecraftServer` | **PORTS-CLEAN** | `protected void runServer()` at `net/minecraft/server/MinecraftServer.java:718` | `@Unique` field + RETURN inject + duck all fine. |
| `MixinServerChunkCache` | `ServerChunkCache` | **PORTS-CLEAN** | `private final DistanceManager distanceManager` at `net/minecraft/server/level/ServerChunkCache.java:59`; `public final ChunkMap chunkMap` `:64` | Accessor duck fine. |
| `MixinServerLevel` | `ServerLevel` | **NEEDS-RETARGET** | The `List.isEmpty()` call inside `tick(BooleanSupplier)` is **gone**: the empty-dimension gate is now `boolean isActive = this.chunkSource.hasActiveTickets();` at `net/minecraft/server/level/ServerLevel.java:408` (then `resetEmptyTime()` `:410`, `emptyTime++` `:414`, `if (this.emptyTime < 300)` `:417`). `toString()` `:1641`; `tickNonPassenger(Entity)` `:826`; `entityManager` (`PersistentEntitySectionManager<Entity>`) `:202` | Retarget the keep-ticking redirect to the `ServerChunkCache.hasActiveTickets()` invocation at `:408` (return `true` when `ImmPtlChunkTracking.shouldLoadDimension(...)`). Note IP's own tickets in `TicketStorage` may already make `hasActiveTickets()` true, but the explicit hook is the faithful equivalent. `toString` HEAD inject and `tickNonPassenger` HEAD inject port clean. |

## 2. `chunk_sync/` — 10

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IEChunkMap_Accessor` | `ChunkMap` (invoker `getChunks()`) | **TARGET-GONE** | No `getChunks` in `net/minecraft/server/level/ChunkMap.java` (grep: zero hits). Holder storage is `private final Long2ObjectLinkedOpenHashMap<ChunkHolder> updatingChunkMap` `:127` and `private volatile ... visibleChunkMap` `:128`; also `Stream<ChunkHolder> allChunksWithAtLeastStatus(ChunkStatus)` `:708` | Role filled by an `@Accessor("visibleChunkMap")` (iterate `.values()`) or the new package-private `allChunksWithAtLeastStatus`. Only IP call site is `PortalDebugCommands.java:459` (debug iteration). |
| `IEChunkTaskPriorityQueueSorter` | `ChunkTaskPriorityQueueSorter` (accessor `mailbox`) | **TARGET-GONE** | Class deleted (not in `net/minecraft/server/level/`; dir listing shows `ChunkTaskDispatcher.java` + `ThrottlingChunkTaskDispatcher.java` instead). `ChunkTaskDispatcher` at `ChunkTaskDispatcher.java:17` has `private final PriorityConsecutiveExecutor dispatcher` `:22` and **public** `submit(Runnable, long, IntSupplier)` `:62` / `release(long, Runnable, boolean)` `:49`; `ProcessorMailbox` is also deleted (`net/minecraft/util/thread/ProcessorMailbox.java` absent) | Role filled by `ThrottlingChunkTaskDispatcher` (`ThrottlingChunkTaskDispatcher.java:12`), reachable as `DistanceManager.ticketDispatcher` (`DistanceManager.java:44`). Its `submit`/`release` being public likely removes the need for a mailbox accessor entirely. IP only referenced the mailbox in a comment (`ImmPtlChunkTickets.java:50`); the declared duck method has no non-mixin call sites. |
| `IEDistanceManager` (accessors) | `DistanceManager` | **NEEDS-RETARGET** | `mainThreadExecutor` survives: `private final Executor mainThreadExecutor` `net/minecraft/server/level/DistanceManager.java:46`. `tickets` field GONE — moved to `TicketStorage`: `private final Long2ObjectOpenHashMap<List<Ticket>> tickets` `net/minecraft/world/level/TicketStorage.java:43`; `DistanceManager` holds `private final TicketStorage ticketStorage` `DistanceManager.java:40`. `ticketThrottler` GONE — now `private final ThrottlingChunkTaskDispatcher ticketDispatcher` `DistanceManager.java:44` | Keep `ip_getMainThreadExecutor` (used by `ImmPtlChunkTickets.java:185`). Replace `ip_getTickets`/`ip_getTicketThrottler` with `ticketStorage`/`ticketDispatcher` accessors. Note: `Ticket` is no longer generic (`new Ticket(TicketType, int)`, `DistanceManager.java:115`), and `SortedArraySet<Ticket<?>>` per-chunk sets became `List<Ticket>` (`TicketStorage.java:130`). |
| `IEServerCommonPacketListenerImpl` | `ServerCommonPacketListenerImpl` (accessor `connection`) | **PORTS-CLEAN** | `protected final Connection connection` at `net/minecraft/server/network/ServerCommonPacketListenerImpl.java:40` | — |
| `MixinChunkHolder` | `ChunkHolder` | **PORTS-CLEAN** | `private void broadcast(List<ServerPlayer>, Packet<?>)` `net/minecraft/server/level/ChunkHolder.java:236`; `broadcastChanges(LevelChunk)` `:174` with `playerProvider.getPlayers(this.pos, true/false)` at `:178`/`:191`; `ChunkHolder.PlayerProvider.getPlayers(ChunkPos, boolean)` `:341-342`; `private final LevelHeightAccessor levelHeightAccessor` `:35` | The `levelHeightAccessor`→`ServerLevel` cast still holds: `ChunkMap` constructs `new ChunkHolder(ChunkPos.unpack(node), level, this.level, this.lightEngine, this::onLevelChange, this)` at `ChunkMap.java:386` (3rd arg = the `ServerLevel`). `@ModifyVariable` on the `Packet<?>` arg of `broadcast` and the `getPlayers` `@Redirect` both bind. |
| `MixinChunkMap_C` | `ChunkMap` | **NEEDS-RETARGET** | `applyChunkTrackingView(ServerPlayer, ChunkTrackingView)` survives at `ChunkMap.java:1109` (HEAD-cancel fine). `onChunkReadyToSend` **changed signature**: now `private void onChunkReadyToSend(ChunkHolder chunkHolder, LevelChunk chunk)` `:690`, and its body (a) pushes to per-player senders via `markChunkPendingToSend(player, chunk)` `:693-697`, (b) calls NEW bookkeeping `this.level.getChunkSource().onChunkReadyToSend(chunkHolder)` `:699` which feeds `chunkHoldersToBroadcast` (`ServerChunkCache.java:586-590`), (c) `debugSynchronizers().registerChunk` `:700`. Shadows survive: `getPlayerViewDistance(ServerPlayer)` `:817`, `level` `:131`, `lightEngine` `:132`, `getVisibleChunkIfPresent(long)` `:255` | The `@Overwrite onChunkReadyToSend` must be re-derived against the new 2-arg signature and MUST preserve the `ServerChunkCache.onChunkReadyToSend(chunkHolder)` broadcast-queue call (new in 26.2) while replacing only the per-player send decision with `ImmPtlChunkTracking.onChunkProvidedDeferred`. |
| `MixinDistanceManager` | `DistanceManager` | **NEEDS-RETARGET** | `removePlayer(SectionPos, ServerPlayer)` `DistanceManager.java:118` (still NPEs if `playersPerChunk` has no entry — `chunkPlayers.remove(player)` `:122` on a possibly-null get `:121`, so the HEAD pre-populate inject is still needed and binds); `playersPerChunk` `:37`; `runAllUpdates(ChunkMap)` `:64` (RETURN inject fine). Shadowed `getTickets(long)` GONE from DistanceManager — now `TicketStorage.getTickets(long)` returning `List<Ticket>` `TicketStorage.java:130` | `portal_getTicketSet(long)` duck must re-route through the `ticketStorage` field (`DistanceManager.java:40`) and change type `SortedArraySet<Ticket<?>>` → `List<Ticket>`. It has **three** active non-mixin call sites — `ImmPtlChunkTickets.removeAllTicketsInWorld` (`ImmPtlChunkTickets.java:299`; method spans `:294-312`), `PortalDebugCommands.java:466`, and `PortalDebugCommands.java:732` — so the type retarget must also update both debug-command sites. `removeAllTicketsInWorld` also uses `DistanceManager.removeRegionTicket` (`ImmPtlChunkTickets.java:307`); `addRegionTicket/removeRegionTicket` are gone from `DistanceManager` (not in the full 320-line file) — 26.2 equivalents are `TicketStorage.addTicketWithRadius/removeTicketWithRadius(TicketType, ChunkPos, int)` `TicketStorage.java:138/:214`. |
| `MixinPlayerChunkSender` | `PlayerChunkSender` | **PORTS-CLEAN** | All five targets exist with same shapes: `markChunkPendingToSend(LevelChunk)` `net/minecraft/server/network/PlayerChunkSender.java:40`, `dropChunk(ServerPlayer, ChunkPos)` `:44`, `sendNextChunks(ServerPlayer)` `:50`, `onChunkBatchReceivedByClient(float)` `:114`, `isPending(long)` `:128` | The sendNextChunks inject-cancel (kept for Fabric-API coexistence) still applies; NeoForge-side coexistence must be checked separately (inventory risk note 9). |
| `MixinPlayerTicketTracker` | `DistanceManager$PlayerTicketTracker` | **PORTS-CLEAN** (with a coverage gap — see note) | Inner class survives: `private class PlayerTicketTracker extends DistanceManager.FixedPlayerDistanceChunkTracker` `DistanceManager.java:241`. All four cancelled methods exist: `onLevelChange(long,int,int)` `:253`, `updateViewDistance(int)` `:257`, `onLevelChange(long,int,boolean,boolean)` `:267`, `runAllUpdates()` `:290` | String target still resolves (class is now `private` — mixins can target private inner classes). **WARNING (new uncovered vanilla path):** `DistanceManager.addPlayer` now directly adds a `TicketType.PLAYER_SIMULATION` ticket per player chunk (`this.ticketStorage.addTicket(new Ticket(TicketType.PLAYER_SIMULATION, this.getPlayerTicketLevel()), chunk)` `DistanceManager.java:115`, removed at `:127`) — player-based chunk loading is no longer exclusively routed through `PlayerTicketTracker`. Cancelling the four methods kills the PLAYER_LOADING graph but NOT this simulation ticket; the takeover needs a decision on this path (kept vanilla vs. also disabled). |
| `MixinServerGamePacketListenerImpl_ChunkSync` | `ServerGamePacketListenerImpl` | **PORTS-CLEAN** | `handleChunkBatchReceived(ServerboundChunkBatchReceivedPacket)` at `net/minecraft/server/network/ServerGamePacketListenerImpl.java:2167`; anchor invocation `this.chunkSender.onChunkBatchReceivedByClient(packet.desiredChunksPerTick())` `:2169` | Anchor method + `desiredChunksPerTick()` both intact. |

## 3. `entity_sync/` — 6

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `MixinChunkMap_E` | `ChunkMap` | **PORTS-CLEAN** | `addEntity(Entity)` `ChunkMap.java:1140` with redirect site `trackedEntity.updatePlayers(this.level.players())` `:1152`; `removeEntity(Entity)` `:1166` (HEAD-cancel fine); `tick()` `:1181` (HEAD-cancel fine); `entityMap` (`Int2ObjectMap<ChunkMap.TrackedEntity>`) `:146`; `getUpdatingChunkIfPresent(long)` `:251`; `updatePlayerStatus(ServerPlayer, boolean)` `:1033` | All hook sites and shadowed members intact. |
| `MixinPersistentEntitySectionManager` | `PersistentEntitySectionManager` | **PORTS-CLEAN** | class at `net/minecraft/world/level/entity/PersistentEntitySectionManager.java:32` | Marker duck only. |
| `MixinServerEntity` | `ServerEntity` | **NEEDS-RETARGET** | `sendChanges()` `net/minecraft/server/level/ServerEntity.java:88` (both HEAD injects fine); `positionCodec` (`VecDeltaCodec`) `:61`; `Entity.trackingPosition()` `Entity.java:3689`; `Entity.getAddEntityPacket(ServerEntity)` `Entity.java:3672`. Redirect sites: `removePairing(ServerPlayer)` still calls `player.connection.send(...)` `:260-263` and `addPairing(ServerPlayer)` still calls `player.connection.send(new ClientboundBundlePacket(packets))` `:265-270` — the IP target string `ServerGamePacketListenerImpl.send(Packet)` (IP `MixinServerEntity.java:48,65`) still matches. **`broadcastAndSend(Packet)` is GONE** — `ServerEntity` now sends everything through a `ServerEntity.Synchronizer` field (interface at `:357-362`; e.g. `this.synchronizer.sendToTrackingPlayers(...)` `:129,:182,:191,:197,:215`, `sendToTrackingPlayersAndSelf` `:225,:344,:350`) | The `broadcastAndSend` redirect must move to the Synchronizer implementation (`ChunkMap.TrackedEntity.sendToTrackingPlayersAndSelf`, `ChunkMap.java:1352-1357` — already covered territory of `MixinTrackedEntity`, see below). The Portal-skip + force-redirect assert on `sendChanges` HEAD port clean. |
| `MixinServerGamePacketListenerImpl_Redirect` | `ServerCommonPacketListenerImpl` (name misleading) | **NEEDS-RETARGET** | `send` second param changed type: `public void send(Packet<?> packet, @Nullable ChannelFutureListener listener)` `ServerCommonPacketListenerImpl.java:160` (was `PacketSendListener`); inner anchor changed identically: `Connection.send(Packet<?>, @Nullable ChannelFutureListener, boolean)` `net/minecraft/network/Connection.java:281` (invoked at `ServerCommonPacketListenerImpl.java:168`) | Same shape, new descriptor: `@ModifyVariable` HEAD on the packet arg and the `@Inject` at `INVOKE Connection.send(...)Z→V` both just need the `ChannelFutureListener` descriptor. `PacketSendListener` still exists as a helper class (`net/minecraft/network/PacketSendListener.java`) producing `ChannelFutureListener`s. |
| `MixinServerPlayer` | `ServerPlayer` | **NEEDS-RETARGET** | `enteredNetherPosition` `net/minecraft/server/level/ServerPlayer.java:260`; `triggerDimensionChangeTriggers(ServerLevel)` `:1157` (private, same sig). Riding changed: 2-arg `startRiding(Entity, boolean)` GONE — Entity has `startRiding(Entity)` `Entity.java:2406` and `startRiding(Entity, boolean force, boolean sendEventAndTriggers)` `Entity.java:2414`; `ServerPlayer` overrides the 3-arg form `:2122-2136` (sends `connection.teleport(...)` + passenger packet). `ServerPlayer` **no longer overrides `stopRiding`** (grep: no hit; `LivingEntity.stopRiding` `LivingEntity.java:3264` → `Entity.stopRiding` `Entity.java:2476` → `removeVehicle()`); the player-specific dismount packets moved to the `removeVehicle()` override `ServerPlayer.java:2137-2150` | `ip_startRidingWithoutTeleportRequest` must call the 3-arg super (`super.startRiding(v, true, <flag>)` — the `sendEventAndTriggers` value must be derived from `Entity.java:2414`'s body to match IP's intended side effects). `ip_stopRidingWithoutTeleportRequest`'s bypass is now INEFFECTIVE as written: `super.stopRiding()` reaches `Entity.stopRiding` which virtually calls `removeVehicle()`, dispatching to `ServerPlayer.removeVehicle` (`:2138`) — the packets IP wanted to skip. The bypass must retarget `removeVehicle`. `portal_worldChanged` shadows port clean. |
| `MixinTrackedEntity` | `ChunkMap$TrackedEntity` | **NEEDS-RETARGET** | Inner class survives but is now `private class TrackedEntity implements ServerEntity.Synchronizer` `ChunkMap.java:1320`. Renames: `broadcast(Packet)` → `sendToTrackingPlayers(Packet<? super ClientGamePacketListener>)` `:1344-1349` (inner redirect site `connection.send(packet)` = `ServerPlayerConnection.send` `:1347`; interface method at `net/minecraft/server/network/ServerPlayerConnection.java:9`); `broadcastAndSend(Packet)` → `sendToTrackingPlayersAndSelf(...)` `:1351-1357` (inner site `player.connection.send(packet)` `:1355`); **NEW third send path** `sendToTrackingPlayersFiltered(Packet, Predicate<ServerPlayer>)` `:1359-1366` (used by `ServerEntity.sendChanges` at `ServerEntity.java:93`). Overwrite targets survive: `updatePlayer(ServerPlayer)` `:1383`, `updatePlayers(List<ServerPlayer>)` `:1425`. Members survive: `serverEntity` `:1321`, `entity` `:1322`, `lastSectionPos` `:1324`, `seenBy` `:1325`, `getEffectiveRange()` `:1412` (private), `broadcastRemoved()` `:1368`, `removePlayer(ServerPlayer)` `:1374` | Rename the two redirect methods AND add coverage for `sendToTrackingPlayersFiltered` — otherwise every `sendChanges` movement packet routed through the filtered path escapes the force-redirect wrap. Note `removePlayer`/`updatePlayer` now also touch `level.debugSynchronizers()` (`:1378,:1397-1400`) — the `ip_updateEntityTrackingStatus` re-implementation should be re-derived from the 26.2 `updatePlayer` body (`:1383-1406`). |

## 4. `position_sync/` — 7

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `MixinPlayerPositionLookS2CPacket` | `ClientboundPlayerPositionPacket` | **TARGET-GONE** | The packet is a record with a composite codec and **no `write(FriendlyByteBuf)` method**: full class body is `record ClientboundPlayerPositionPacket(int id, PositionMoveRotation change, Set<Relative> relatives)` + `STREAM_CODEC = StreamCodec.composite(...)` at `net/minecraft/network/protocol/game/ClientboundPlayerPositionPacket.java:12-21` (+`of` factory `:24`, `type()` `:28`, `handle` `:33`) — verified by reading the whole file | The duck field (get/set) can still be mixed onto the record class, but the appended-bytes hook has no method to inject. 26.2 mechanism: the dimension stamp must ride the **`STREAM_CODEC`** — wrap the codec built at `:13-21` (e.g. `@ModifyExpressionValue`/`@Redirect` on the `StreamCodec.composite` call in `<clinit>`, wrapping `encode` to append `writeResourceKey` and `decode` to read it — `FriendlyByteBuf.writeResourceKey` `net/minecraft/network/FriendlyByteBuf.java:591` / `readResourceKey` `:586` both survive). Client-side read counterpart lives in the client slice and has the same problem. UNKNOWN-NEEDS-DESIGN on the exact wrapping idiom; the codec is the only serialization path. |
| `MixinServerboundMovePlayerPacket_S` | `ServerboundMovePlayerPacket` | **PORTS-CLEAN** | abstract base class at `net/minecraft/network/protocol/game/ServerboundMovePlayerPacket.java:9` | Duck-field holder only, no injections. |
| `MixinServerboundMovePlayerPacketPos` | `ServerboundMovePlayerPacket$Pos` | **PORTS-CLEAN** | `private static ServerboundMovePlayerPacket.Pos read(FriendlyByteBuf)` `:113` (class `Pos` `:100`; codec built from it via `Packet.codec(Pos::write, Pos::read)` `:101-103`) | The static `read` factory the RETURN-inject targets still exists (unlike the S2C packet, these classes kept hand-written read/write referenced by `Packet.codec`). |
| `MixinServerboundMovePlayerPacketPosRot` | `...$PosRot` | **PORTS-CLEAN** | `read(FriendlyByteBuf)` `:149` (class `:136`) | Same. |
| `MixinServerboundMovePlayerPacketRot` | `...$Rot` | **PORTS-CLEAN** | `read(FriendlyByteBuf)` `:185` (class `:176`) | Same. |
| `MixinServerboundMovePlayerPacketStatusOnly` | `...$StatusOnly` | **PORTS-CLEAN** | `read(FriendlyByteBuf)` `:215` (class `:206`) | Same. |
| `MixinServerGamePacketListenerImpl` (priority 900) | `ServerGamePacketListenerImpl` | **NEEDS-RETARGET** | (1) `handleMovePlayer(ServerboundMovePlayerPacket)` `ServerGamePacketListenerImpl.java:1064` with anchor `PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level())` `:1065` (3-arg `(Packet,T,ServerLevel)` overload survives, `net/minecraft/network/protocol/PacketUtils.java:17`) — inject ports. (2) **teleport overwrite**: `teleport(double,double,double,float,float,Set<RelativeMovement>)` GONE; 26.2 has `teleport(double,double,double,float,float)` `:1257` delegating to `teleport(PositionMoveRotation, Set<Relative>)` `:1261-1270` (`RelativeMovement` class deleted — `net/minecraft/world/entity/RelativeMovement.java` absent; replaced by `net/minecraft/world/entity/Relative.java`; `PositionMoveRotation` at `net/minecraft/world/entity/PositionMoveRotation.java`). New body is small: `teleportSetPosition` + `awaitingPositionFromClient = player.position()` + `send(ClientboundPlayerPositionPacket.of(this.awaitingTeleport, destination, relatives))`. Also new extracted helper `updateAwaitingTeleport()` `:1223`. (3) **anti-cheat**: `isPlayerCollidingWithAnythingNew` renamed/re-signatured → `private boolean isEntityCollidingWithAnythingNew(LevelReader, Entity, AABB, double, double, double)` `:1243-1255`, body now uses `level.getPreMoveCollisions(entity, newAABB.deflate(1e-5), oldAABB.getBottomCenter())` `:1245` (still `Shapes.joinIsNotEmpty(shape, oldShape, BooleanOp.AND)` `:1249`). (4) `tick()` `:293`, `clientIsFloating` `:262` (float-kick block `:322-329`). (5) `handleAcceptTeleportPacket` `:531`; the `absMoveTo` anchor is now **`absSnapTo`** (`this.player.absSnapTo(...)` `:539-546`); note new `player.hasChangedDimension()` call `:550`. `handlePlayerCommand` `:1717` still PUTFIELDs `awaitingPositionFromClient` `:1731`. Fields all survive: `tickCount` `:241`, vehicle fields `:252-258`, `awaitingPositionFromClient/awaitingTeleport/awaitingTeleportTime` `:259-261`, `clientVehicleIsFloating` `:264` | The `@Overwrite teleport` must be re-copied from the 26.2 `teleport(PositionMoveRotation, Set<Relative>)` body (`:1261-1270`) — the dimension stamp then applies to the `ClientboundPlayerPositionPacket` built by `.of(...)` `:1269` (which is itself TARGET-GONE for the write hook, see above). The anti-cheat HEAD-cancel re-anchors to `isEntityCollidingWithAnythingNew` and its replacement body must be re-derived against `getPreMoveCollisions`. The accept-teleport inject re-anchors from `absMoveTo` to `absSnapTo`. |

## 5. `collision/` — 8

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IEEntity_Collision` | `Entity` (invoker `collideWithShapes`) | **PORTS-CLEAN** | `private static Vec3 collideWithShapes(Vec3, AABB, List<VoxelShape>)` `net/minecraft/world/entity/Entity.java:1235` | Same signature. |
| `MixinAbstractArrow` | `AbstractArrow` | **PORTS-CLEAN** | class moved to `net/minecraft/world/entity/projectile/arrow/AbstractArrow.java:56` | Inert (body commented out); only the import/package changes. |
| `MixinAbstractMinecartEntity` | `AbstractMinecart` | **TARGET-GONE** | `lerpTo` no longer exists on `AbstractMinecart` or `Entity` (grep of `Entity.java` finds only `getInterpolation` `:2550`, `:2522`, `:2538`); minecart exposes `public InterpolationHandler getInterpolation()` `net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java:341` (class moved to `vehicle/minecart/`) | The lerp system was replaced by `InterpolationHandler` (`net/minecraft/world/entity/InterpolationHandler.java`). The debug snap-instead-of-interpolate hook (gated by `IPGlobal.allowClientEntityPosInterpolation`) would re-anchor inside `InterpolationHandler`'s position-apply, or on the minecart's `getInterpolation` consumer. Debug-only feature — decision needed on carry vs. drop-list (fidelity default: carry, re-anchored). |
| `MixinEntity` | `Entity` | **NEEDS-RETARGET** | Survives: `move(MoverType, Vec3)` `:712` with redirect site `Vec3 movement = this.collide(delta)` `:739`; `private Vec3 collide(Vec3)` `:1138`; `fireImmune()` `:1562`; `isInWall()` `:2235`; `setPosRaw(double,double,double)` `:3786`; `getInBlockState()` `:3698`; fields `level` `:215`, `position` `:219`, `blockPosition` `:220`, `chunkPosition` `:221`, `inBlockState` `:296`. **GONE:** the `getBoundingBox()` redirect inside `checkInsideBlocks` — the method was rearchitected into movement-segment form: `private void checkInsideBlocks(List<Entity.Movement>, InsideBlockEffectApplier.StepBasedCollector)` `:1269` → per-segment `checkInsideBlocks(Vec3 from, Vec3 to, ...)` `:1299` which builds its box via `this.makeBoundingBox(to).deflate(1.0E-5F)` `:1302` and walks blocks with `BlockGetter.forEachBlockIntersectedBetween(from, to, box, ...)` `:1307`; entry point `applyEffectsFromBlocks(List<Entity.Movement>)` `:948-959` | The active-collision-box substitution for block-inside effects (fire/powder-snow/portal triggers only inside the portal-clipped box) has NO direct anchor anymore — the faithful equivalent is clipping the box produced at `:1302` (and/or the `from`/`to` segment) against the portal plane; needs design. Everything else ports with anchors intact. `ip_setPositionWithoutTriggeringCallback` (`@IPVanillaCopy` of `setPosRaw`) must be re-copied: 26.2 `setPosRaw` gained waypoint-update code after `levelCallback.onMove()` (`:3800-3809`). The `onMove` anchor itself survives (`:3800`). |
| `MixinPlayer_Collision` | `Player` | **PORTS-CLEAN** | `protected boolean canPlayerFitWithinBlocksAndEntitiesWhen(Pose)` `net/minecraft/world/entity/player/Player.java:373`, body still `noCollision(this, getDimensions(newPose).makeBoundingBox(position()).deflate(1.0E-7))` `:374` | Overwrite re-copy is a one-liner, same shape. |
| `MixinProjectile` | `Projectile` | **TARGET-GONE** | `getOwner()` no longer calls `ServerLevel.getEntity(UUID)`: it is `EntityReference.getEntity(this.owner, this.level())` `net/minecraft/world/entity/projectile/Projectile.java:61-62` (`owner` is `EntityReference<Entity>`), and `EntityReference.getEntity(Level, Class)` resolves via `level::getEntityInAnyDimension` (`net/minecraft/world/entity/EntityReference.java:78-81`), i.e. `ServerLevel.getEntityInAnyDimension(UUID)` `net/minecraft/server/level/ServerLevel.java:1364` which searches other levels (`:1372`) | **The role is now vanilla**: cross-dimension owner resolution is built in (`Level.getEntityInAnyDimension` `Level.java:785`). The all-levels-search redirect has nothing to fix; drop-list candidate (verify no IP behavior depends on the redirect's player-list fallback — 26.2's pearl has its own `findOwnerIncludingDeadPlayer` `ThrownEnderpearl.java:73-76`). Note: `MixinProjectile extends MixinEntity` (mixin inheritance) — if dropped, keep the hierarchy note in mind. |
| `MixinThrowableProjectile` | `ThrowableProjectile` | **PORTS-CLEAN** | class at `net/minecraft/world/entity/projectile/ThrowableProjectile.java:13` | Inert (fully commented out). |
| `MixinThrownEnderPearl` | `ThrownEnderpearl` | **NEEDS-RETARGET** | Class moved: `net/minecraft/world/entity/projectile/throwableitemprojectile/ThrownEnderpearl.java`. `onHit(HitResult)` `:85` survives; `discard()` invocations inside it survive (both branches near the end of `onHit`) | Retarget = package rename in the `@Mixin` reference + the `discard()` target descriptor. **Semantics re-check required:** 26.2 vanilla `onHit` already teleports the owner cross-dimension itself via `player.teleport(new TeleportTransition(level, teleportPos, ...))` (`:119-127` for players, `:133-135` for non-players) where `level` is the PEARL's level — IP's inject (which fires `ServerTeleportationManager.teleportEntityGeneral` when owner level ≠ pearl level) now runs after a vanilla cross-dim teleport has already happened; IP's 1.21.3 code faces the same vanilla behavior, so port as-is but flag for the teleportation-slice owner. |

## 6. `container_gui/` — 3

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `MixinAbstractContainerMenu` | `AbstractContainerMenu` | **NEEDS-RETARGET** | The reach-check lambda still exists inside `protected static boolean stillValid(ContainerLevelAccess, Player, Block)` `net/minecraft/world/inventory/AbstractContainerMenu.java:93-95`: `access.evaluate((level, pos) -> !level.getBlockState(pos).is(block) ? false : player.isWithinBlockInteractionRange(pos, 4.0), true)`. The wrapped operation renamed: `Player.canInteractWithBlock(BlockPos,double)` is GONE (zero grep hits in `Player.java`) → `public boolean isWithinBlockInteractionRange(BlockPos, double)` `net/minecraft/world/entity/player/Player.java:1993` | Two retargets: (a) the intermediary lambda name `method_17696` will not resolve under Mojang-mapped 26.2 (inventory risk note 4) — re-anchor to the Mojang-mapped synthetic lambda of `stillValid` (`lambda$stillValid$...`) or use a `@WrapOperation` scoped by the outer method; (b) the wrapped call becomes `isWithinBlockInteractionRange`. |
| `MixinContainer` | `Container` (interface) | **NEEDS-RETARGET** | `static boolean stillValidBlockEntity(BlockEntity, Player, float)` `net/minecraft/world/Container.java:94-101` survives; the wrapped op is now `player.isWithinBlockInteractionRange(worldPosition, distanceBuffer)` `:99` | Only the operation rename (`canInteractWithBlock` → `isWithinBlockInteractionRange`). |
| `MixinContainerOpenersCounter` | `ContainerOpenersCounter` | **NEEDS-RETARGET** | `getPlayersWithContainerOpen` is GONE → `public List<ContainerUser> getEntitiesWithContainerOpen(Level, BlockPos)` `net/minecraft/world/level/block/entity/ContainerOpenersCounter.java:51-58` (AABB scan radius now `maxInteractionRange + 4.0` `:52`); membership test moved from `isOwnContainer` to `private boolean hasContainerOpen(Entity, BlockPos)` `:60-63` which calls `ContainerUser.hasContainerOpen(this, blockPos)`; `isOwnContainer(Player)` still exists abstract `:26`; consumer `recheckOpeners` `:66-94` also recomputes `maxInteractionRange` from `ContainerUser.getContainerInteractionRange()` | The HEAD-cancel replacement must re-implement against the new name, return type (`List<ContainerUser>`, no longer players-only — non-player `ContainerUser`s exist) and the `hasContainerOpen`/interaction-range recompute semantics. |

## 7. `debug/` — 9

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IEChunkHolder_Debug` | `ChunkHolder` (invoker `updateFutures`) | **PORTS-CLEAN** | `protected void updateFutures(ChunkMap, Executor)` `ChunkHolder.java:273` | — |
| `MixinChunkTaskPriorityQueue` | `ChunkTaskPriorityQueue` | **PORTS-CLEAN** | class at `net/minecraft/server/level/ChunkTaskPriorityQueue.java:10` | Inert. (Internals changed — `TasksForChunk` record `:89` — irrelevant while commented out.) |
| `MixinClientboundSectionBlocksUpdatePacket_Debug` | `ClientboundSectionBlocksUpdatePacket` | **PORTS-CLEAN** | class at `net/minecraft/network/protocol/game/ClientboundSectionBlocksUpdatePacket.java:15` | Inert. |
| `MixinDistanceManager_Debug` | `DistanceManager` | **PORTS-CLEAN** | class exists (`DistanceManager.java:34`) | Inert. |
| `MixinHashMapPalette` | `HashMapPalette` | **PORTS-CLEAN** | `write` survives (signature grew a param: `public void write(FriendlyByteBuf, IdMap<T>)` `net/minecraft/world/level/chunk/HashMapPalette.java:78`); the redirected invocation `IdMap.getId` still inside: `globalMap.getId(this.values.byId(i))` `:83` | IP's `method = "write"` has no descriptor (IP `MixinHashMapPalette.java:13`) so it re-binds to the new signature; the `IdMap.getId(Object)I` target `:16` is unchanged. |
| `MixinLinearPalette` | `LinearPalette` | **PORTS-CLEAN** | `write(FriendlyByteBuf, IdMap<T>)` `net/minecraft/world/level/chunk/LinearPalette.java:87`; `globalMap.getId(this.values[i])` `:91` | Same as above. |
| `MixinPlayerTicketTracker_Debug` | `DistanceManager$PlayerTicketTracker` | **PORTS-CLEAN** | inner class `DistanceManager.java:241` | Inert. |
| `MixinServerChunkCacheMainThreadExecutor` | `ServerChunkCache$MainThreadExecutor` | **PORTS-CLEAN** | `private final class MainThreadExecutor extends BlockableEventLoop<Runnable>` `ServerChunkCache.java:592` | Inert. |
| `MixinServerLevel_Debug` | `ServerLevel` | **PORTS-CLEAN** | `private boolean addEntity(Entity)` `ServerLevel.java:994` | HEAD-cancel guard binds. |

## 8. `interaction/` — 4

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IEClipContext` | `ClipContext` (accessors `block`/`fluid`) | **PORTS-CLEAN** | `block` `ClipContext.java:24`, `fluid` `:25` | — |
| `MixinBucketItem` | `BucketItem` | **PORTS-CLEAN** | `public InteractionResult use(Level, Player, InteractionHand)` `net/minecraft/world/item/BucketItem.java:43`; redirect target `getPlayerPOVHitResult(level, player, this.getFluidContext())` `:45`, declared `protected static BlockHitResult getPlayerPOVHitResult(Level, Player, ClipContext.Fluid)` `net/minecraft/world/item/Item.java:342` | Method + redirect target + the `Level` param (for the `@Local LocalRef<Level>` rewrite) all intact. |
| `MixinItem_Interaction` | `Item` | **PORTS-CLEAN** | class at `net/minecraft/world/item/Item.java:99` | Inert. |
| `MixinServerPlayerGameMode` | `ServerPlayerGameMode` | **NEEDS-RETARGET** | Fields survive: `level` `net/minecraft/server/level/ServerPlayerGameMode.java:37`, `player` `:38`, `isDestroyingBlock` `:41`, `destroyPos` `:43` (PUTFIELD `:208`), `hasDelayedDestroy` `:45`, `delayedDestroyPos` `:46`. Methods survive: `tick()` `:107`, `incrementDestroyProgress(BlockState, BlockPos, int)` `:132` (GETFIELD `level` `:137`, `player.level()` `:134`), `handleBlockBreakAction(BlockPos, Action, Direction, int, int)` `:150` (GETFIELD `level` `:154,:158,:164,...`; `player.level()` `:196,:219`), `destroyAndAck` (called `:171,:200,:223`), `destroyBlock(BlockPos)` `:260`, `useItemOn(ServerPlayer, Level, ItemStack, InteractionHand, BlockHitResult)` `:347` with `new UseOnContext(player, hand, hitResult)` `:383` (ctor at `net/minecraft/world/item/context/UseOnContext.java:20`). **One rename:** the reach check in `handleBlockBreakAction` is now `this.player.isWithinBlockInteractionRange(pos, 1.0)` `:151` | Only the `@WrapOperation canInteractWithBlock` re-targets to `isWithinBlockInteractionRange` (`Player.java:1993`). Everything else binds as-is. |

## 9. `mc_util/` — 4

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IELevelEntityGetterAdapter` | `LevelEntityGetterAdapter` | **PORTS-CLEAN** | `visibleEntities` (`EntityLookup<T>`) `net/minecraft/world/level/entity/LevelEntityGetterAdapter.java:10`; `sectionStorage` (`EntitySectionStorage<T>`) `:11` | — |
| `MixinEntitySection` | `EntitySection` | **PORTS-CLEAN** | `storage` (`ClassInstanceMultiMap<T>`) `net/minecraft/world/level/entity/EntitySection.java:14`; model method `getEntities(EntityTypeTest, AABB, AbortableIterationConsumer)` `:40-41` (uses `storage.find(type.getBaseClass())`); `getStatus()` (`Visibility`) `:64` | `ip_traverse` is `@IPVanillaCopy` — re-derive from the 26.2 `getEntities` body. |
| `MixinEntitySectionStorage` | `EntitySectionStorage` | **PORTS-CLEAN** | `sections` `net/minecraft/world/level/entity/EntitySectionStorage.java:29`; `sectionIds` (`LongSortedSet`) `:30`; model method `forEachAccessibleNonEmptySection(AABB, AbortableIterationConsumer)` `:37` with the per-X-slab `sectionIds.subSet(lo, hi + 1)` trick intact `:48` (also `getChunkSections` `:79-83`) | `ip_traverseSectionInBox` is `@IPVanillaCopy` — re-derive from the 26.2 body. |
| `MixinEntity_U` | `Entity` | **PORTS-CLEAN** | `setPosRaw` inner anchor `this.levelCallback.onMove()` `Entity.java:3800`; `setRemoved(Entity.RemovalReason)` `:3912` (RETURN inject on a final method is fine) | The Portal entity's own move/remove event spine binds unchanged. |

## 10. `miscellaneous/` — 5

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IEEndDragonFight` | `EndDragonFight` | **NEEDS-RETARGET** | Class **renamed** `EnderDragonFight` at `net/minecraft/world/level/dimension/end/EnderDragonFight.java` (old filename absent); `needsStateScanning` `:100`, `private void scanState()` `:221` | Class-name retarget only; both members intact. |
| `MixinBlockGetter` | `BlockGetter` (static `traverseBlocks`) | **PORTS-CLEAN** | `static <T, C> T traverseBlocks(Vec3, Vec3, C, BiFunction<C, BlockPos, T>, Function<C, T>)` `net/minecraft/world/level/BlockGetter.java:112` | `@ModifyVariable` HEAD argsOnly index 1 (the `to` Vec3) binds. |
| `MixinEnderDragon` | `EnderDragon` | **PORTS-CLEAN** | class at `net/minecraft/world/entity/boss/enderdragon/EnderDragon.java:55` | Duck-only. |
| `MixinFishingHook` | `FishingHook` | **PORTS-CLEAN** | class at `net/minecraft/world/entity/projectile/FishingHook.java:52` | Duck-only. |
| `MixinLeashable` | `Leashable` | **PORTS-CLEAN** | interface at `net/minecraft/world/entity/Leashable.java:28` | Inert. |

## 11. `networking/` — 1

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `MixinClientboundCustomPayloadPacket` | `ClientboundCustomPayloadPacket` | **PORTS-CLEAN** | `record ClientboundCustomPayloadPacket(CustomPacketPayload payload)` at `net/minecraft/network/protocol/common/ClientboundCustomPayloadPacket.java:15`; `public void handle(ClientCommonPacketListener)` `:33` | The HEAD-cancellable inject on `handle` and the `payload` shadow (record component field) both bind. This is the client unwrap hot path of the whole redirection system — the loader-side payload registration (Fabric `PayloadTypeRegistry` in IP) is separate multiloader work, not this mixin. |

## 12. `other_sync/` — 4

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IEServerConfigurationPacketListenerImpl` | `ServerConfigurationPacketListenerImpl` | **PORTS-CLEAN** | `private final GameProfile gameProfile` `net/minecraft/server/network/ServerConfigurationPacketListenerImpl.java:49` | — |
| `MixinMapItemSavedData` | `MapItemSavedData` | **PORTS-CLEAN** | class at `net/minecraft/world/level/saveddata/maps/MapItemSavedData.java:43` | Inert. |
| `MixinPlayerList` | `PlayerList` | **PORTS-CLEAN** | `sendLevelInfo(ServerPlayer, ServerLevel)` `net/minecraft/server/players/PlayerList.java:642`; `placeNewPlayer(Connection, ServerPlayer, CommonListenerCookie)` `:142`; `broadcastAll(Packet<?>, ResourceKey<Level>)` `:485`; `respawn(ServerPlayer, boolean, Entity.RemovalReason)` `:389` with redirect site `player.restoreFrom(serverPlayer, keepAllPlayerData)` `:396`; `broadcast(@Nullable Player, double,double,double,double, ResourceKey<Level>, Packet<?>)` `:603`; fields `server` `:114`, `players` `:115`; `ServerGamePacketListenerImpl.player` field survives (e.g. `connection.player` used at `PlayerChunkSender.java:83`) | All five hooks bind. The `broadcast` `@Overwrite` is IP's own re-implementation (same signature) — no vanilla re-copy needed, but diff the 26.2 body at `:603` for any new side effects before overwriting. |
| `MixinPlayer_Pose` | `Player` | **PORTS-CLEAN** | `protected void updatePlayerPose()` `net/minecraft/world/entity/player/Player.java:343` | — |

## 13. `portal_generation/` — 4

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `MixinItemEntity_P` | `ItemEntity` | **NEEDS-RETARGET** | `tick()` `net/minecraft/world/entity/item/ItemEntity.java:109` (TAIL inject fine). Shadowed `thrower` **changed type**: now `private @Nullable EntityReference<Entity> thrower` `:52` (IP shadows `UUID`, IP `MixinItemEntity_P.java:22`). Also the injected body calls `level().getProfiler()` (IP `:42,:50`) — `Level.getProfiler()` is GONE (zero grep hits in `Level.java`); 26.2 uses `Profiler.get()` (e.g. `Entity.java:727`, `ServerLevel.java:353`) | Shadow type update + profiler call rewrite (`Profiler.get().push/pop`). Hook site itself is clean. |
| `MixinItemStack` | `ItemStack` | **PORTS-CLEAN** | `public InteractionResult useOn(UseOnContext)` `net/minecraft/world/item/ItemStack.java:357` | RETURN inject binds. |
| `MixinMinecraftServer_P` | `MinecraftServer` | **PORTS-CLEAN** | class exists (`MinecraftServer.java`) | Inert. |
| `MixinPlayerList_P` | `PlayerList` | **PORTS-CLEAN** | class exists (`PlayerList.java`) | Inert. |

## 14. `registry/` — 1

| Mixin | Target | Verdict | 26.2 evidence | Notes |
|---|---|---|---|---|
| `IERegistryDataLoader` | `RegistryDataLoader` | **PORTS-CLEAN** | class at `net/minecraft/resources/RegistryDataLoader.java:81` | Inert (empty interface mixin). |

---

## Cross-cutting 26.2 facts this slice must absorb (non-mixin plumbing the mixins serve)

1. **Ticket system**: `DistanceManager.addRegionTicket/removeRegionTicket` are gone from `DistanceManager` (whole file read, 320 lines); ticket CRUD lives on `TicketStorage` (`addTicket(long,Ticket)` `TicketStorage.java:147`, `addTicketWithRadius(TicketType,ChunkPos,int)` `:138`, `removeTicketWithRadius` `:214`, `getTickets(long)` `:130`), a `SavedData` (`:33`). `Ticket` is non-generic. `ImmPtlChunkTickets`' add/remove/purge paths (`ImmPtlChunkTickets.java:233-241,:250-276,:294-312`) re-plumb onto this.
2. **Entity references**: `EntityReference<T>` + `Level.getEntityInAnyDimension(UUID)` (`Level.java:785`) / `getPlayerInAnyDimension` (`:789`) mean several vanilla systems (projectile owner, aggro target, item thrower) now resolve UUIDs across dimensions natively — audit each IP cross-dimension patch in this slice for obsolescence-by-vanilla before porting (confirmed for `MixinProjectile`; flagged for `MixinThrownEnderPearl`, `MixinLivingEntity`).
3. **`ServerEntity.Synchronizer`** (`ServerEntity.java:357-362`): entity sync now flows through an injectable interface implemented by `ChunkMap.TrackedEntity` (`ChunkMap.java:1320`). This is a structurally cleaner seam than 1.21.3's hard-wired `seenBy` loops — IP's redirects still work, but a fidelity-preserving port could implement redirection at the Synchronizer boundary; either way the NEW `sendToTrackingPlayersFiltered` path (`:1359`, used by `sendChanges` `ServerEntity.java:93`) MUST be covered.
4. **`PacketSendListener` → `ChannelFutureListener`** in all send-path signatures (`ServerCommonPacketListenerImpl.java:160`, `Connection.java:277-281`).
5. **Weather** is per-server `WeatherData` applied per-level behind `Level.canHaveWeather()` (`Level.java:857`, `ServerLevel.java:702,713`), with per-dimension gradient packets (`ServerLevel.java:774-783`) except the rain-flip broadcast (`:785-795`) which is still un-dimensioned.
6. **Render-related**: nothing in this slice touches the render pipeline; MIGRATION_API_MAP.md's submit→prepare→execute rewrite does not affect these targets (verified: no touchpoint class in this slice appears in that map).
