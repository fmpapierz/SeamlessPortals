# 26.2 API Map — Ducks, API, Commands, Small Packages

Slice inventory: `migration/inventory/ducks-api-misc.md` (§4 touchpoint list).
26.2 citations are `path/File.java:line` relative to `C:/Users/warwa/ModDev/mc262-ref` (Mojang mappings, decompiled 26.2 vanilla).
Render-pipeline verdicts defer to the pre-verified `MIGRATION_API_MAP.md` (cited as `MAP §<section>`); every other verdict below was read directly from the 26.2 source.

**Global renames that touch everything in this slice** (verified, listed once, not repeated per row):
- `ResourceLocation` → **`Identifier`** (`net/minecraft/resources/Identifier.java` exists; no `ResourceLocation.java`). Affects argument-type registration ids (`imm_ptl:axis` etc.), `ImageWidget.texture(... Identifier ...)` (ImageWidget.java:21), everything.
- `ChunkPos.asLong()`/`toLong()` → **`pack()`** — instance `ChunkPos.pack()` (ChunkPos.java:69), static `pack(int,int)` (:73), `pack(BlockPos)` (:81). Vanilla usage: `ChunkPos.pack(pos)` LevelChunk.java:417, `ChunkPos.unpack(node)` ChunkMap.java:386.
- `Level.getMinBuildHeight()/getMaxBuildHeight()` → **`getMinY()/getMaxY()`** (LevelHeightAccessor.java:9-12). **SEMANTIC TRAP: `getMaxY()` is INCLUSIVE** (`getMinY() + getHeight() - 1`, :11-12); old `getMaxBuildHeight` was exclusive. 26.2 vanilla compares `pos.getY() > maxY` (ServerGamePacketListenerImpl.java:1362) where 1.21.3 compared `>= getMaxBuildHeight()`. Every `@IPVanillaCopy` re-diff and `BlockManipulation` height check must adjust the off-by-one.
- `net.minecraft.Util` → **`net.minecraft.util.Util`** (`net/minecraft/util/Util.java`; `backgroundExecutor()` :252, returns `TracingExecutor`).

---

## 1. GONE (no compatible member — replacement mechanism cited)

| # | IP usage (where) | 26.2 fate + replacement | Migration note |
|---|---|---|---|
| G1 | `MultiBufferSource` in `IEWorldRenderer.ip_myRenderEntity(...)` (ducks/IEWorldRenderer.java:19-27) | **GONE** — no `MultiBufferSource.java` anywhere in mc262-ref (find verified). Replaced by submit→prepare→execute: `SubmitNodeCollector`/`OrderedSubmitNodeCollector` backed by `SubmitNodeStorage`, drawn by `FeatureRenderDispatcher.prepareFrame(...)`/`renderAllFeatures(...)` (MAP §RenderBuffers "What replaced the MultiBufferSource/BufferSource batched model"). | The duck method's signature must be redesigned by the render slice; per-entity render now = extraction into `LevelRenderState.entityRenderStates` + feature submission. Escape hatch for raw geometry: `submitCustomGeometry(PoseStack, RenderType, CustomGeometryRenderer)` (OrderedSubmitNodeCollector.java:184) whose callback receives `(PoseStack.Pose, VertexConsumer)` (SubmitNodeCollector.java:12-14). |
| G2 | `CompiledShaderProgram` + uniform location int in `IEShader.ip_getClippingEquationUniformLocation()` (mixin/client/render/shader/MixinShaderInstance.java:19) | **GONE** — no `CompiledShaderProgram.java`, no `ShaderInstance.java` in 26.2 (find verified). Shaders are `RenderPipeline`s; uniforms are declared on `BindGroupLayout` (`withBindGroupLayout`, BindGroupLayout.builder().withUniform(name, UniformType.UNIFORM_BUFFER)) and set per-pass via `RenderPass.setUniform` (MAP §RenderPipeline.Builder, §RenderType immediate draw). There is no GL uniform-location int anymore. | The portal clipping-plane mechanism needs a full re-map by the render slice: custom `RenderPipeline` with a clipping uniform in a `BindGroupLayout`, value bound via `RenderPass.setUniform`. **UNKNOWN-NEEDS-DESIGN** (owned by render-core slice; this slice only consumes the toggle from ClientDebugCommand `front_clipping_enable`). |
| G3 | `LightTexture` in `IEGameRenderer.ip_setLightmapTextureManager(LightTexture)` (ducks/IEGameRenderer, MixinGameRenderer.java:45) | **GONE** — no `LightTexture.java`. Replaced by **`Lightmap`** (net/minecraft/client/renderer/Lightmap.java:25), `getTextureView()` → `GpuTextureView` (:52), `render(LightmapRenderState)` (:63). GameRenderer holds `private final Lightmap lightmap` (GameRenderer.java:112) + `LightmapRenderStateExtractor` (:113) + a separate `UiLightmap` (:114); getters `lightmap()`/`levelLightmap()` return `GpuTextureView` (MAP §GameRenderer bonus getters :661/:665). | Duck becomes a `@Mutable @Accessor` on the final `lightmap` field, or (more faithful to what IP used it for — swapping the lightmap per-dimension) the render slice decides whether per-world `Lightmap` instances are needed at all under the extract/render split. |
| G4 | `LevelRenderer.renderLineBox(PoseStack, VertexConsumer, 6×double, 4×float)` (WireRenderingHelper.java:64-74) | **GONE** — zero grep hits for `renderLineBox` in all of mc262-ref (also no `ShapeRenderer.java`). Nearest vanilla line-drawing surfaces: `submitShapeOutline(PoseStack, VoxelShape, RenderType, int color, float width, boolean afterTerrain)` (OrderedSubmitNodeCollector.java:171) and free-form `submitCustomGeometry(PoseStack, RenderType, CustomGeometryRenderer)` (:184) → callback gets a `VertexConsumer` (SubmitNodeCollector.java:12-14). | WireRenderingHelper's own `putLine`-style vertex emission survives (VertexConsumer intact, see S2) — only the *box helper* and the *acquisition of a LINES VertexConsumer* change: emit the 12 box edges manually inside a `submitCustomGeometry` callback. Whether a LINES-topology `RenderType` still exists is a render-slice question (`PrimitiveTopology.LINES` exists, MAP §PrimitiveTopology). |
| G5 | `GuiGraphics` (`drawCenteredString`) — GuiPortalScreen (ExampleGuiPortalRendering) + IPortalInitialScreen | **GONE** — no `GuiGraphics.java`. Replaced by **`GuiGraphicsExtractor`** (net/minecraft/client/gui/GuiGraphicsExtractor.java:88): `drawCenteredString` → `centeredText(Font, String/Component/FormattedCharSequence, x, y, color)` (:262-271); `drawString` → `text(...)` (:233-258); `fill`/`blit` survive on it (:181, :296-369). `Screen.render(GuiGraphics,...)` is gone: screens implement `extractRenderState(GuiGraphicsExtractor graphics, int mouseX, int mouseY, float a)` (Screen.java:116; background :377). | Both Screens in this slice re-target `extractRenderState` + `GuiGraphicsExtractor`. Widget tree building (`init()`, `addRenderableWidget`) is unchanged (Screen.java:365, :202). |
| G6 | `CommandSourceStack.hasPermission(int)` — `canUsePortalCommand` (PortalCommand.java:146-157) + every perm-gated literal (perm 2–4) | **GONE** — no int-level check on CommandSourceStack. 26.2: `CommandSourceStack.permissions()` → `PermissionSet` (CommandSourceStack.java:377; `PermissionSet.hasPermission(Permission)`, net/minecraft/server/permissions/PermissionSet.java:3-8). Command gating: `.requires(Commands.hasPermission(PermissionCheck))` (Commands.java:541) with constants `Commands.LEVEL_ALL/LEVEL_MODERATORS/LEVEL_GAMEMASTERS/LEVEL_ADMINS/LEVEL_OWNERS` (Commands.java:163-167), e.g. vanilla `/tp` requires `Commands.hasPermission(Commands.LEVEL_GAMEMASTERS)` (TeleportCommand.java:40). Levels map: old 2 → `LEVEL_GAMEMASTERS` (`Permissions.COMMANDS_GAMEMASTER` = `HasCommandLevel(PermissionLevel.GAMEMASTERS)`, Permissions.java:7), 3 → `LEVEL_ADMINS` (:8), 4 → `LEVEL_OWNERS` (:9). | `canUsePortalCommand`'s ad-hoc predicate becomes `source.permissions().hasPermission(Permissions.COMMANDS_GAMEMASTER)` OR the creative-ease check; sub-literal `.requires(...)` gates use `Commands.hasPermission(...)`. |
| G7 | `InteractionResult.shouldSwing()` (BlockManipulationServer.doProcessUseItemOn, :233-286) | **GONE** — replaced by the pattern `result instanceof InteractionResult.Success success && success.swingSource() == InteractionResult.SwingSource.SERVER` (`record Success(SwingSource swingSource, ItemContext itemContext)` InteractionResult.java:33; vanilla usage ServerGamePacketListenerImpl.java:1381-1383). | Part of the mandatory `@IPVanillaCopy` re-diff of `doProcessUseItemOn` (see C24). |
| G8 | `ListTag.getElementType()` (MyNbtTextFormatter list printing) | **GONE** — no element-type accessor on ListTag (lists are heterogeneous since 1.21.5; only stream-level `TagTypes.getType(...)` internals remain, ListTag.java:62-64). | MyNbtTextFormatter must be re-transcribed from the 26.2 `SnbtPrinterTagVisitor` (exists: net/minecraft/nbt/SnbtPrinterTagVisitor.java:16, `visitString` :54, `visitCompound` :168) rather than patched — this was already flagged as a full re-diff (`@IPVanillaCopy`). `TagVisitor`'s 13 visit methods are unchanged (TagVisitor.java:4-28). |
| G9 | `AbstractClientPlayer.clientLevel` field behind `IEAbstractClientPlayer.ip_setClientLevel(ClientLevel)` (mixin/client/MixinAbstractClientPlayer.java:12) | **GONE** — 26.2 `AbstractClientPlayer` has no level field (full field list = `playerInfo`, `showExtraEars`, `clientAvatarState`; net/minecraft/client/player/AbstractClientPlayer.java:20-26). The only level storage is `Entity.level` (Entity.java:215) with `protected setLevel(Level)` (:3962) / `level()` (:3958). | The duck collapses into `IEEntity.ip_setWorld` (one field to swap instead of two). Delete `IEAbstractClientPlayer` in the port; verify no caller relied on setting the two fields to *different* values (IP never does). |
| G10 | `Minecraft.useShaderTransparency()` — exactly two IP usages (ClientDebugCommand never references it; repo-wide grep): (a) **this slice's MixinMinecraft.java:178-183 — a HEAD-cancellable `@Inject` INTO `Minecraft.useShaderTransparency()` that forces `false` while a portal view renders** (gated on `WorldRenderInfo.isRendering()`; comment :177 "avoid messing up rendering states in fabulous"); (b) a static read in render/renderer/RendererUsingStencil.java:91 (render slice). | **GONE from Minecraft** — the flag lives on the render state: `gameRenderer.gameRenderState().useShaderTransparency()` (GameRenderState.java:17-19, computed `!levelRenderState.cameraRenderState.isPanoramicMode && optionsRenderState.improvedTransparency` :18; usage LevelRenderer.java:835). | The port item is an OVERRIDE, not a read: re-anchor the force-false-while-portal-rendering suppression onto the flag's 26.2 home — inject into `GameRenderState.useShaderTransparency()` (net/minecraft/client/renderer/state/GameRenderState.java:17), reached via `GameRenderer.gameRenderState()` (MAP §GameRenderer :192). A read-only re-map would silently drop the fabulous-graphics suppression mixin from the port checklist. The RendererUsingStencil.java:91 read belongs to the render slice's re-map. |
| G11 | `RenderBuffers.bufferSource()/outlineBufferSource()/crumblingBufferSource()` (via `IEMinecraftClient.ip_setRenderBuffers` / `IEWorldRenderer.ip_get/setRenderBuffers` consumers) | **GONE** — RenderBuffers' entire public API is now ctor + `fixedBufferPack()` + `sectionBufferPool()` + `stagedVertexBuffer()` + `endFrame()` + `close()` (MAP §RenderBuffers, RenderBuffers.java:12-34). | The ducks that *hold* RenderBuffers survive (field moved, see C7); everything that *drew* through a BufferSource re-routes through the submit pipeline (G1). |
| G12 | `GameRenderer` booleans behind `ip_getDoRenderHand` / `ip_setIsRenderingPanorama` (ducks/IEGameRenderer) | **GONE as fields** — no `renderHand`/`isRenderingPanorama` booleans. Hand rendering is gated inline by render state: `renderItemInHand` checks `!cameraState.isPanoramicMode && optionsRenderState.cameraType.isFirstPerson() && !entityRenderState.isSleeping && !guiRenderState.isHudHidden && gameMode != SPECTATOR` (GameRenderer.java:336-341). Panorama is an object: `protected final Panorama panorama` (GameRenderer.java:117; class net/minecraft/client/renderer/Panorama.java:12 writes `guiRenderState.panoramaRenderState`, :33). | The "don't render hand in portal view" switch must instead influence the render-state flags for the secondary pass (e.g. `CameraRenderState.isPanoramicMode` or skipping `renderItemInHand`) — render-slice decision; the duck as-shaped dies. |

**GONE count: 12**

---

## 2. CHANGED (exists, but moved / renamed / re-signatured — exact 26.2 replacement cited)

| # | IP usage (where) | 26.2 replacement + citation | Migration note |
|---|---|---|---|
| C1 | `GameRenderer.pick(F)V` — injection anchor for `BlockManipulationClient.updatePointedBlock` (MixinGameRenderer_B.java:16/25) | **MOVED to `Minecraft.pick(float partialTicks)`** (private, Minecraft.java:2934); body sets `this.hitResult = this.player.raycastHitResult(partialTicks, cameraEntity)` (:2939) and `crosshairPickEntity` (:2940). | Re-anchor both the RETURN inject and the portal-rendering HEAD cancel onto `Minecraft.pick`. Camera pos for the raytrace: `gameRenderer.mainCamera()` (MAP :657) + `Camera.position()` (see C6). |
| C2 | `Minecraft.pickBlock` wrap point (MixinMinecraft_B.java:110-126) | **RENAMED `pickBlockOrEntity()`** (private, Minecraft.java:2353). `startAttack` (:1614), `continueAttack` (:1589), `startUseItem` (:1688), `handleKeybinds` (:1865) keep their names. | The raw `Minecraft.level`+`hitResult` field-swap trick still works — both fields survive (S5). |
| C3 | `Minecraft.setScreen` / `Minecraft.screen` (ClientDebugCommand config screen, ExampleGuiPortalRendering, IPortalInitialScreen) | **MOVED to `Gui`**: `Minecraft.gui` public final (Minecraft.java:290); `Gui.setScreen(@Nullable Screen)` (net/minecraft/client/gui/Gui.java:222), `Gui.screen()` (:218), private field (:74). `Minecraft.setScreenAndShow(Screen)` convenience exists (Minecraft.java:2184). | `ip_getCurrentScreen` duck → `minecraft.gui.screen()` (no mixin needed). |
| C4 | `Minecraft.addInitialScreens(List<Function<Runnable,Screen>>)` RETURN — IPortalInitialScreen wiring (MixinMinecraft.java:185-194) | **MOVED to `Gui.addInitialScreens(List<Function<Runnable, Screen>>)`** (private, Gui.java:381; called from `Gui` at :364). | Re-target the mixin to `Gui`; same append-if-not-shown pattern. |
| C5 | `Minecraft.fps` field write anchor for `ClientPerformanceMonitor.updateEverySecond` (MixinMinecraft.java:144-154) | Field is now **`private static int fps`** (Minecraft.java:350); the once-per-second write survives in `runTick`: `fps = this.frames;` (:1355). | Injection point intact; `@Shadow` must declare it static. |
| C6 | `Camera.getPosition()` (BlockManipulationClient.java:68) | **RENAMED `position()`** (net/minecraft/client/Camera.java:359; field :52; `setPosition` is protected :353). `eyeHeight`/`eyeHeightOld` fields for `IECamera.ip_get/setCameraY` are unchanged (:62-63). | Mechanical rename. |
| C7 | `Minecraft.mainRenderTarget` (IEMinecraftClient.ip_setFrameBuffer, MixinMinecraft.java:197-199) + `Minecraft`-owned `RenderBuffers` (ip_setRenderBuffers) | Both **MOVED to GameRenderer**: `private final RenderTarget mainRenderTarget` (GameRenderer.java:104, getter :673 per MAP), `private final RenderBuffers renderBuffers` (:103, getter :184). `Minecraft.getMainRenderTarget()` removed (MAP §Minecraft). | The FBO-swap duck becomes a `@Mutable @Accessor` on `GameRenderer.mainRenderTarget`; ditto renderBuffers. `Minecraft.levelRenderer` is still on Minecraft but now `public final` (Minecraft.java:281, + `levelExtractor` :280) — `ip_setWorldRenderer` needs `@Mutable`. |
| C8 | `TextureTarget(2, 2, true, true)` (ExampleGuiPortalRendering.java:112) | Ctor is **`TextureTarget(@Nullable String label, int width, int height, boolean useDepth, GpuFormat format)`** (MAP §TextureTarget, TextureTarget.java:11); standard color format `GpuFormat.RGBA8_UNORM`. | `new TextureTarget("gui portal", 2, 2, true, GpuFormat.RGBA8_UNORM)`. |
| C9 | `RenderTarget` stencil toggle (`IEFrameBuffer.ip_setIsStencilBufferEnabledAndReload`) | RenderTarget ctor is now `(label, useDepth, GpuFormat format)` (com/mojang/blaze3d/pipeline/RenderTarget.java:30) with `protected final GpuFormat format` (:24); default depth texture is `GpuFormat.D32_FLOAT` (:86). Stencil = create with a combined depth-stencil format: `D32_FLOAT_S8_UINT`/`D24_UNORM_S8_UINT` (MAP §GpuFormat, GpuFormat.java:59-63). | "Enable stencil and reload" becomes "recreate/resize the target with a stencil-capable format" — mechanism confirmed by the mod's existing STENCIL_DIRECT work (memory: stencil-direct spec). |
| C10 | `KeyMapping.matches(int,int)` (`options.keyInventory.matches` in GuiPortalScreen key handling) | **`matches(KeyEvent)`** (net/minecraft/client/KeyMapping.java:167; also `matchesMouse(MouseButtonEvent)` :173). `keyInventory` itself survives (Options.java:670). | Screens now receive `KeyEvent`-style input; adapt at the Screen override. |
| C11 | `EntityType.create(Level)` (PortalCommand.java:1244 etc., Mirror :2624) | **`create(Level, EntitySpawnReason)`** (net/minecraft/world/entity/EntityType.java:298; also `create(Level, EntitySpawnRequest)` :302; `EntitySpawnReason` exists at net/minecraft/world/entity/EntitySpawnReason.java). | Use `EntitySpawnReason.COMMAND`-equivalent (pick the constant the 26.2 `/summon` path uses — verify in SummonCommand during the port). |
| C12 | `Entity.saveWithoutId(CompoundTag)` / `Entity.load(CompoundTag)` (PortalCommand.java:2241/:2451-2453, `updateEntityFullNbt`) | **`saveWithoutId(ValueOutput)`** (Entity.java:2059; `save(ValueOutput)` :2055, `saveAsPassenger` :2040) and **`load(ValueInput)`** (:2133). CompoundTag bridges: `TagValueInput`/`TagValueOutput` (imports Entity.java:132-135). | Wrap the command-supplied `CompoundTag` in `TagValueInput/Output` (with a `ProblemReporter`, per the 26.2 call sites) — same pattern the entity-core slice will use for Portal NBT. |
| C13 | `IEDistanceManager.portal_getTicketSet(long)` → `SortedArraySet<Ticket<?>>` (per-chunk ticket dump, PortalDebugCommands.java:690-746) | Tickets moved off DistanceManager into **`TicketStorage`** (`private final TicketStorage ticketStorage`, DistanceManager.java:40; class net/minecraft/world/level/TicketStorage.java:33 — a `SavedData`): storage is `Long2ObjectOpenHashMap<List<Ticket>>` (:43), public **`getTickets(long)` → `List<Ticket>`** (:130), `addTicket` (:143/:147), `getTicketLevelAt(long, boolean simulation)` (:185). `Ticket` is **no longer generic** — `Ticket.getType()` → `TicketType` (Ticket.java:48). `DistanceManager.inEntityTickingRange(long)` survives (DistanceManager.java:135). | Duck re-targets `TicketStorage` (reachable from DistanceManager via accessor, or from ServerLevel's SavedData). Histogram code (`report_chunk_ticket_stat`) iterates `List<Ticket>` instead of `SortedArraySet`. |
| C14 | `ActiveProfiler.WARNING_TIME_NANOS` public static write (`/portal debug profile set_lag_logging_threshold`, PortalDebugCommands.java:156-167) | Field survives but is **`private static final long`**, initialized `Duration.ofMillis(100).toNanos()` (net/minecraft/util/profiling/ActiveProfiler.java:27); consumed at :120 behind a new `this.suppressWarnings` BooleanSupplier gate. | Requires `@Mutable @Accessor` (static) to keep the command; it is a debug nicety — flag to the user if mixin-on-static-final proves flaky. |
| C15 | `RegistryAccess.Frozen.registryOrThrow(Registries.BIOME)` (`check_biome_registry`, PortalDebugCommands.java:576-597) | **RENAMED `lookupOrThrow`** (net/minecraft/core/RegistryAccess.java:21). `Registry.getId` (Registry.java:64), `entrySet()` (:93), `getRandom(RandomSource)` → `Optional<Holder.Reference<T>>` (:97) all survive. | Mechanical rename. |
| C16 | `CompoundTag.getAllKeys()` (+ direct-value getters) — MyNbtTextFormatter, `sendPortalInfo`, `set_portal_nbt` | **`keySet()`** (net/minecraft/nbt/CompoundTag.java:193). Getters are Optional-based now: `getString(String)` → `Optional<String>` (:331), `getStringOr(String, String)` (:335); `get` (:271), `contains(String)` (:275, one-arg — the `(name, type)` overload is gone), `merge` (:435), `remove` → returns `@Nullable Tag` (:375). | Feeds the G8 re-transcription; `set_portal_nbt`'s `dimensionTo` refusal check uses one-arg `contains`. |
| C17 | `ClientLevel.mapData` — `IEClientWorld.ip_getAllMapData()/ip_addMapData(Map<String, MapItemSavedData>)` | Key type changed: **`Map<MapId, MapItemSavedData>`** (net/minecraft/client/multiplayer/ClientLevel.java:160; `MapId` exists at net/minecraft/world/level/saveddata/maps/MapId.java). Also relevant: `tickRateManager` is `private final` (:155 — `ip_setTickRateManager` needs `@Mutable`), `tickingEntities` EntityTickList (:149), `blockStatePredictionHandler` (:172, package-private getter :219). `entitiesForRendering()` unchanged (:449). | Duck signatures re-typed `MapId`; everything else is shadow-field work as before. |
| C18 | `Level.mayInteract(Player, BlockPos)` (BlockManipulationServer.doProcessUseItemOn :262) | Param widened: **`mayInteract(Entity, BlockPos)`** (net/minecraft/world/level/Level.java:816). | Source-compatible for the copy; note it in the re-diff. |
| C19 | `UseOnContext` ctor swap in `performUseItemOn` (MixinMultiPlayerGameMode.java:70-80) | Vanilla still constructs **`new UseOnContext(player, hand, blockHit)`** inside `performUseItemOn` (MultiPlayerGameMode.java:341, :370); the explicit-level ctor survives as **protected** `UseOnContext(Level, @Nullable Player, InteractionHand, ItemStack, BlockHitResult)` (net/minecraft/world/item/context/UseOnContext.java:24; public 3-arg :20). | The @Redirect to the 5-arg ctor still works (mixin can invoke protected ctor via accessor/AT); anchor method + ctor both exist. |
| C20 | `MultiPlayerGameMode.startDestroyBlock` synthetic lambda `method_41930` @Redirect (MixinMultiPlayerGameMode.java:42) + `LocalPlayer.level()` redirects | 26.2 `startDestroyBlock(BlockPos, Direction)` (net/minecraft/client/multiplayer/MultiPlayerGameMode.java:151) has TWO prediction lambdas (creative :167-170, survival :188-199). Remaining in-lambda level read: `this.minecraft.player.level()` (:194); `continueDestroyBlock` (:229) reads `this.minecraft.player.level()` at :256. **`startPrediction` gained a parameter**: `startPrediction(ClientLevel level, PredictiveAction)` (:297-303) — callers pass `this.minecraft.level` (:167, :188). | Lambda name must be re-derived from 26.2 bytecode (the Mojang-mapped dev name will be `lambda$startDestroyBlock$N`). The new explicit `ClientLevel` parameter is a cleaner interception point than the old lambda redirect — a `@ModifyArg` on the `startPrediction` call can swap the level. The `player.level()` redirects re-anchor to :194/:256. |
| C21 | `ClientPacketListener.send(Packet)` @ModifyArg anchors (MixinMultiPlayerGameMode.java:82-113) | Sends survive: inside `startPrediction` `this.connection.send(packetConcludingPrediction)` (MultiPlayerGameMode.java:301), direct sends in `startDestroyBlock` (:179) and `stopDestroyBlock` (:221). `send` is declared on the superclass: **`ClientCommonPacketListenerImpl.send(Packet<?>)`** (net/minecraft/client/multiplayer/ClientCommonPacketListenerImpl.java:349); `connection` field is `ClientPacketListener` (:78). | @ModifyArg target descriptor changes to the superclass method (`Packet<?>` param). Packet reconstruction inputs unchanged (S15/S16). |
| C22 | `ClientboundPlayerPositionPacket` duck (`IEPlayerPositionLookS2CPacket` — adds dimension tag; MixinPlayerPositionLookS2CPacket.java:14) | Now a **record**: `record ClientboundPlayerPositionPacket(int id, PositionMoveRotation change, Set<Relative> relatives)` (net/minecraft/network/protocol/game/ClientboundPlayerPositionPacket.java:12). | Mixins can still add fields+duck to a record; construction sites move to the canonical ctor. `ServerboundMovePlayerPacket` is NOT a record — still abstract class + `Pos/PosRot/Rot/StatusOnly` (ServerboundMovePlayerPacket.java:9-206) — duck unchanged there. |
| C23 | `ServerPlayer` ride-without-teleport duck (`ip_stopRidingWithoutTeleportRequest`/`ip_startRidingWithoutTeleportRequest`) | Signatures changed: **`startRiding(Entity, boolean force, boolean sendEventAndTriggers)`** (ServerPlayer.java:2122 override; base Entity.java:2414); `stopRiding()` (Entity.java:2476), `removeVehicle()` (Entity.java:2464; ServerPlayer override :2138). | The duck's "without telling the client" variants must be re-derived from the 26.2 ServerPlayer overrides (which packets they now emit differs — 3rd param `sendEventAndTriggers` may subsume part of it). Flag for the entity-sync slice re-verify. |
| C24 | `@IPVanillaCopy` sources `ServerGamePacketListenerImpl.handlePlayerAction`/`handleUseItemOn` (BlockManipulationServer.doProcessPlayerAction :204-225 / doProcessUseItemOn :233-286) | Both exist — `handlePlayerAction` (net/minecraft/server/network/ServerGamePacketListenerImpl.java:1273), `handleUseItemOn` (:1343) — but bodies changed materially: new `Action.STAB` case (:1281); reach check is `player.isWithinBlockInteractionRange(pos, 1.0)` (:1354, decl Player.java:1993); height uses `getMaxY()/getMinY()` inclusive (:1360-1365); spawn-protection via `server.isUnderSpawnProtection` (:1367); swing via `swingSource()` (:1381-1383); block-break forward is `player.gameMode.handleBlockBreakAction(pos, action, direction, this.player.level().getMaxY(), packet.getSequence())` (:1323; decl ServerPlayerGameMode.java:150 — `int maxY` now INCLUSIVE); `ackBlockChangesUpTo(int)` survives (:1479) plus a flush field (:242, :294-296); dual `ClientboundBlockUpdatePacket(level, pos)` sends survive (:1399-1400). | Line-by-line re-transcription required (already a port-checklist item); the reach-validation replacement point is now `isWithinBlockInteractionRange`. |
| C25 | `Screen` render model (IPortalInitialScreen, GuiPortalScreen) | `Screen` survives; per-frame hook is **`extractRenderState(GuiGraphicsExtractor, int, int, float)`** (Screen.java:116; full pipeline entry `extractRenderStateWithTooltipAndSubtitles` :107, background :377-392). Layout/widget API unchanged (S22). | Pairs with G5. |
| C26 | `IEEntityTrackingSection.ip_traverse` / `IESectionedEntityCache.ip_traverseSectionInBox` targets | Classes + traversal survive with same shapes: `EntitySection.getEntities(AABB, AbortableIterationConsumer)` (net/minecraft/world/level/entity/EntitySection.java:30), typed variant with `EntityTypeTest` (:40); `EntitySectionStorage.forEachAccessibleNonEmptySection(AABB, AbortableIterationConsumer<EntitySection<T>>)` (EntitySectionStorage.java:37), `getSection(long)` (:97). `AbortableIterationConsumer` at net/minecraft/util/AbortableIterationConsumer.java, `EntityTypeTest` at world/level/entity/EntityTypeTest.java. | The ducks' *purpose* (bypass AbortableIterationConsumer) still applies — mixin bodies re-shadow the same private storage; minor member re-verify at port time. Classified CHANGED only for the private internals re-check. |
| C27 | `ChunkMap.TrackedEntity` duck target (`IETrackedEntity`, MixinTrackedEntity.java:40) | Inner class survives with same fields — `serverEntity`, `entity`, `range`, `lastSectionPos`, `seenBy` (ChunkMap.java:1320-1325) — but now **`implements ServerEntity.Synchronizer`** and broadcast is `sendToTrackingPlayers(Packet<? super ClientGamePacketListener>)` (:1345). `ServerEntity.sendChanges()` (ServerEntity.java:88) and `positionCodec` (:61, `setBase(entity.trackingPosition())` :79/:134) back `ip_sendChanges`/`ip_updateTrackedEntityPosition`. | Duck method bodies re-map ~1:1; the Synchronizer interface is new surface the entity-sync slice may exploit. |
| C28 | `Util.backgroundExecutor()` (sculpt, PortalCommand.java:2719) | Package moved: **`net.minecraft.util.Util.backgroundExecutor()`** → `TracingExecutor` (net/minecraft/util/Util.java:252). | Import fix only. |

**CHANGED count: 28**

---

## 3. SAME (verified compatible in 26.2)

| # | Touchpoint (where) | 26.2 citation | Note |
|---|---|---|---|
| S1 | `PoseStack` + `pushPose/popPose/translate/scale/mulPose/last()` (WireRenderingHelper throughout) | com/mojang/blaze3d/vertex/PoseStack.java:27-85; `Pose` mutators :122-165 | Intact. |
| S2 | `VertexConsumer.addVertex(Matrix4f,f,f,f)/.setColor(int)/.setNormal(f,f,f)` (WireRenderingHelper.java:242-263 etc.) | com/mojang/blaze3d/vertex/VertexConsumer.java — `addVertex(Matrix4fc,f,f,f)` :98, `setColor(int)` :20, `setNormal` :28 | Matrix param widened to `Matrix4fc` — source-compatible. The *acquisition* of the consumer changes (G4). |
| S3 | `Frustum` duck target (`IEFrustum`) | net/minecraft/client/renderer/culling/Frustum.java:13 — `viewVector` :17, ctor `(Matrix4fc, Matrix4f)` :22, `prepare` :73, `isVisible(AABB)` :85, `getFrustumPoints` :108 | Fields the duck shadows exist; mixin body re-verify at render-slice port. |
| S4 | `ParticleEngine` world swap (`IEParticleManager.ip_setWorld`) | net/minecraft/client/particle/ParticleEngine.java — `protected ClientLevel level` :31, **public `setLevel(@Nullable ClientLevel)`** :131 | Duck likely unnecessary: vanilla setter is public now. |
| S5 | `Minecraft.level` / `hitResult` / `smartCull` fields | Minecraft.java:335 (`public @Nullable ClientLevel level`), :341 (`public @Nullable HitResult hitResult`), :354 (`public boolean smartCull = true`) | All public; field-swap tricks survive. |
| S6 | `Minecraft.startAttack/continueAttack/startUseItem/handleKeybinds/runTick/updateLevelInEngines/getRunningThread/getWindow` | Minecraft.java:1614/:1589/:1688/:1865/:1152/:2191+2195 (now two overloads, `@Nullable ClientLevel` + `stopSound` variant)/:2663/:2815 | `pickBlock` is the only rename in the wrap set (C2). |
| S7 | `Window.getWidth()/getHeight()` (ExampleGuiPortalRendering.java:186-187) | com/mojang/blaze3d/platform/Window.java:463/:467 | — |
| S8 | `Options.getEffectiveRenderDistance()` / `keyInventory` | net/minecraft/client/Options.java:1971 / :670 | `matches` signature changed (C10). |
| S9 | Light debug: `LevelLightEngine.checkBlock/updateSectionStatus/getLayerListener(LightLayer.BLOCK)`; `ThreadedLevelLightEngine.lightChunk(ChunkAccess, boolean)`; `DataLayer.getData()/isEmpty()` | LevelLightEngine.java:32/:62/:94; ThreadedLevelLightEngine.java:160; DataLayer.java:86/:141 | All intact. |
| S10 | `PostChain` (unused import in IEWorldRenderer) | net/minecraft/client/renderer/PostChain.java exists; vanilla use GameRenderer.java:234 | Drop the import if unused. |
| S11 | `ClientboundCustomPayloadPacket` duck target | net/minecraft/network/protocol/common/ClientboundCustomPayloadPacket.java:15 (`record (CustomPacketPayload payload)`) | Was already a record at 1.21.3; empty-marker duck unaffected. |
| S12 | `ServerboundMovePlayerPacket` duck target | ServerboundMovePlayerPacket.java:9 (abstract) + subclasses :100-206 | — |
| S13 | `ServerGamePacketListenerImpl.teleport(x,y,z,yaw,pitch)` (`/portal debug align`) + `awaitingPositionFromClient` (`ip_hasAwaitingTeleport`) | ServerGamePacketListenerImpl.java:1257 (+ `PositionMoveRotation` overload :1261); field :259 | — |
| S14 | `ackBlockChangesUpTo(int)` | ServerGamePacketListenerImpl.java:1479 | Plus new deferred-flush field :242/:294-296 — include in re-diff. |
| S15 | `ServerboundPlayerActionPacket`: `STREAM_CODEC`, ctors `(Action,BlockPos,Direction[,int])`, `getPos/getDirection/getAction/getSequence`, `Action.{START,ABORT,STOP}_DESTROY_BLOCK` | ServerboundPlayerActionPacket.java:11/:19/:26/:53-65/:70-72 | Enum gained members (e.g. `STAB`, used SGPLI:1281) — `isAttackingAction` whitelist still valid. |
| S16 | `ServerboundUseItemOnPacket`: `STREAM_CODEC/getHand/getHitResult/getSequence` | ServerboundUseItemOnPacket.java:11/:45/:49/:53 | — |
| S17 | `ClientboundBlockUpdatePacket(world, pos)` | usage ServerGamePacketListenerImpl.java:1399-1400 | — |
| S18 | `FriendlyByteBuf` + netty `Unpooled` | net/minecraft/network/FriendlyByteBuf.java exists | — |
| S19 | `Packet<ClientGamePacketListener>` (PortalAPI.sendPacketToEntityTrackers) | net/minecraft/network/protocol/game/ClientGamePacketListener.java exists; usage ChunkMap.java:1345 | — |
| S20 | `BlockStatePredictionHandler` (IEClientWorld) + `startPredicting()` | net/minecraft/client/multiplayer/prediction/BlockStatePredictionHandler.java; usage MultiPlayerGameMode.java:298-299 (`prediction.currentSequence()`) | Getter on ClientLevel is package-private (:219). |
| S21 | GUI widgets: `Button.builder`, `StringWidget`, `MultiLineTextWidget`, `ImageWidget.texture`, `HeaderAndFooterLayout`, `LinearLayout`, `Screen.addRenderableWidget/init` | Button.java:24; StringWidget.java:17/:21; MultiLineTextWidget.java:14; ImageWidget.java:21 (**`Identifier` param**); HeaderAndFooterLayout.java:23/:27; LinearLayout.java:94/:98; Screen.java:202/:365 | Widget layer intact; only the render entry changed (C25/G5). |
| S22 | `ChunkMap`: `.size()`, `entityMap` `Int2ObjectMap<TrackedEntity>`, `getUpdatingChunkIfPresent(long)`, `lightEngine`/`getLightEngine()`, `level` field, `getPlayerViewDistance(ServerPlayer)` | ChunkMap.java:841/:146/:251/:132+247/:131/:817 | All duck-shadowed members present (getPlayerViewDistance still private). |
| S23 | `ChunkHolder.getTicketLevel()` / `.getPos()` | ChunkHolder.java:241; `getPos` on superclass GenerationChunkHolder.java:278 | `.getPos().toLong()` → `.getPos().pack()` (global rename). |
| S24 | `ServerChunkCache.chunkMap` (+ DistanceManager access) | ServerChunkCache.java:64 (`public final`), `chunkMap.getDistanceManager()` usage :120 | `IEServerChunkCache` duck may collapse to direct access. |
| S25 | `LevelChunk.getFullStatus()`, `EmptyLevelChunk`, `ChunkAccess` | LevelChunk.java:665; net/minecraft/world/level/chunk/EmptyLevelChunk.java exists | — |
| S26 | `ServerLevel.getAllEntities()` / `entityManager` (IEServerWorld) | ServerLevel.java:1636 / :202 (`private final PersistentEntitySectionManager<Entity> entityManager`) | — |
| S27 | `MinecraftServer.getLevel/getAllLevels/saveAllChunks(bool,bool,bool)/getPlayerList/registryAccess/execute` | MinecraftServer.java:1189/:1197/:588/usages :478+624/:541 (usage); `execute` via BlockableEventLoop superclass | — |
| S28 | `ClientChunkCache.getLoadedChunksCount()` | ClientChunkCache.java:171 | — |
| S29 | `Entity.setPosRaw(DDD)V` @ `EntityInLevelCallback.onMove()` + `Entity.setRemoved` RETURN (MixinEntity_U anchors) | Entity.java:3786 (`public final void setPosRaw`) with `this.levelCallback.onMove()` :3800; `setRemoved(RemovalReason)` :3912 (`public final`); `EntityInLevelCallback.onMove/onRemove` interface :16/:18 | **Both injection anchors survive verbatim.** Bonus: vanilla now has `protected unsetRemoved()` (Entity.java:3926) — `ip_unsetRemoved` can delegate. `removalReason` field :235, `getRemovalReason` :3907, `level` :215/`setLevel` :3962. |
| S30 | `ServerEntity` duck target + `PersistentEntitySectionManager.saveAll()/gatherStats()` | ServerEntity.java (positionCodec :61, sendChanges :88); PersistentEntitySectionManager.java:268/:349 | — |
| S31 | `EntityTickList`, `LevelEntityGetter`, `EntityAccess`, `EntityTypeTest`, `SectionPos`, `Entity.RemovalReason` | files exist (world/level/entity/*, EntityTickList usage ClientLevel.java:149); `Level.getEntities()` protected abstract Level.java:1000 | `IEWorld.portal_getEntityLookup` unchanged. |
| S32 | Entity misc: `getViewVector(float)/getLookAngle/getYRot/position()/getUUID/getId/tickCount/getRemovalReason/setCustomName/getDisplayName/getEyePosition(float)` | Entity.java:1929/:2562/:3862/:3685/:3246/:381/:247/:3907/:3277/:3272/:1975 | `getViewVector`/`getEyePosition` now `final` — irrelevant to callers. |
| S33 | `entitiesForRendering()` (ClientDebugCommand) | ClientLevel.java:449 | — |
| S34 | `Level` duck internals: weather gradients, `WritableLevelData`, thread | Level.java:118-121 (`oRainLevel/rainLevel/oThunderLevel/thunderLevel`), :126 (`levelData`), :113 (`thread`) | `portal_setWeather`/`portal_getThread` shadow targets intact. |
| S35 | `BlockGetter.traverseBlocks(Vec3,Vec3,C,BiFunction,Function)` + `clipWithInteractionOverride` | BlockGetter.java:112 / :84 | The destination re-trace core survives. |
| S36 | `ClipContext`: both ctors, `getFrom/getTo/getBlockShape/getFluidShape`, `Block.OUTLINE/COLLIDER` | ClipContext.java:28/:32/:44/:40/:48/:52/:57-58 | `IERayTraceContext` setStart/setEnd = @Mutable on private finals — unchanged. |
| S37 | `VoxelShape.clip/toAabbs/isEmpty`; `getBlockCollisions(Entity, AABB)` | VoxelShape.java:163/:135/:73; CollisionGetter.java:96 | Sculpt pipeline intact. |
| S38 | `BlockHitResult` ctor(+inside)/`miss`/getters; `HitResult.Type.MISS` | BlockHitResult.java:17 (+5-arg worldBorderHit :21)/:13/:46-50; HitResult.java:26 | — |
| S39 | `Level.setBlockAndUpdate`; `BlockState.isAir/isSolid`; `Blocks.BARRIER` | Level.java:307; BlockBehaviour.java:568/:532; Blocks.java:2979 | — |
| S40 | `Player.blockInteractionRange()` | Player.java:1971 | Plus new `isWithinBlockInteractionRange` :1993 (C24). |
| S41 | `ServerPlayerGameMode.handleBlockBreakAction(pos, action, dir, maxY, seq)` / `useItemOn(player, world, stack, hand, hit)` | ServerPlayerGameMode.java:150; useItemOn usage SGPLI:1370 | `maxY` arg is now the INCLUSIVE `getMaxY()` (SGPLI:1323) — adjust the copy. |
| S42 | `player.resetLastActionTime/swing(hand,true)/getItemInHand`; `ItemStack.isItemEnabled(FeatureFlagSet)`; `world.enabledFeatures()` | usage SGPLI:1359; LivingEntity.java:2014/:2240; ItemStack.java:323; LevelReader.java:217 | — |
| S43 | `TickRateManager`, `MapItemSavedData` | files exist; ClientLevel.java:155/:160 | Key-type change is C17. |
| S44 | Brigadier + vanilla argument types: `DimensionArgument.dimension/getDimension`, `Vec3Argument.vec3(boolean)`, `EntityArgument.entity/entities/player`, `BlockPosArgument.getSpawnablePos`, `ColumnPosArgument.columnPos/getColumnPos`, `RotationArgument.rotation/getRotation`, `ComponentArgument.textComponent(CommandBuildContext)`, `CompoundTagArgument`, `Commands.literal/argument`, `CommandBuildContext` | DimensionArgument.java:47/:51; Vec3Argument.java:30/:34; EntityArgument.java:48/:56/:77; BlockPosArgument.java:52; ColumnPosArgument.java:25/:29; RotationArgument.java:17/:21; ComponentArgument.java:49; CompoundTagArgument.java exists; Commands.java:439/:443 | Whole `/portal` tree re-registers unchanged except permissions (G6). |
| S45 | `CommandSourceStack.getPlayerOrException/getLevel/getPosition/sendSuccess(Supplier,boolean)/sendFailure` | CommandSourceStack.java:401/:385/:381/:464/:495 | Only `hasPermission(int)` died (G6). |
| S46 | `SharedSuggestionProvider.suggest(Iterable/Stream/String[])` + `SingletonArgumentInfo.contextFree` | SharedSuggestionProvider.java:216/:228/:234; SingletonArgumentInfo.java:17 | Argument-type *registration* is loader-specific (F3). |
| S47 | NBT/text: `TagVisitor` (13 methods), `StringTag.quoteAndEscape`, `CompoundTag.get/contains/merge/remove`, `Component.literal/translatable`, `MutableComponent`, `ChatFormatting` | TagVisitor.java:4-28; StringTag.java:101; CompoundTag.java:271/:275/:435/:375; Component.java:135/:139-143; net/minecraft/ChatFormatting.java exists | `getAllKeys` and Optional getters are C16; `getElementType` is G8. |
| S48 | `SnbtPrinterTagVisitor` (re-diff source for MyNbtTextFormatter) | net/minecraft/nbt/SnbtPrinterTagVisitor.java:16 | Exists — the mandated re-transcription source. |
| S49 | `EntityInLevelCallback` (interface itself) | net/minecraft/world/level/entity/EntityInLevelCallback.java:16-18 | — |
| S50 | `DistanceManager.inEntityTickingRange(long)` | DistanceManager.java:135 | Ticket set access is C13. |

**SAME count: 50**

---

## 4. FABRIC-API touchpoints (route through the mod's loader abstraction)

The mod is multiloader (common/fabric/neoforge) with a `PlatformHelper` ServiceLoader seam (`migration/inventory/current-mod-core.md:111-112`); Fabric loader events live in `SeamlessPortalsModFabric`/`SeamlessPortalsClientFabric` (current-mod-core.md:256-257, :374-375). None of these are vanilla-API questions — each needs a seam decision:

| # | IP usage | Loader routing note |
|---|---|---|
| F1 | `CommandRegistrationCallback.EVENT` → `PortalCommand.register(dispatcher, ctx)` (IPModMain.java:103-105) | Fabric: same callback. NeoForge: `RegisterCommandsEvent`. Add a `registerCommands` hook to the platform seam. |
| F2 | `ClientCommandRegistrationCallback.EVENT` + `ClientCommandManager`/`FabricClientCommandSource` (ClientDebugCommand.java:13-14, :83) | Client-side command trees are loader-specific end to end (the `FabricClientCommandSource` type leaks into ClientDebugCommand's generics). NeoForge: `RegisterClientCommandsEvent` + `CommandSourceStack`. Either abstract the source type or accept per-loader command shims. |
| F3 | `ArgumentTypeRegistry.registerArgumentType` for `imm_ptl:axis`, `imm_ptl:sub_command_argument_type`, `imm_ptl:timing_function_argument_type` (all three arg types, ids are **synced** — client must know them) | Fabric: same API. NeoForge: `DeferredRegister<ArgumentTypeInfo<?,?>>` against `BuiltInRegistries.COMMAND_ARGUMENT_TYPE` + `ArgumentTypeInfos.registerByClass`. Needs a seam method; ids now built with `Identifier`. |
| F4 | `ServerTickEvents.END_SERVER_TICK` (ServerTaskList.java:11; GcMonitor.java:71) | The mod already routes END_SERVER_TICK per loader (current-mod-core.md:374). Attach `ServerTaskList.processTasks` + GcMonitor to the existing hook. |
| F5 | `EventFactory.createArrayBacked` / `Event<Predicate<Player>>` — `BlockManipulationServer.canDoCrossPortalInteractionEvent` (BlockManipulationServer.java:55-68), checked on BOTH sides | This is public API for other mods. A Fabric `Event` type cannot live in common code — replace with a loader-neutral listener list in common (the mod owns the contract), or expose per-loader event wrappers. Behavioral contract: results are AND-composed. |
| F6 | `@Environment(EnvType.CLIENT)` annotations throughout (WireRenderingHelper, ClientDebugCommand, IPortalInitialScreen, monitors) | 26.2 decompile uses `@OnlyIn(Dist.CLIENT)` (e.g. AbstractClientPlayer.java:21). Multiloader: keep sided classes out of common classloading paths or use the project's existing sidedness convention; do not copy IP's Fabric annotations into common. |
| F7 | `McRemoteProcedureCall` string-addressed RPC endpoints (wire protocol: `BlockManipulationServer.RemoteCallables.*`, `ClientDebugCommand.RemoteCallables.*`, `PortalCommand.RemoteCallables.clientAccelerate`, `ExampleGuiPortalRendering.RemoteCallables.*`, `ClientPerformanceMonitor` → `ImmPtlChunkTracking.RemoteCallables.acceptClientPerformanceInfo`) | Not Fabric API per se (IP's own `q_misc_util` networking), but transport registration is loader-specific — must ride the mod's `PlatformHelper.registerPayloads()` scheme (current-mod-core.md:258, :351). **Class/method FQNs are wire protocol — renames break compat** (inventory §5). |

**FABRIC-API count: 7** (not included in same/changed/gone totals)

---

## 5. Slice-specific port directives distilled

1. **Injection anchors verified intact:** `Entity.setPosRaw`@`onMove` + `setRemoved` RETURN (S29); `Minecraft.fps` write (C5); `handleKeybinds` wrap set (S6, C2). **Anchors that moved:** `GameRenderer.pick` → `Minecraft.pick` (C1); `addInitialScreens` → `Gui` (C4); screen ownership → `Gui` (C3); the fabulous-suppression HEAD-cancel on `Minecraft.useShaderTransparency()` (MixinMinecraft.java:178-183) → `GameRenderState.useShaderTransparency()` (G10 — it is an override to port, not a read).
2. **The two `@IPVanillaCopy` server replays (C24) and MyNbtTextFormatter (G8) are re-transcriptions, not patches** — new Action enum member, inclusive `getMaxY`, `swingSource()`, Optional NBT getters.
3. **`method_41930` problem got easier**: `startPrediction(ClientLevel, ...)` now takes the level as an argument (C20) — prefer a `@ModifyArg` on the `startPrediction` call sites over lambda-name pinning where possible.
4. **Dead ducks stay dead** (`IEPlayerEntity`, `IEPlayerListEntry`, `IESimpleRegistry`, `IEWorldChunk`) and `IEAbstractClientPlayer` joins them (G9). `IEParticleManager` (S4) and possibly `IEServerChunkCache` (S24) can collapse to public vanilla API.
5. **Everything drawing lines/text lives or dies with the render slice**: WireRenderingHelper's vertex math ports verbatim; its consumer acquisition (G4) and `IEWorldRenderer.ip_myRenderEntity` (G1) wait on the render-core mapping of the submit pipeline.
