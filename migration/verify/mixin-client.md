# Adversarial Verification — Client Mixins Triage slice (pass 2)

**Docs verified:**
- `migration/inventory/mixin-client.md` (inventory doc)
- `migration/api-map/mixin-client.md` (API-map doc)

**Ground truth used:** IP 1.21.3 source at `C:/Users/warwa/ModDev/ImmersivePortalsMod/src/main/java/qouteall/imm_ptl/core/mixin/client/` (abbrev `IP:`), decompiled 26.2 at `C:/Users/warwa/ModDev/mc262-ref` (abbrev `REF:`), mod repo (abbrev `MOD:`).

**Result: 57 claims checked · 55 CONFIRMED · 2 REFUTED (both minor) · severity MINOR.**

**Note on pass 1:** the previous version of this file refuted the api-map's "`cubeInFrustum` same descriptor" claim. The current api-map already carries the correction (`(DDDDDD)Z` → `(DDDDDD)I`, CIR generic Boolean→Integer, cancel with an outside code) — I re-verified that corrected text against `REF:net/minecraft/client/renderer/culling/Frustum.java:85-102` and it is accurate. That refutation is RESOLVED.

---

## 0. Scope completeness

| Claim | Verdict | Evidence |
|---|---|---|
| Inventory covers 62 Java files with the stated per-package breakdown (7 top-level, 3 accessor, 1 collisions, 1 debug, 4 interaction, 2 multiworld, 2 particle, 20 render top-level, 2 framebuffer, 1 isometric, 4 optimization, 3 shader, 1 sound, 11 sync) | **CONFIRMED** | `find` listing of `IP:` = exactly 62 files; every file appears in an inventory table row and an api-map verdict row. No silently skipped files. |
| API map verdict counts "PORTS-CLEAN 22 · NEEDS-RETARGET 26 · TARGET-GONE 14 (all 62 accounted)" | **CONFIRMED** | 22+26+14 = 62; every inventory mixin has a verdict row. |
| mixins.json: all registered under `"client"` array, package root `qouteall.imm_ptl.core.mixin` (:3), plugin `IPMixinPlugin` (:5), `defaultRequire: 1` (:146); `MixinShaderInstanceForIris` NOT listed | **CONFIRMED** | grep of `imm_ptl.mixins.json`: 61 `client.` entries (= 62 files − the unlisted dead Iris file, which greps to zero hits); plugin/package/defaultRequire lines verified. |
| `IPMixinPlugin.shouldApplyMixin` returns false for MixinRenderTarget/MixinMainTarget when porting_lib loaded | **CONFIRMED** | `IP:../IPMixinPlugin.java:23-30` — exact `isModLoaded("porting_lib")` + name-contains check. |

## 1. Geometry / sign / transform claims (re-derived from source)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 1.1 | Inventory: `MixinFrustum` snapshots the ORIGINAL cam pos at `prepare` TAIL (because `offsetToFullyIncludeCameraCube` mutates camX/Y/Z and a tilted portal frustum must not be moved back); `cubeInFrustum` HEAD culls using box coords minus the ORIGINAL pos, `cir.setReturnValue(false)` when FrustumCuller proves invisibility; `calculateFrustum` RETURN normalizes viewVector | **CONFIRMED** | `IP:render/optimization/MixinFrustum.java:29-35` (javadoc), `:57-74` (TAIL snapshot + `portal_frustumCuller.update(camX,camY,camZ)`), `:76-95` (`minX - portal_camX` … → `setReturnValue(false)`), `:97-106`. Sign convention re-derived: box-corner minus camera = camera-relative coords, consistent throughout. |
| 1.2 | Inventory ⑪ / api-map §10 ⑪: `@WrapOperation Matrix4f.rotation(Quaternionfc)` in `renderLevel` → `TransformationManager.processTransformation(mainCamera, r)`; in 26.2 that call moved into `Camera.getViewRotationMatrix` (`cachedViewRotMatrix.rotation(conjugate)`), dirty-flag cached, copied to `cameraState.viewRotationMatrix` at extract | **CONFIRMED** | `IP:render/MixinGameRenderer.java:304-317`; `REF:Camera.java:384-392` (rotation call at `:387` under `(matrixPropertiesDirty & 1)` guard), extract copy at `:135`. The invalidate-dirty-flag caveat is well-founded. |
| 1.3 | Inventory ⑩ / api-map §10 ⑩: `getProjectionMatrix(F)` @Redirect **ordinal 0** in `renderLevel` substitutes/captures a COPY of `RenderStates.basicProjectionMatrix` (bob-free base projection); in 26.2 the un-bobbed projection is `cameraState.projectionMatrix` before the bob mul | **CONFIRMED** | `IP:render/MixinGameRenderer.java:275-302` (comment "does not contain view bobbing", `new Matrix4f(...)` both directions); `REF:GameRenderer.java:535` (`new Matrix4f(cameraState.projectionMatrix)`), `:542` (`projectionMatrix.mul(bobStack.last().pose())` — applied to the local copy only), `:557` (`setProjectionMatrix`). Geometrically correct: cameraState's matrix stays un-bobbed. |
| 1.4 | Inventory ⑨: 3× `@ModifyArg PoseStack.translate(FFF)` in `bobView`, scale by `getViewBobbingOffsetMultiplier()` unless rendering hand; api-map: 26.2 `bobView(CameraRenderState,PoseStack)` :322 has a single translate :326-330 | **CONFIRMED** | `IP:render/MixinGameRenderer.java:213-253` (indices 0/1/2, `portal_isRenderingHand` gate); `REF:GameRenderer.java:322-334`. |
| 1.5 | Inventory §3.3: portal-view terrain discovery uses a **copied** frustum offset by `offsetToFullyIncludeCameraCube(8)`, cancels vanilla `setupRender`, gated on no-Sodium + not-Iris-shadow | **CONFIRMED** | `IP:render/MixinLevelRenderer.java:234-263` (`new Frustum(frustum).offsetToFullyIncludeCameraCube(8)`, `ci.cancel()`), gate `:265-268`. |
| 1.6 | api-map `MixinFrustum` row: 26.2 `cubeInFrustum(DDDDDD)` private returns **int** (raw `intersectAab`), `isVisible(AABB)` = `== -2 \|\| == -1`, NEW public `cubeInFrustum(BoundingBox)`, `calculateFrustum(Matrix4fc, Matrix4f)`, `offsetToFullyIncludeCameraCube` still mutates camX/Y/Z with the decrement restructured into the for-header | **CONFIRMED** | `REF:Frustum.java:46-71` (loop: `camZ` decrement in for-update, camX/camY in body), `:73-77` (prepare), `:79-83` (Matrix4fc + transformTranspose viewVector), `:85-88` (isVisible), `:90-92` (BoundingBox overload), `:94-102` (private int). The Boolean→Integer CIR retarget instruction is correct; "outside code = plane index (non −2/−1)" matches JOML intersectAab semantics. |

## 2. IP-side behavior/flow claims (inventory)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 2.1 | `MixinClientLevel`: ctor @RETURN → `O_O.createMyClientChunkManager` into `chunkSource`; `addEntity` @TAIL removes same-id from other worlds; `hasChunk` @HEAD gate (FULL & non-EmptyLevelChunk) under `tickOnlyIfChunkLoaded`; `toString` @HEAD; `tickNonPassenger` @HEAD `ip_tickCollidingPortal` "right before setting last tick pos" | **CONFIRMED** | `IP:MixinClientLevel.java:97-110, 113-125, 132-144, 147-151, 153-160` — 1.21.3 ctor handler signature `(ClientPacketListener, ClientLevelData, ResourceKey, Holder, int, int, Supplier, LevelRenderer, boolean, long)` also matches inventory §4 item 24. |
| 2.2 | `MixinMinecraft` ③ tick driver (after `ClientLevel.tick(BooleanSupplier)` shift AFTER): `ClientWorldLoader.tick()` → `RenderStates.setPartialTick(0)` → StableClientTimer tick+update → `ClientPortalAnimationManagement.tick()` ("must be after remote world ticking") → `manageTeleportation(true)` → `POST_CLIENT_TICK_EVENT` — the §3.1 twice-per-frame teleport invariant's tick half | **CONFIRMED** | `IP:MixinMinecraft.java:119-142` — exact order and comment. ② (`tickEntities` INVOKE handler) is indeed empty/commented `:104-116`. |
| 2.3 | Lambda anchors: `method_62214` (addMainPass body — 5 handlers), `method_62216` (weather ×2), `method_62218` (clear/pollLightUpdates region) | **CONFIRMED** | `IP:render/MixinLevelRenderer.java:124,139,160,186,214` (62214), `:309` (62218), `:365,381` (62216) — with the inline comments naming addMainPass/renderLevel/addWeatherPass. ⚠L flag justified. |
| 2.4 | `MixinMultiBufferSourceBufferSource`: `endBatch(RenderType)` HEAD sets `shouldForceDisableCull` + `_disableCull()` on odd mirror count; RETURN clears + `_enableCull()` | **CONFIRMED** | `IP:render/MixinMultiBufferSourceBufferSource.java:15-35`. |
| 2.5 | `MixinMultiPlayerGameMode` ③ `ip_redirectPacket`: attack actions only via `BlockManipulationServer.isAttackingAction`, STREAM_CODEC re-encode, `McRemoteProcedureCall` to `processPlayerActionPacket`/`processUseItemOnPacket` with dimension; use-item packets always redirected, `ServerboundUseItemPacket` not | **CONFIRMED** | `IP:interaction/MixinMultiPlayerGameMode.java:115-144` incl. the "not redirected" comment. |
| 2.6 | `MixinClientPacketListener` ② handleMovePlayer after `ensureRunningOnSameThread` INVOKE → `forceTeleportPlayer(packetDim, packet pos)` when stamped dim ≠ player dim, gated `doesServerHaveImmPtl()` | **CONFIRMED** | `IP:sync/MixinClientPacketListener.java:88-129`; 1.21.3 @At third param is `BlockableEventLoop` — exactly the landmark the api-map flags as changed. |
| 2.7 | `MixinMinecraft_RedirectedPacket`: `wrapRunnable` @HEAD-cancel wraps in `withSwitchedWorldFailSoft` when `PacketRedirectionClient.clientTaskRedirection` set; + `scheduleExecutables` override ("won't be delayed") | **CONFIRMED** | `IP:sync/MixinMinecraft_RedirectedPacket.java:23-42+`. |
| 2.8 | `MixinCamera` ① `setup(BlockGetter,Entity,ZZF)` @RETURN → `WorldRenderInfo.adjustCameraPos`; ② `getFluidInCamera` @HEAD → `FogType.NONE` while portal rendering | **CONFIRMED** | `IP:render/MixinCamera.java:41-51, 53-63`. |
| 2.9 | `MixinRenderSystem_Fog`: `@ModifyVariable setShaderFogStart/End` argsOnly @HEAD → `MyRenderHelper.transformFogDistance` | **CONFIRMED** | `IP:render/MixinRenderSystem_Fog.java:11-23`. |
| 2.10 | `MixinLevelRenderer` ⑲ `isSectionCompiled` @HEAD → `ImmPtlViewArea.rawGet` + `compiled.get() != CompiledSection.UNCOMPILED`, because `repositionCamera` never ran for the portal camera; ⑩ `@Redirect NEW ViewArea` in `allChanged` (`(SectionRenderDispatcher, Level, int, LevelRenderer)` descriptor) → `ImmPtlViewArea` under `useHackedChunkRenderDispatcher`; ⑰ ModifyVariable `isSpectator`; ⑱ pollLightUpdates redirect | **CONFIRMED** | `IP:render/MixinLevelRenderer.java:491-518` (incl. rationale javadoc), `:322-345`, `:463+`, `:482-487`. |

## 3. 26.2 architecture facts (api-map §0)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 3.1 | Frame split: `runTick` drives `gameRenderer.update` → `pick(worldPartialTicks)` → `gameRenderer.extract` (`Minecraft.java:1290-1295`); `GameRenderer.update :372 / extract :379 / render :396 / renderLevel :525` | **CONFIRMED** | `REF:Minecraft.java:1285-1296` (exact sequence, incl. `this.level.update()` at :1287); all four GameRenderer declarations verified at cited lines. |
| 3.2 | `ClientLevel` holds final `LevelExtractor` (:152), no `levelRenderer` field; ctor `(ClientPacketListener, ClientLevelData, ResourceKey, Holder<DimensionType>, int, int, LevelExtractor, boolean, long, int seaLevel)` :238-249; `chunkSource` final :169 assigned `new ClientChunkCache(this, serverChunkRadius)` :252; `connection` :151 final; `mapData` :160; `hasChunk` body `return true` :521; `tickNonPassenger` :470 first stmt `entity.setOldPosAndRot()` | **CONFIRMED** | `REF:ClientLevel.java:149-172, 238-252, 466-472, 519-523` — every field/line re-read. The "HEAD before-last-tick-pos contract survives exactly" claim is correct. |
| 3.3 | `MultiBufferSource`/`BufferSource`, `CoreShaders`, `CompiledShaderProgram`, `CompiledShader`, `ShaderInstance` do not exist in 26.2 | **CONFIRMED** | `find` across `REF:` for all five class files returns nothing. TARGET-GONE verdicts for `MixinMultiBufferSourceBufferSource`, `CoreShadersAccessor`, `MixinShaderInstance`, `MixinCompiledShader`, `MixinRenderSystem_Clipping` stand. |
| 3.4 | Entity path extract→submit→execute: `extractVisibleEntities :221` / `isEntityVisible :251` (calls `shouldRender` :254) → `submitFeatures :279` / `submitEntities :653` (loop `entityRenderDispatcher.submit(state, cameraRenderState, …)` :660-661) → `executeSolid :419` / `executeTranslucent :434` | **CONFIRMED** | `REF:LevelExtractor.java:221-258`, `REF:LevelRenderer.java:279-281, 419, 434, 653-662`. §11 ⑪'s "receives EntityRenderState, not Entity" caveat verified from the loop. |
| 3.5 | Camera owns view/projection: `update :93` → `alignWithEntity` call :103 → `prepareCullFrustum` :106 (decl :195, snapshots `this.position`) → `setupPerspective` :109 (decl :326); `setupOrtho :330` exists; `extractRenderState :118` copies isPanoramicMode :120, smartCull :121-125 (spectator-in-solid → false), pos, cullFrustum :132, projectionMatrix :134, viewRotationMatrix :135; `isDetached :419`, `getFluidInCamera :437`, `setLevel :491`, `setPosition :353` protected | **CONFIRMED** | `REF:Camera.java` — all verified. The api-map's injection-ordering instruction (adjustCameraPos after alignWithEntity, before prepareCullFrustum) is correct: prepareCullFrustum consumes `this.position` at :106. |
| 3.6 | Terrain visibility: `LevelExtractor.applyFrustum :373-381` → `sectionOcclusionGraph().addSectionsInFrustum(frustum, visibleSections(), nearbyVisibleSections())`; BFS `sectionOcclusionGraph.update(...)` `LevelRenderer.java:271`; `setupRender` gone | **CONFIRMED** | `REF:LevelExtractor.java:373-381` exact body (incl. main-thread assert); `REF:LevelRenderer.java:271`; no `setupRender` in 26.2 LevelRenderer. |
| 3.7 | Terrain draw: `prepareChunkRenders(Matrix4fc)` :509 → `renderGroup(OPAQUE :409 / TRANSLUCENT :438)` inside addMainPass lambda; `executeTranslucentAfterTerrain` :440 | **CONFIRMED** | `REF:LevelRenderer.java:409, 419, 434, 438, 440, 509`. |
| 3.8 | GL-only surface: `GlStateManager`/`GlDebug` under `com.mojang.blaze3d.opengl`; Vulkan backend exists | **CONFIRMED** | `REF:com/mojang/blaze3d/opengl/GlStateManager.java`, `GlDebug.java` exist; cull funnel `GlCommandEncoder.java:791-795` (`if (pipeline.isCull()) _enableCull(); else _disableCull();`) — the mirror-cull re-site strategy (flip inside `_enableCull` cancel) is sound. |
| 3.9 | Shaders: pipelines via `RenderPipelines.register` (private, :815 region); GLSL via `ShaderManager` (`preprocessor.process` :93, `getShaderSource` :248, `device.precompilePipeline(pipeline, cache::getShaderSource)` :155) | **CONFIRMED** | `REF:ShaderManager.java:93, 155, 203, 248` verified. The `MixinCompiledShader` re-site (transform at the ShaderManager source layer so every backend sees patched GLSL) is architecturally coherent. |
| 3.10 | Fog: instance `fog/FogRenderer` — `setupFog(Camera,int,DeltaTracker,float,ClientLevel)` :167 → `FogData` (environmentalStart/End, renderDistanceStart/End, skyEnd, cloudEnd, color — `FogData.java:8-15`); `updateBuffer :188`; `getBuffer :76`; `RenderSystem.setShaderFog(GpuBufferSlice)` :112; no static fog-color fields | **CONFIRMED** | All verified at cited lines. `MixinRenderSystem_Fog`'s retarget (ModifyArg/inject on `updateBuffer` or mutate setupFog's FogData) is viable. `MixinFogRenderer` TARGET-GONE verdict stands. |

## 4. 26.2 per-mixin verdicts (api-map §1–§11)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 4.1 | `AbstractClientPlayer` has NO `clientLevel` field; ctor `(ClientLevel, GameProfile)`; level in private `Entity.level :215` + `protected setLevel :3962` | **CONFIRMED** | `REF:AbstractClientPlayer.java:22-30` (fields: playerInfo, showExtraEars, clientAvatarState only); `REF:Entity.java:215, 3962`. NEEDS-RETARGET verdict correct. |
| 4.2 | `LivingEntity.lerpTo` gone → `InterpolationHandler` (field :252, `getInterpolation()` :3279; `interpolateTo :49`, `interpolate :77`, `cancel :111`, `hasActiveInterpolation :69`) | **CONFIRMED** | `REF:LivingEntity.java`, `REF:world/entity/InterpolationHandler.java` — all lines verified. |
| 4.3 | `useShaderTransparency` → `GameRenderState.useShaderTransparency()` :17-19; `addInitialScreens` → `Gui` private boolean :381 (called :364); both gone from Minecraft | **CONFIRMED** | `REF:state/GameRenderState.java:17-19` exact body; `REF:gui/Gui.java:364, 381`; grep in Minecraft.java: absent. |
| 4.4 | `pick(float)` moved to `Minecraft` private :2934 (per-frame :1292); `handleKeybinds :1865` with `startAttack :1940` / `startUseItem :1944,:1959` / `pickBlockOrEntity :1948` (decl :2353) / `continueAttack :1962`; `shouldEntityAppearGlowing :2657` PORTS-CLEAN; `tick :1758`, `tickEntities :1797`, `level.tick(() -> true) :1819`; `wrapRunnable :2668`; superclass `ReentrantBlockableEventLoop` :261; `levelRenderer` **public final** :281; `smartCull` public :354; `debugEntries :292,:422` | **CONFIRMED** | Every line verified in `REF:Minecraft.java`. `MixinGameRenderer_B` retarget-to-Minecraft.pick and `MixinMinecraft_B` pickBlockOrEntity rename both correct. |
| 4.5 | `MultiPlayerGameMode`: `startDestroyBlock :151`, `stopDestroyBlock :212`, `continueDestroyBlock :229`, `startPrediction :297` (try-with-resources :298) + send ModifyArg site :301; `player.level()` survives in **exactly two** places (:194 in the start-destroy prediction lambda, :256 in continueDestroyBlock); direct `this.connection.send` in startDestroyBlock/stopDestroyBlock at :178/:220; `UseOnContext` public 3-arg ctor derives `player.level()`, **protected** 5-arg ctor :24; site `new UseOnContext(player, hand, blockHit)` :370 | **CONFIRMED** | All verified. Full-file grep of `player.level()` yields exactly :194 and :256. The :178/:220 sends are line-wrapped (`this.connection` ⏎ `.send(...)`) — citations point at statement start and are correct. |
| 4.6 | `BiomeAmbientSoundsHandler` TARGET-GONE: no `biomeManager` field; `tick()` resolves live `player.level().environmentAttributes().getValue(AMBIENT_SOUNDS, pos)` each tick — vanilla solved the mixin's purpose | **CONFIRMED** | `REF:BiomeAmbientSoundsHandler.java:45-49` exact body. |
| 4.7 | `ParticleEngine`: `extract(ParticlesRenderState,Frustum,Camera,float)` :122, invoked `LevelExtractor.extract :199` with `new Frustum(cullFrustum).offset(-3.0F)`; vanilla `setLevel(@Nullable ClientLevel)` :131 (duck obsolete); `tick()` :79; `ParticleGroup.tickParticles()` :31; `level` field :31 | **CONFIRMED** | All verified incl. the exact offset(-3.0F) at `REF:LevelExtractor.java:199`. |
| 4.8 | `GlDebug.printDebugLog` now **private instance** :71, registered as callback :119/:133, package `com.mojang.blaze3d.opengl` | **CONFIRMED** | `REF:opengl/GlDebug.java:71,119,133`. |
| 4.9 | `GlStateManager` opengl: `_glGenBuffers :202`, `_glGenVertexArrays :208`, `_enableCull :322`, `_disableCull :327`; mod already mixes into it (`MOD:...stencil/GlStateManagerMixin.java:22-31`) | **CONFIRMED** | `REF:opengl/GlStateManager.java` + MOD file verified (its :22-31 is the `_glBindFramebuffer` capture — the class-level "already mixes into" claim is what matters and holds). |
| 4.10 | `RenderTarget`/`MainTarget` TARGET-GONE: GpuDevice-based (`createBuffers :78` with `GpuFormat.D32_FLOAT` :86; `copyDepthFrom :65`; MainTarget `createFrameBuffer :25`, `allocateDepthAttachment(Dimension)` :85 with D32_FLOAT :87); stencil formats `D32_FLOAT_S8_UINT`/`D24_UNORM_S8_UINT` exist in GpuFormat (~:59-61); replacement = mod's shipped chain (`GlConstMixin.java:33-40` HEAD-cancel `toGlInternalId`: D32_FLOAT→GL_DEPTH24_STENCIL8 (35056); `RenderTargetMixin.java:39+` `createFbo` @RETURN re-attach) | **CONFIRMED** | All verified, incl. both MOD files at cited lines. Note: GpuFormat lives at `REF:com/mojang/blaze3d/GpuFormat.java` (doc cites bare filename — unpathed, not wrong). |
| 4.11 | `RenderSystem`: **no `setShader`** method; `setShaderFog(GpuBufferSlice)` :112; `modelViewStack` private static final :60; `getModelViewStack()` :208 | **CONFIRMED** | grep + reads of `REF:RenderSystem.java`. `MixinRenderSystem_Clipping` TARGET-GONE verdict stands. |
| 4.12 | `SectionRenderDispatcher`: ctor :57 with redirect site surviving (`this.bufferPool = renderBuffers.sectionBufferPool()` :65); `setCameraPosition(Vec3)` :110 called from `LevelRenderer.repositionCamera` :311; `RenderSection.index` public final :205; `sectionMesh` AtomicReference init UNCOMPILED :206; `reset()` **public** :257 (vanilla calls from `ViewArea.releaseAllBuffers` :40-44); `resortTransparency :285`; `SectionTask.isCancelled` protected final AtomicBoolean :559 | **CONFIRMED** | All verified. `IEChunkCompileTask` retarget to `SectionTask` and `MixinRenderSection` duck simplifications are correct. |
| 4.13 | `ViewArea` new 7-arg ctor `(SectionRenderDispatcher, minY, maxY, minSectionY, maxSectionY, renderDistance, SectionOcclusionGraph)` + `RotatingSectionStorage` factory lambda :35-37; `getRenderSectionAt :87` / protected `getRenderSection(long) :91` | **CONFIRMED** | `REF:ViewArea.java:19-44, 87-93`. |
| 4.14 | `ClientboundPlayerPositionPacket` = record `(int id, PositionMoveRotation change, Set<Relative> relatives)`, composite STREAM_CODEC, **no FriendlyByteBuf ctor** → TARGET-GONE / UNKNOWN-NEEDS-DESIGN | **CONFIRMED** | `REF:ClientboundPlayerPositionPacket.java:12-21`; no buf ctor in file. The "read pos from packet.change()" retarget note follows directly. |
| 4.15 | `ServerboundMovePlayerPacket` base ctor now `(DDDFFZZZZ)V` (added horizontalCollision); subclass `write(FriendlyByteBuf)` private, registered `Packet.codec(X::write, X::read)` — write-@RETURN wire-append still viable | **CONFIRMED** | `REF:ServerboundMovePlayerPacket.java:43-55` (ctor), `:99-128` (Pos codec/read/write handling the raw buf). |
| 4.16 | `ClientPacketListener`: ctor :431, `level` :390; handlers `handleAddEntity :566`, `handleMovePlayer :794`, `handleLevelChunkWithLight :849`, `handleForgetLevelChunk :908`, `handleSetTime :1129`, `handleSetEntityPassengersPacket :1145`, `handleBlockChangedAck :2435` (site :2437), `handleSetEntityData :631` (site `this.level.getEntity(packet.id())` :633); `ensureRunningOnSameThread` now `(Packet, T, PacketProcessor)` (`PacketUtils.java:21`); `setTimeFromServer :445` used :1132 **plus** `clockManager.handleUpdates` + `environmentAttributes().invalidateTickCache()` :1133-1135; `ClientboundSetTimePacket` record with `gameTime()` | **CONFIRMED** | Every line verified. The warning that the per-world set-time fan-out must replicate the clock/attribute side-effects is real vanilla behavior. |
| 4.17 | `ClientLevel.playSound` PORTS-CLEAN: exact private descriptor `(DDD,SoundEvent,SoundSource,FFZJ)` at :710, single positioned-sound funnel (playSeededSound :676/:685, playLocalSound :694/:706), distance source `gameRenderer.mainCamera().position()`, same >100² playDelayed body | **CONFIRMED** | `REF:ClientLevel.java:676-720`. |
| 4.18 | `ClientLevel.update()` = pollLightUpdates + `getLightEngine().runLightUpdates()` :496-503, called only for main level from `runTick` :1287 — matches mod's shipped lateUpdateLight anchor | **CONFIRMED** | `REF:ClientLevel.java:496-503`; `REF:Minecraft.java:1287`. |
| 4.19 | `ReceivingLevelScreen` removed; `LevelLoadingScreen` exists; `startWaitingForNewLevel` decl :1624 (calls :537, :1294) | **CONFIRMED** | file absent from REF; `REF:ClientPacketListener.java:537, 1294, 1624`. |
| 4.20 | `SectionBufferBuilderPack` sizing → `ChunkSectionLayer.bufferSize()` inside `Util.makeEnumMap` field-init lambda :13-15 (⚠L) | **CONFIRMED** | `REF:SectionBufferBuilderPack.java:13-15` exact. |
| 4.21 | `DebugScreenOverlay.getSystemInformation` gone; F3 assembled from registered entries; `Minecraft.debugEntries` :292/:422 | **CONFIRMED** (mechanism claim partially refuted — see R1) | grep: no getSystemInformation; `REF:Minecraft.java:292,422`. |
| 4.22 | §10 ②③④⑦: `renderLevel(deltaTracker)` INVOKE at `GameRenderer.java:425` (then `doEntityOutline` :427, post-chain :428-433); world-render WrapOperation target = `minecraft.levelRenderer.render(...)` :563-565 (8-arg `LevelRenderer.render` decl :156); `resize(II)` :294 (calls `levelRenderer.resize` :297; `LevelRenderer.resize` public :763); `renderItemInHand(CameraRenderState,float,Matrix4fc)` :336 | **CONFIRMED** | `REF:GameRenderer.java:420-433, 533-566, 294-298, 336`; `REF:LevelRenderer.java:156, 763`. |
| 4.23 | §10 ducks: `mainCamera` private final :125; `lightmap` :112 (+ extractor :113); `renderBuffers` :103 + `mainRenderTarget` :104 (getter `renderBuffers()` :184); `Camera.isPanoramicMode` :79 + enable/disable ~:495-502 | **CONFIRMED** | `REF:GameRenderer.java:103-104, 112-113, 125, 184`; `REF:Camera.java:79, 496, 501`. |
| 4.24 | §11 map: ⑨ clear lambda `clearPass.executes(() -> …clearColorAndDepthTextures(…))` :195-206 + GameRenderer pre-clear :404-408; ⑫/⑬ `addWeatherPass` :465; ⑯ `addSkyPass` :314 still wraps runnable in `pass.executes`, `FramePass` class survives; ⑰ `cameraState.smartCull` spectator-in-solid at Camera :121-125 + `Minecraft.smartCull` :354; ④ `Lighting.setupLevel` gone → `gameRenderer.lighting().setupFor(Lighting.Entry.LEVEL)` (`LevelRenderer.java:410`; `Lighting.setupFor :75`, `updateLevel :58`); ② `Sheets.translucentItemSheet()` exists :83 but zero call sites in 26.2 LevelRenderer; ①/③ `constantAmbientLight`/`DimensionSpecialEffects`/`endLastBatch` absent; ⑲ `isSectionCompiledAndVisible(BlockPos)` :897 | **CONFIRMED** | All anchors re-verified at cited lines; greps for `translucentItemSheet` (LevelRenderer), `DimensionSpecialEffects`, `endLastBatch` in REF renderer: absent. |
| 4.25 | `MixinLevelRenderer_ForceMainThreadRebuild` retarget: `compileSections(CameraRenderState)` :608 (private) reads `optionsRenderState.prioritizeChunkUpdates` :619-623 → `compileSync :635` vs `compileAsync :638`; translucent resort `scheduleTranslucentSectionResort` :840 (called :643) | **CONFIRMED** | `REF:LevelRenderer.java:605-643, 840`. |
| 4.26 | `IEWorldRenderer` duck map: `entityRenderDispatcher()` :912, `viewArea()` :960 @Nullable, `visibleSections` field :119 / getter :964 / `nearbyVisibleSections` :968 / `clearVisibleSections` :873, `resetLevelRenderData` :878; cullingFrustum moved to `Camera.cullFrustum`/cameraState | **CONFIRMED** | `REF:LevelRenderer.java:117-119, 873, 878, 912, 960-968`; Camera cull-frustum ownership verified in 3.5. |

## 5. REFUTED

| # | Claim (doc) | Verdict | Evidence + correction |
|---|---|---|---|
| R1 | **API map §1, `MixinDebugScreenOverlay` row** (`migration/api-map/mixin-client.md`): "New mechanism: `DebugScreenEntries.register(String, DebugEntry)` static registry (`gui/components/debug/DebugScreenEntries.java:13-21`)" | **REFUTED (minor)** | The entry type is **`DebugScreenEntry`** (not `DebugEntry`), and both `register` overloads are **private**: `private static Identifier register(String id, DebugScreenEntry entry)` (`REF:DebugScreenEntries.java:60`) and `(Identifier, DebugScreenEntry)` (`:64`). Lines :13-21 are vanilla's own static field registrations, not a mod-callable API. **Correction:** the retarget needs an invoker/accessor (or AT) on the private `register` — or direct insertion into the backing map — and the custom entry must additionally be enabled in the profile list (`Minecraft.debugEntries` → `DebugScreenEntryList`) to actually render. The TARGET-GONE verdict and the re-site-as-registered-entry direction remain correct. |
| R2 | **API map §7, `MixinScreenEffectRenderer` row** (`migration/api-map/mixin-client.md`): "overlay is submit-based: `submit(boolean isFirstPerson, boolean isSleeping, float, SubmitNodeStorage, boolean hideGui)` (`ScreenEffectRenderer.java:58`)" | **REFUTED (minor)** | The 4th parameter is **`SubmitNodeCollector`**, not `SubmitNodeStorage`: `public void submit(boolean isFirstPerson, boolean isSleeping, float partialTicks, SubmitNodeCollector submitNodeCollector, boolean hideGui)` (`REF:ScreenEffectRenderer.java:58`). Both class names exist in 26.2, so the wrong one yields a wrong injector descriptor on first attempt. **Correction:** use `SubmitNodeCollector` in the retargeted descriptor. The line number, the `submitBlockSprite(...)` :146 anchor, and the driving site `GameRenderer.renderLevel :574-581` are all correct. |

## 6. Notes (non-refutations)

- **N1:** api-map §12's 26.2 lambda line hints for `startDestroyBlock` prediction lambdas are ":167, :187"; the second `startPrediction` call is at `REF:MultiPlayerGameMode.java:188` (off-by-one). Immaterial — the doc itself mandates re-deriving synthetic-lambda names from the compiled 26.2 jar, not the decompile.
- **N2:** api-map cites `GpuFormat.java:60-61` without a package path; the file is `REF:com/mojang/blaze3d/GpuFormat.java`. Formats and approximate lines are correct.
- **N3:** `MultiPlayerGameMode` `this.connection.send` at :178/:220 are line-wrapped statements (`this.connection` ⏎ `.send(...)`) — a single-line grep misses them; the citations are correct. Recorded so future verifiers don't false-flag.
- **N4 (pass-1 resolution):** pass 1's refutation of the `cubeInFrustum` "same descriptor" claim has been incorporated into the current api-map, whose corrected text (`(DDDDDD)Z` → `(DDDDDD)I`, CIR Boolean→Integer, cancel with a non −2/−1 code, or retarget `isVisible(AABB)`) was re-verified against `REF:Frustum.java:85-102` and is accurate.
