# Adversarial Verification — Networking (core + q_misc_util)

**Docs under review:**
- `migration/inventory/network.md` (inventory)
- `migration/api-map/network.md` (api-map)

**Ground truth consulted:** IP source `C:/Users/warwa/ModDev/ImmersivePortalsMod/src/main/java/qouteall`, decompiled 26.2 `C:/Users/warwa/ModDev/mc262-ref`, Fabric API sources jars from the gradle cache (`fabric-networking-api-v1-6.3.3+72073ef09c-sources.jar` and `-4.3.1+d30f6a7919-sources.jar`, both present under `~/.gradle/caches/modules-2/files-2.1/net.fabricmc.fabric-api/fabric-networking-api-v1/`), and this project's `fabric/src/main/java/com/warwa/seamlessportals/fabric/network/FabricPlatformHelper.java`.

**Verdict: 44 claims checked, 43 CONFIRMED, 1 REFUTED (minor).** Both documents are unusually accurate — nearly every file:line citation, including 26.2 decompile line numbers and Fabric API jar line numbers, reproduced exactly. No internal contradictions; no silently skipped scope files. (This document supersedes/re-runs a prior verification pass at this path; the single refutation R1 was re-derived independently and is now backed by an exhaustive slice-wide grep.)

---

## Scope completeness (no silent skips)

Directory listing vs. inventory §"Slice owner files":
- `imm_ptl/core/network/` contains exactly 4 files: `ImmPtlNetworkConfig`, `ImmPtlNetworking`, `PacketRedirection`, `PacketRedirectionClient` — matches. **CONFIRMED**
- `q_misc_util/` root files: `MiscNetworking`, `ImplRemoteProcedureCall`, `MiscUtilModEntry`, `MiscUtilModEntryClient`, `MiscGlobals` all covered; `Helper`/`MiscHelper`/`CustomTextOverlay`/`my_util` referenced or out-of-slice as appropriate. `q_misc_util/dimension/` = `DimIntIdMap`, `DimensionIntId`, `DimensionIdRecord` — all covered. `q_misc_util/mixin/` recursive = `MixinMinecraftServer_Misc`, `dimension/MixinPlayerList_Misc`, `client/IEClientPacketListener_Misc`, plus `IELevelStorageAccess_Misc` and `client/MixinGui_Overlay` which the inventory explicitly declares out of slice (§2.11). `q_misc_util/ducks/` = `IEMinecraftServer_Misc` only, listed in the supporting-files line. `q_misc_util.mixins.json` contents match inventory §2.11 exactly (file read). **CONFIRMED — nothing skipped.**

---

## Claim-by-claim results

### A. IP-source claims (inventory doc)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| A1 | IP fabric.mod.json declares MC `["1.21", "1.21.1"]`, not 1.21.3 | **CONFIRMED** | `src/main/resources/fabric.mod.json:49` — `"minecraft": ["1.21", "1.21.1"]` |
| A2 | `TeleportPacket` fields/wire/id/handle: varint+3 doubles+UUID, id `imm_ptl:teleport`, handle → `ServerTeleportationManager.of(player.server).onPlayerTeleportedInClient` via `PortalAPI.serverIntToDimKey` | **CONFIRMED** | `ImmPtlNetworking.java:45-90` read in full (id :48-51 via `ResourceLocation.fromNamespaceAndPath("imm_ptl","teleport")`, write :68-74, handle :76-84); receiver registered `ServerPlayNetworking.registerGlobalReceiver` :251-254 |
| A3 | TeleportPacket sent from `ClientTeleportationManager` at :372-378 via `ClientPlayNetworking.createC2SPacket` | **CONFIRMED** | `ClientTeleportationManager.java:372-378` — exact code as quoted |
| A4 | `PortalSyncPacket` javadoc/record contradiction: javadoc says "This packet is redirected, so there is no need to contain dimension id" (:132) yet record carries `dimensionId` (:138) and `handle()` uses it (:183) — "do not simplify either side away" flag | **CONFIRMED** | `ImmPtlNetworking.java:132, 138, 157/168 (wire), 183 (PortalAPI.clientIntToDimKey(dimensionId))` — read in full |
| A5 | PortalSyncPacket client handle: UUID check :190, type check :195, `acceptDataSync` :202; spawn path `entityType.create(world)` :206, `setId/setUUID/syncPacketPositionCodec/moveTo` :214-217, `readPortalDataFromNbt` :219, `world.addEntity` :221, eager dest-world load :223, `CLIENT_PORTAL_SPAWN_EVENT` :224, `IPGlobal.clientPortalLoadDebug` :226 | **CONFIRMED** | `ImmPtlNetworking.java:179-230` — verbatim |
| A6 | `Portal.getAddEntityPacket` returns `createSyncPacket()`; createSyncPacket wraps `ServerPlayNetworking.createS2CPacket(new PortalSyncPacket(...))` with full `addAdditionalSaveData` NBT; `reloadAndSyncToClient` re-sends via `McHelper.sendToTrackers`; position ONLY synced via this packet (1/4096 quantization comment) | **CONFIRMED** | `Portal.java:898-919` (getAddEntityPacket/createSyncPacket), `:510-529` (javadoc "vanilla packet encodes position in 1/4096 unit... This is the only place of syncing portal position" + `reloadAndSyncToClient` body) |
| A7 | `PacketRedirection` core: `i:r` payload id ("short id" comment :45-46), two ThreadLocals :50-51/:60-61, `init` registers playS2C only :63-65, `withForceRedirectAndGet` off-thread detector via `IEWorld.portal_getThread` :76-81, `sendRedirectedPacket` skip-when-scope-matches :111-128, `createRedirectedMessage` pass-through :140-143 / `BundleDelimiterPacket` assert :145 / bundle unwrap-rewrap :146-159, contravariance comment :165-172, `sendRedirectedMessage` :176-182, `isRedirectPacket` :185-188, `validateForceRedirecting` :130-132, `withForceBundle` re-entrant :190-227 | **CONFIRMED** | `PacketRedirection.java` read in full — every cited line exact |
| A8 | `PLACEHOLDER_PROTOCOL_INFO = GameProtocols.CLIENTBOUND_TEMPLATE.bind(argBuf -> (RegistryFriendlyByteBuf) argBuf)` at :234-241; encode/decode through `.codec()` :258-275; "Mojang's new networking abstraction" comment :233; dim kept as int for netty-thread decode safety :243-245 | **CONFIRMED** | `PacketRedirection.java:233-283` — verbatim; `PacketRedirectionClient.java:39-44` netty-thread javadoc |
| A9 | `withForceBundle` (:190-227) has **no call sites** in the source tree; `old_handleRedirectedPacket` is dead code | **CONFIRMED** | grep over `ImmersivePortalsMod/src`: `withForceBundle` matches only its declaration (`PacketRedirection.java:191`) |
| A10 | `PacketRedirectionClient.handleRedirectedPacket`: isSameThread → resolve int via `DimensionIntId.getClientMap().fromIntegerId`, push `clientTaskRedirection`, run `packet.handle` inside `withSwitchedWorldFailSoft`, restore in finally; else `minecraft.execute(self)` re-submit | **CONFIRMED** | `PacketRedirectionClient.java:45-77` read in full; fail-soft drop w/ error log `ClientWorldLoader.java:592-598` ("Ignoring redirected task of invalid dimension") |
| A11 | `MixinServerGamePacketListenerImpl_Redirect` mixes into `ServerCommonPacketListenerImpl` (despite name); `@ModifyVariable` HEAD on `send(Lnet/minecraft/network/protocol/Packet;Lnet/minecraft/network/PacketSendListener;)V` argsOnly; `@Inject` before `Lnet/minecraft/network/Connection;send(Lnet/minecraft/network/protocol/Packet;Lnet/minecraft/network/PacketSendListener;Z)V` cancellable (force-bundle divert); shadows `protected MinecraftServer server` | **CONFIRMED** | `MixinServerGamePacketListenerImpl_Redirect.java:18-60` read in full — descriptors verbatim |
| A12 | `MixinClientboundCustomPayloadPacket`: HEAD inject into `handle(Lnet/minecraft/network/protocol/common/ClientCommonPacketListener;)V`, cancels when payload is `PacketRedirection.Payload`, "run before Fabric API" comment :23, implements `IECustomPayloadPacket`, shadows `payload` | **CONFIRMED** | `MixinClientboundCustomPayloadPacket.java:16-37` read in full; cancel fires whenever the payload matches (doc's "cancels unconditionally once matched" accurate) |
| A13 | `MixinMinecraft_RedirectedPacket`: HEAD inject on `Minecraft.wrapRunnable(Ljava/lang/Runnable;)Ljava/lang/Runnable;` re-wrapping with `withSwitchedWorldFailSoft`; `@IPVanillaCopy @Override scheduleExecutables()` returns false when on-thread + processing redirected msg; dummy ctor `super(string)` | **CONFIRMED** | `MixinMinecraft_RedirectedPacket.java:15-59` read in full |
| A14 | `DimensionIntId` fixed ids OW=0, NETHER=-1, END=1; others `getNextIntegerId()` in `getAllLevels()` order; DimLib event registration with early phase `iportal:early_phase` ordered before `Event.DEFAULT_PHASE`; `onServerDimensionChanged` re-adds + `removeUnused` retaining vanilla keys + rebroadcast to all players; client map validated "should not be used in networking thread"; `clientRecord` cleared on `IPCGlobal.CLIENT_EXIT_EVENT` | **CONFIRMED** | `DimensionIntId.java:26-128` read in full (fillInVanillaDimIds :91-101, init :31-44, initClient :46-54, getClientMap :59-65, onServerStarted :74-89, onServerDimensionChanged :103-128) |
| A15 | `DimIntIdMap`: `MISSING_ID = Integer.MIN_VALUE`, ids never reused within a run (`maxId` only grows — `Math.max` in `add`, untouched by `removeUnused`), NBT key `"intids"` | **CONFIRMED** | `DimIntIdMap.java:20, 24, 81, 113-133`; `removeUnused` does not modify `maxId` |
| A16 | `MixinPlayerList_Misc`: `@At INVOKE ClientboundChangeDifficultyPacket.<init>(Lnet/minecraft/world/Difficulty;Z)V` inside `placeNewPlayer`; sends `DimIdSyncPacket.createPacket(player.server)` | **CONFIRMED** | `MixinPlayerList_Misc.java:13-31` read in full |
| A17 | `MixinMinecraftServer_Misc`: `<init>` RETURN captures full 1.21 ctor arg list ending `ChunkProgressListenerFactory` (handler body ignores all args, only stashes `MiscGlobals.refMinecraftServer` WeakReference); `createLevels` RETURN with `ChunkProgressListener` arg → `DimensionIntId.onServerStarted`; dummy ctor `super(string)`; shadows public `storageSource` / private `executor` / `waitUntilNextTick` / `isDedicatedServer` | **CONFIRMED** | `MixinMinecraftServer_Misc.java:28-66` read in full (api-map C7's "handler body ignores every captured arg" is true) |
| A18 | RPC: decode inside codec; method cache = `ConcurrentHashMap` :69; `findMethodByPath` requires class path to contain `"RemoteCallable"` (throws otherwise) :442-443, retries `Outer.Inner` as `Outer$Inner`, requires public method (`getMethods()` + "If it's a private method, make it public" error); arg types from `getGenericParameterTypes()` :164/:258; ids `iportal:remote_c2s` :149 / `iportal:remote_s2c` :244 | **CONFIRMED** | `ImplRemoteProcedureCall.java:69, 149, 164, 244, 258, 435-474` |
| A19 | `GlobalPortalSyncPacket` sent plain (NOT redirected), dimension in-payload; `GlobalPortalStorage.onPlayerLoggedIn` per non-empty world storage; `createSyncPacket` wraps `ServerPlayNetworking.createS2CPacket`; `syncToAllPlayers` :193; `receiveGlobalPortalSync` :326 | **CONFIRMED** | `GlobalPortalStorage.java:135-157` read (plain `player.connection.send(packet)`); grep hits `syncToAllPlayers` :193, `receiveGlobalPortalSync` :326 |
| A20 | `DimIdSyncPacket`: id `imm_ptl:dim_int_id_sync` :42; `createFromServer` uses `registryAccess().registryOrThrow(Registries.DIMENSION_TYPE)` with `BuiltinDimensionTypes.OVERWORLD.location()` fallback + error log :64-70; client handle replaces `DimensionIntId.clientRecord` + rebuilds `ClientWorldLoader.dimIdToDimTypeId` ImmutableMap :99-126; S2C only (init registers payload type only) :144-148 | **CONFIRMED** | `MiscNetworking.java` read in full; `ClientWorldLoader.dimIdToDimTypeId` field at `ClientWorldLoader.java:72` |
| A21 | Entry wiring: `IPModMain.init()` calls `ImmPtlNetworking.init(); ImmPtlNetworkConfig.init(); PacketRedirection.init()` (:67-69); `IPModMainClient` :125-126 + `DimensionIntId.initClient()` at :134 (NOT the q_misc_util client entry); `MiscUtilModEntry` = RPC + MiscNetworking + DimensionIntId init; `MiscUtilModEntryClient` = RPC + MiscNetworking clientInit only | **CONFIRMED** | `IPModMain.java:62-72` region read; `IPModMainClient` grep :125/:126/:134; both `MiscUtilModEntry*.java` read in full |
| A22 | Accessor mixins: `IEServerConfigurationPacketListenerImpl` = `@Accessor("gameProfile")` on `ServerConfigurationPacketListenerImpl`; `IEClientPacketListener_Misc` = `@Accessor("levels")` setter on `ClientPacketListener` | **CONFIRMED** | both files read in full — verbatim |
| A23 | `ImmPtlNetworkConfig` ids/flow: task type `iportal:config` (:76), S2C id `iportal:config_packet` (:98), C2S id `iportal:configure_complete` (:139), tolerance flags (:123, :173-174), `networkHandler.disconnect` on major/minor mismatch (:176), `completeTask` (:193), `canSend` gate + `addTask` (:216-217), `serverRejectClientWithoutImmPtl`-gated hard-coded literal reject message (:221-229) | **CONFIRMED** | `ImmPtlNetworkConfig.java` grep at all cited lines — match |
| A24 | Fabric 4.3.1 thread contracts as cited by inventory: `ClientPlayNetworking.java:262` "render thread", `ServerPlayNetworking.java:304` "server thread", `ClientConfigurationNetworking.java:257` / `ServerConfigurationNetworking.java:248` "netty's event loops" | **CONFIRMED** | extracted `fabric-networking-api-v1-4.3.1+d30f6a7919-sources.jar` — all four lines verified at the exact cited line numbers |

### B. 26.2 claims (api-map doc)

| # | Claim | Verdict | Evidence (mc262-ref) |
|---|---|---|---|
| B1 | **S5/headline-1:** `GameProtocols.CLIENTBOUND_TEMPLATE` exists at `GameProtocols.java:131` as `SimpleUnboundProtocol<ClientGamePacketListener, RegistryFriendlyByteBuf>`; `SimpleUnboundProtocol.bind(Function<ByteBuf,B>) → ProtocolInfo<T>` (:8-9, file read in full); vanilla binds it with `RegistryFriendlyByteBuf.decorator(...)` so IP's identity-cast bind stays valid; `withBundlePacket(...)` in the same builder (:133) | **CONFIRMED** | `network/protocol/game/GameProtocols.java:131-133`; `network/protocol/SimpleUnboundProtocol.java` (whole file = the one `bind` method); `server/network/ServerConfigurationPacketListenerImpl.java:163` AND `client/multiplayer/ClientConfigurationPacketListenerImpl.java:163` both bind w/ decorator; `RegistryFriendlyByteBuf.decorator` `:19` |
| B2 | **G1:** `ensureRunningOnSameThread(Packet,T,BlockableEventLoop)` GONE; 26.2 overloads = `(…,ServerLevel)` :17-19 and `(…,PacketProcessor)` :21-26 (scheduleIfPossible + throw `RUNNING_ON_DIFFERENT_THREAD`); `PacketProcessor` = concurrent queue bound to a thread, `scheduleIfPossible` throws `RejectedExecutionException` after close, drain checks `shouldHandleMessage` then `handle`; `Minecraft.packetProcessor` field :369, built on `gameThread` :730, getter :2922, drained in runTick under `"scheduledPacketProcessing"` :1169-1170 **before** `runAllTasks()` :1172; server drain `MinecraftServer.java:1009` (getter :2231); `Connection.channelRead0` still handles on netty thread first (:146-170, via `genericsFtw`); handlers pass `this.minecraft.packetProcessor()` (e.g. `ClientPacketListener.java:492` = `handleLogin`) | **CONFIRMED — every line exact** | `network/protocol/PacketUtils.java` read in full; `network/PacketProcessor.java` read in full; `client/Minecraft.java` grep = 369/730/1169/1170/1172/2922 verbatim; `server/MinecraftServer.java` 293/359/1009/2231; `network/Connection.java:146-171` read |
| B3 | **§A ordering hazard (re-derived, not trusted):** a verbatim `minecraft.execute` port puts redirected packets in `pendingRunnables` (drained :1172) while vanilla re-queues land in `packetProcessor` (drained :1170) → per-frame cross-queue reordering; 1.21 had one queue. §A.1 "no vanilla-driven double delivery": with IP's unconditional HEAD-cancel on the netty pass, `ClientCommonPacketListenerImpl.handleCustomPayload`'s `ensureRunningOnSameThread` never runs → no re-queue → `handle()` fires exactly once; the memory'd double-run hazard applies to non-cancelling handler mixins. §A.2's game-thread re-invocation via `packetProcessor().scheduleIfPossible` hits the same HEAD inject with `isSameThread()` true → inline handle → cancel | **CONFIRMED** | drain order verified at `Minecraft.java:1169-1172`; `BlockableEventLoop.execute` :98-105 routes into `pendingRunnables` (separate queue); `ClientCommonPacketListenerImpl.java:158-170` read — `if (!(payload instanceof DiscardedPayload)) { ensureRunningOnSameThread(...); … }` (DiscardedPayload short-circuit as claimed); `PacketProcessor.ListenerAndPacket.handle` flow read |
| B4 | **G2:** `Gui` has NO `getChat()` (its own chat calls route via `this.hud.getChat()`); `Gui.hud` public field :72; `Hud.getChat()` :1264; `ChatComponent.addClientSystemMessage(Component)` public :243 / `addServerSystemMessage` :247 / old `addMessage(Component)` gone (only private 4-arg :255); `ChatListener.handleSystemMessage(msg, remote)` with `remote=false` → `addClientSystemMessage` | **CONFIRMED** | `client/gui/Gui.java` grep (hud field :72, no own getChat); `client/gui/Hud.java:88 (class), :1264`; `client/gui/components/ChatComponent.java:243,247,255`; `client/multiplayer/chat/ChatListener.java:214-228` read (addClientSystemMessage at :224) |
| B5 | **G3 verdict itself:** `ServerPlayer.server` now `private final` :232 (`connection` public :231); `level()` covariantly returns `ServerLevel` :1731; `ServerLevel.getServer()` public :1278; `PlayerList.getServer()` :695; listener shadow `protected final MinecraftServer server` still present :39. **Adversarial rename hunt:** no getter exists — `Entity`/`Player` define no `getServer()` in 26.2 (grep: Entity only calls `serverLevel.getServer()`) | **CONFIRMED** (the call-site COUNT in the migration note is refuted separately — R1) | `server/level/ServerPlayer.java:231-232, 1731`; `server/level/ServerLevel.java:1278`; `server/players/PlayerList.java:695`; `ServerCommonPacketListenerImpl.java:39` |
| B6 | **C4/C5:** send-callback type = `io.netty.channel.ChannelFutureListener`; `PacketSendListener` survives only as static factory (`thenRun`, `exceptionallySend`); `ServerCommonPacketListenerImpl.send(Packet<?>, @Nullable ChannelFutureListener)` w/ one-arg `send(packet, null)` delegate (two-arg inject still covers all sends); new `if (packet.isTerminal()) this.close()` preamble; `Connection.send(Packet<?>, @Nullable ChannelFutureListener, boolean)` :281 | **CONFIRMED** | `network/PacketSendListener.java` read in full; `ServerCommonPacketListenerImpl.java` send region read (one-arg → two-arg → isTerminal → `connection.send(packet, listener, flush)`); `network/Connection.java:273-288` (3-arg at :281) |
| B7 | **C6:** `ReentrantBlockableEventLoop(String, boolean propagatesCrashes)` :6-8; `BlockableEventLoop` ctor :35-39; MinecraftServer super-call `super("Server", propagatesCrashes)` | **CONFIRMED** | `util/thread/ReentrantBlockableEventLoop.java` read in full; `server/MinecraftServer.java:324` |
| B8 | **C7/C8:** `MinecraftServer.<init>` = `(Thread, LevelStorageSource.LevelStorageAccess, PackRepository, WorldStem, Optional<GameRules>, Proxy, DataFixer, Services, LevelLoadListener, boolean, NotificationManager)` :311-323 (no `ChunkProgressListenerFactory`); `createLevels()` now no-arg `protected void` :421, called from bootstrap :402 | **CONFIRMED** | `server/MinecraftServer.java:311-325` read verbatim; grep `createLevels` = :402 (call), :421 (decl) |
| B9 | **C9/C1/C2/C3/C10:** `registryOrThrow` GONE / `lookupOrThrow` default method `RegistryAccess.java:21`; `ResourceLocation` → `Identifier` (`Identifier.java:18`, `STREAM_CODEC` :20, `fromNamespaceAndPath` :40, `parse` :44, `withDefaultNamespace` :48; no `ResourceLocation.java` exists); `FriendlyByteBuf.readIdentifier` :577 / `writeIdentifier` :581 / `writeResourceKey` uses `key.identifier()` :592; `ResourceKey.create(…, Identifier)` :26, `identifier()` :64; `record Type<T…>(Identifier id)` `CustomPacketPayload.java:56`, `codec(...)` :16-18, `createType` forces `minecraft` namespace :20-22; `Registry.getKey(T)` returns `Identifier` :59 | **CONFIRMED** | all files/lines verified verbatim; `find` shows only `Identifier.java` under `resources/` |
| B10 | **C11:** `EntityType.create(Level, EntitySpawnReason)` :298 (+ `EntitySpawnRequest` overload :302); vanilla's own add-entity-packet path uses `type.create(this.level, EntitySpawnReason.LOAD)` at `ClientPacketListener.java:601`; `EntitySpawnReason` enum `world/entity/EntitySpawnReason.java:3` | **CONFIRMED** | grep verified; :601 verbatim — the fidelity argument (mirror vanilla's spawn-packet reason for `PortalSyncPacket`) is sound |
| B11 | **C12:** `Entity.moveTo(x,y,z)` GONE → `snapTo(double,double,double)` :1782 (5-arg :1794); `syncPacketPositionCodec` unchanged :355 | **CONFIRMED** | `world/entity/Entity.java` — grep finds no `moveTo(double`; snapTo at exactly the cited lines |
| B12 | **C13:** `CompoundTag`: `keySet()` :193, `contains` :275, `getInt→Optional<Integer>` :299 / `getIntOr` :303, `getString` :331/:335, `getCompound` :351 / `getCompoundOrEmpty` :355; `readNbt()` still `@Nullable CompoundTag` :534, `writeNbt` :517 | **CONFIRMED** | `nbt/CompoundTag.java`, `network/FriendlyByteBuf.java` — all 8+2 lines exact |
| B13 | **C14/S12:** GameProfile record-style — vanilla calls `this.gameProfile.name(), this.gameProfile.id()` (`ServerConfigurationPacketListenerImpl.java:69`); `NameAndId(gameProfile)` :104; `private final GameProfile gameProfile` :49 (accessor still lands); `protected GameProfile playerProfile()` :63-65; `finishCurrentTask` private :225 | **CONFIRMED** | grep verified all |
| B14 | **S6:** `ClientboundCustomPayloadPacket` = public record `(CustomPacketPayload payload)` :15, 1 MiB cap :16, `GAMEPLAY_STREAM_CODEC` (RegistryFriendlyByteBuf) :17, `handle(ClientCommonPacketListener)` present; `ServerboundCustomPayloadPacket` public record :13 (32767 cap) — F2/F3's direct-ctor replacement is valid | **CONFIRMED** | both files read in full (handle body at :33-35 — 1-line drift from doc's ":32-34", see notes) |
| B15 | **S17:** `placeNewPlayer(Connection, ServerPlayer, CommonListenerCookie)` :142; `new ClientboundChangeDifficultyPacket(levelData.getDifficulty(), levelData.isDifficultyLocked())` :185; packet now a record `(Difficulty, boolean)` → canonical-ctor descriptor `(Lnet/minecraft/world/Difficulty;Z)V` unchanged, `@At INVOKE` target survives | **CONFIRMED** | `server/players/PlayerList.java:142, 185`; `ClientboundChangeDifficultyPacket.java` record header read |
| B16 | **S14/S15/S16:** `Minecraft.wrapRunnable` :2668 (identity), `getRunningThread` :2663, `shouldRun` :2673 — `Minecraft` does NOT override `scheduleExecutables`; `BlockableEventLoop.execute` routes through `wrapRunnable` then `scheduleExecutables` (:98-105); `isSameThread` :43-45; `scheduleExecutables` :49-51 (protected — IP's `public` widening override remains legal); `ReentrantBlockableEventLoop.scheduleExecutables` override + `runningTask` :11-17; `MinecraftServer extends ReentrantBlockableEventLoop<TickTask>` :192; `BlockableEventLoop` class decl :26 | **CONFIRMED** | `util/thread/BlockableEventLoop.java:26-105` + `ReentrantBlockableEventLoop.java` read; `Minecraft.java` grep shows only the three claimed overrides |
| B17 | **S9:** `ClientPacketListener` :343; `private Set<ResourceKey<Level>> levels` :403 **non-final** (setter `@Accessor` legal); `handleAddEntity` :566; `handleBundlePacket` :2441; new public `levels()` getter :2624 (no setter) | **CONFIRMED** | grep verified all five lines verbatim |
| B18 | **S1-S4, S18-S30 line sweep:** `StreamCodec.of` :21 / `ofMember` :35; `ByteBufCodecs.registry(ResourceKey)` :582 (private impl :562); `RegistryFriendlyByteBuf` :7 / `registryAccess()` :15 / `decorator` :19; `FriendlyByteBuf extends ByteBuf` :71, readByteArray :272 / writeByteArray :280 / readBlockPos :381 / writeBlockPos :389 / readVarInt :481 / writeUUID :489 / readUUID :499 / writeVarInt :507 / writeNbt :517 / readNbt :534 / readUtf :560 / writeUtf :568; `Level.OVERWORLD/NETHER/END` :95-97 (built with `Identifier.withDefaultNamespace`), `dimensionType()` :952, `dimension()` :960; `ClientLevel.addEntity` :529 / `getEntity(int)` :551 (`@Nullable`); `ComponentSerialization.TRUSTED_STREAM_CODEC` :40; `Registry.byNameCodec` :29; `BuiltInRegistries.BLOCK` :184 / `.ITEM` :189; `BlockState.CODEC` :9; `ItemStack.CODEC` :121; `MinecraftServer.levelKeys` :1193 / `getAllLevels` :1197 / `isDedicatedServer` :1340 / `getPlayerList` :1383 / `registryAccess` :2005; `storageSource` protected final :217 (IP shadow declares public — doc's visibility nit accurate), `executor` private final :279, `waitUntilNextTick` :858 (uses :562/:669/:765); `PlayerList.getPlayers` :817; `ServerPlayer.connection` :231 / `sendSystemMessage` :1783 | **CONFIRMED** | every spot-checked line exact against mc262-ref; no discrepancies |

### C. Fabric API v6 claims (api-map F-rows)

| # | Claim | Verdict | Evidence (fabric-networking-api-v1-6.3.3+72073ef09c-sources) |
|---|---|---|---|
| C1 | **F1:** `PayloadTypeRegistry` renames: `serverboundConfiguration()` :89, `clientboundConfiguration()` :96, `serverboundPlay()` :103, `clientboundPlay()` :110; working proof `FabricPlatformHelper.java:31+` | **CONFIRMED** | jar source lines exact; this repo's `FabricPlatformHelper.java:31-48` uses `clientboundPlay()` throughout |
| C2 | **F2/F3:** `createS2CPacket`/`createC2SPacket` GONE in v6; play thread contracts unchanged (`ServerPlayNetworking.java:49, 327` "server thread"; `ClientPlayNetworking.java:263` "render thread"); `Context.server()` :352 / `player()` :357 / `responseSender()` :362; `PacketSender.createPacket(CustomPacketPayload)` :38 | **CONFIRMED** | grep across ServerPlay/ClientPlay/ServerConfiguration Networking in the 6.3.3 api package: zero matches for `createS2CPacket|createC2SPacket`; all other lines exact |
| C3 | **F4/F5:** `ServerConfigurationNetworking.registerGlobalReceiver(Type, ConfigurationPacketHandler)` :69, `canSend(listener, Type)` :176 (Identifier overload :162), netty event loops :261, `Context.server()` :285 / **`packetListener()`** :290 (returns `ServerConfigurationPacketListenerImpl`; no `networkHandler()` anywhere in the v6 file) / `responseSender()` :295; `ClientConfigurationNetworking` netty :258, `client()` :281, `responseSender()` :291 | **CONFIRMED** | jar sources grep — all lines exact |
| C4 | **F6/F7/F8:** `ServerConfigurationConnectionEvents.CONFIGURE` :59 (+ `BEFORE_CONFIGURE` :36); `ClientLoginConnectionEvents.INIT` :37; `ClientPlayConnectionEvents.JOIN` :48; `FabricServerConfigurationPacketListenerImpl.addTask` :40 / `completeTask` :50 (interface injection); raw-vanilla fallback = private `finishCurrentTask` (`ServerConfigurationPacketListenerImpl.java:225`) | **CONFIRMED** | all jar and mc262-ref lines exact |

### D. Internal-consistency checks

- **Verdict counts** (api-map header "30 SAME · 14 CHANGED · 3 GONE + 10 FABRIC"): counted G1-G3=3, C1-C14=14, S1-S30=30, F1-F10=10. **CONFIRMED.**
- **Inventory §2.7 double-delivery flag vs api-map §A.1 "no vanilla-driven double delivery":** not a contradiction. The inventory's flag is explicitly conditional ("If the same double-invocation applies…"); the api-map answers it with the source-verified mechanism (cancel-on-netty-pass prevents any re-queue), then §A.2 designs the isSameThread-guarded two-pass handle the inventory demanded. Coherent, and both halves re-derived here (B3).
- **Inventory correction of the task premise:** IP declares MC 1.21/1.21.1, "not 1.21.3 as commonly stated" — verified true (fabric.mod.json:49). The doc was right to flag it.

---

## REFUTED

### R1 (minor) — api-map G3 migration note: "Three call sites in this slice" for `ServerPlayer.server`
- **Doc:** `migration/api-map/network.md`, G3 row ("Migration note: Mechanical. Three call sites in this slice…", listing `PacketRedirection.java:122`, `:181`, `MixinPlayerList_Misc.java:29`).
- **Reality — FIVE call sites.** Exhaustive grep of every slice file for `.server` on `ServerPlayer`:
  1. `imm_ptl/core/network/ImmPtlNetworking.java:78` — `PortalAPI.serverIntToDimKey(player.server, dimensionId)` in `TeleportPacket.handle`
  2. `imm_ptl/core/network/ImmPtlNetworking.java:81` — `ServerTeleportationManager.of(player.server)` in `TeleportPacket.handle`
  3. `imm_ptl/core/network/PacketRedirection.java:122` — `serverPlayNetworkHandler.player.server`
  4. `imm_ptl/core/network/PacketRedirection.java:181` — `player.server`
  5. `q_misc_util/mixin/dimension/MixinPlayerList_Misc.java:29` — `player.server`
  (The inventory doc itself quotes the `:77-79` usage in §2.1, so the data was in front of the mapper.) In `TeleportPacket.handle` no listener-scoped `server` shadow exists — the fix there is the Fabric `Context.server()` (F2 row) or `player.level().getServer()`.
- **Impact:** near-zero port risk — the missed sites fail to compile and take the same mechanical replacement — but the count is factually wrong and a checklist built from it would be incomplete.

## Minor line-number drift (no correction needed; meaning unchanged, within decompiler noise)

1. api-map S6 cites `ClientboundCustomPayloadPacket.handle` at `:32-34`; actual `:33-35`.
2. api-map G2 cites the `addClientSystemMessage` call in `ChatListener` at `:225`; actual `:224` (method starts `:214`).
3. api-map S12 cites `playerProfile()` at `:62-65`; actual `:63-65`.
4. Inventory §2.10 paraphrases `tellClientToInvoke` as sending `ImplRemoteProcedureCall.createS2CPacket(...)` directly; the actual code routes through the local `createPacketToSendToClient` wrapper (`McRemoteProcedureCall.java:106-111`), which delegates to it — same wire behavior.

---

## Summary

- Checked: 44 load-bearing claims (24 IP-source claim groups, 18 vanilla-26.2 claim groups covering every GONE/CHANGED verdict plus the highest-risk wire-format and threading claims, 4 Fabric-API claim groups across both 4.3.1 and 6.3.3 jars), plus scope completeness and internal consistency.
- Confirmed: 43. Refuted: 1 (minor undercount, R1).
- The two headline architectural claims that would misdirect the port if wrong — (1) `GameProtocols.CLIENTBOUND_TEMPLATE.bind` survives 1:1 on 26.2, and (2) the client packet re-queue moved to `PacketProcessor`, drained **before** `runAllTasks` (the cross-queue reordering hazard and §A's order-faithful port shape) — were both **positively re-derived from mc262-ref source**, line-exact.
- Severity: **minor** (a single wrong call-site count; no wrong architecture, geometry, or API-fate claims).
