# Adversarial Verification — Render Core slice (pass 2)

Verified: `migration/inventory/render-core.md` + `migration/api-map/render-core.md`
Method: re-derived every geometry/sign claim from IP source; re-opened every cited 26.2 file and checked signatures/line numbers; grepped `mc262-ref` for renames behind GONE verdicts; listed the slice directory against the inventory; checked mod-side proof citations; re-read every IP mixin annotation cited as a hook point.

Abbreviations: `IP:` = `C:/Users/warwa/ModDev/ImmersivePortalsMod/src/main/java/qouteall/imm_ptl/core/`, `262:` = `C:/Users/warwa/ModDev/mc262-ref/`, `MOD:` = `Portal 26.2/common/src/main/java/com/warwa/seamlessportals/`.

**Verdict summary: 38 claims checked, 35 CONFIRMED, 3 REFUTED — one of them a wrong API-fate verdict (api-map G24) that would misdirect the port. Severity: MAJOR.**

Note on the earlier verification pass: its single finding (a dropped negation in inventory §3.5's lateUpdateLight placement sentence) is already fixed in the current inventory revision — §3.5 now correctly reads "must **not** run before world rendering" and quotes `IP:render/MyRenderHelper.java:440` verbatim. That finding is closed.

---

## REFUTED

### R1 — api-map G24 (MAJOR): entity-visibility gate classified GONE; the actual IP mixin target survives signature-identical in 26.2
- **Claim** (`migration/api-map/render-core.md`, row G24): "`EntityRenderDispatcher.render` HEAD visibility gate … `render` no longer exists; `submit` sees only `EntityRenderState` … The only per-entity hooks left are extraction-side … Move the gate to extraction (inject into `extractVisibleEntities`/`isEntityVisible`)."
- **Reality:** IP's mixin never targeted `render`. `IP:mixin/client/render/MixinEntityRenderDispatcher.java:15-19` is `@Inject(method = "…shouldRender(Lnet/minecraft/world/entity/Entity;Lnet/minecraft/client/renderer/culling/Frustum;DDD)Z", at = @At("HEAD"), cancellable = true)`, forcing `false` when `CrossPortalEntityRenderer.shouldRenderEntityNow` denies (`:28-31`). That exact method **exists in 26.2 with an identical signature**: `public <E extends Entity> boolean shouldRender(E entity, Frustum culler, double camX, double camY, double camZ)` — `262:net/minecraft/client/renderer/entity/EntityRenderDispatcher.java:127-129` — and vanilla's own extraction path calls it: `LevelExtractor.isEntityVisible` → `entityRenderDispatcher().shouldRender(entity, frustum, camX, camY, camZ)` (`262:net/minecraft/client/renderer/extract/LevelExtractor.java:254`).
- **Correction:** this touchpoint is **SAME (call timing changed)**, not GONE. The zero-deviation port keeps the identical `@Mixin(EntityRenderDispatcher)` `shouldRender`-HEAD inject; the semantic delta is only that the gate now runs at **extraction time**, so `shouldRenderEntityNow`'s inputs (`PortalRendering.isRendering()`, the rendering portal) must be valid when the portal pass's extract runs — which G1's per-dimension extract choreography already guarantees. The new-injection redesign G24 prescribes is unnecessary.
- **Why major:** a wrong API-fate verdict on a hook the api-map's own Top-risk summary #2 folds into "the single hardest 1:1-fidelity item in this slice"; as written it steers the porter away from the faithful mechanism that exists unchanged.

### R2 — inventory §3.4 / §4 (minor, same root error): wrong target method named for the gate
- **Claim** (`migration/inventory/render-core.md` §3.4 item 3): gate "injected at `EntityRenderDispatcher.render` HEAD (`mixin/client/render/MixinEntityRenderDispatcher.java:28`)"; repeated in §4: "`EntityRenderDispatcher.render` HEAD gate — `MixinEntityRenderDispatcher.java:28`". This inventory line is the origin the api-map's G24 inherited.
- **Correction:** the `@Inject` targets **`EntityRenderDispatcher.shouldRender(Entity, Frustum, double, double, double)Z`** at HEAD (cancellable, forces `false`) — `IP:mixin/client/render/MixinEntityRenderDispatcher.java:15-19`; the `shouldRenderEntityNow` consumption at `:28` is correctly cited, only the target method name is wrong.

### R3 — api-map G29 (minor): misquotes IP's premultiplied blend func; contradicts the doc's own C42
- **Claim** (`migration/api-map/render-core.md`, row G29): "IP's exact premultiplied func `(ONE, ONE_MINUS_SRC_ALPHA, ONE, ONE_MINUS_SRC_ALPHA)` exists as a vanilla constant: `BlendFunction.TRANSLUCENT_PREMULTIPLIED_ALPHA`."
- **Reality:** IP's actual call is `blendFuncSeparate(ONE, ONE_MINUS_SRC_ALPHA, **ZERO, ONE**)` — `IP:render/MyRenderHelper.java:388-393` (alpha factors ZERO/ONE, deliberately preserving destination alpha per the derivation comment `:380-387`). The vanilla constant is `(ONE, ONE_MINUS_SRC_ALPHA, ONE, ONE_MINUS_SRC_ALPHA)` (`262:com/mojang/blaze3d/pipeline/BlendFunction.java:16-18`) — its alpha equation differs from IP's. The doc's own row C42 states IP's factors correctly and prescribes a custom `BlendFunction`; G29 and C42 contradict each other, and C42 is the correct one.
- **Correction:** in G29, IP's func is `(ONE, ONE_MINUS_SRC_ALPHA, ZERO, ONE)`; `TRANSLUCENT_PREMULTIPLIED_ALPHA` matches only the color equation. The faithful port builds `new BlendFunction(ONE, ONE_MINUS_SRC_ALPHA, ZERO, ONE)` via the 4-factor ctor (`BlendFunction.java:33`). Impact contained because C42 already carries the right instruction — but a porter reading only G29 would ship the wrong alpha equation for `drawScreenFrameBuffer`.

---

## CONFIRMED (35 nontrivial claims, re-derived)

### A. Geometry / sign / transform (all re-derived from IP source, none trusted)

| # | Claim (doc §) | Evidence |
|---|---|---|
| A1 | Inner clip equation code quote; kept side `normal·p + c > 0`; camera-relative world coords; "normal points to the non-clipped side" (inventory §3.3) | `IP:render/FrontClipping.java:74` (comment), `:102-120` (quote matches character-for-character; `.subtract(cameraPos)` `:111`), `:113` (kept-side comment) |
| A2 | Uniform upload uses the BEFORE-model-view equation; disabled uniform `(0,0,0,1)` (inventory §2.2/§3.3) | `IP:render/FrontClipping.java:191` (after-MV variant commented out `:192`), `:204`, `:223` |
| A3 | `transformClipEquation` = `transpose(inverse(M))·eq`, exposed via getter only (inventory §3.3) | `IP:render/FrontClipping.java:95-98` (invert→transpose→transform), `:171-173` |
| A4 | Terrain layers pass `-FrontClipping.ADJUSTMENT` with the z-fight comment; view-area mesh path passes `0`; `ADJUSTMENT = 0.01` (inventory §3.3) | `IP:mixin/client/render/MixinLevelRenderer.java:195-201`; `IP:render/FrontClipping.java:54-59`, `:29` |
| A5 | Mod-vs-IP clip-space mismatch (mod = VIEW space, IP = camera-relative world space) — real fidelity gap (inventory §2.2, api-map C41) | `MOD:render/FrontClipping.java` header ("Plane is in VIEW space (camera at origin, looking down −Z)") vs `IP:render/FrontClipping.java:191` |
| A6 | Rotation continuity: `immediateFinalRot = oldCameraRotation · conj(portalRot)`; gravity strip; ±90° pitch reflection; direct writes to `setYRot/setXRot`, `yRotO/xRotO`, `yBob/xBob/yBobO/xBobO`; `animationDelta = conj(newCameraRotationWithGravity) · immediateFinalRot`; 0.1° threshold, 1 s duration, `sin` easing (inventory §2.6/§3.7) | `IP:render/TransformationManager.java:154-157`, `:180`, `:188-193`, `:195-203`, `:211-213`, `:215-220`, `:226-228`, `:128-132` |
| A7 | Mirror transform `new Matrix4f().reflection(x, y, z, 0)` (inventory §2.6) | `IP:render/TransformationManager.java:251-253` |
| A8 | Camera re-setup after rotation writes: 5-arg `camera.setup(level, player, !firstPerson, isMirrored, partialTick)` (inventory §2.6, api-map G21 IP side) | `IP:render/TransformationManager.java:230-239` |
| A9 | Projection camera-displacement trick: `newCameraPos = entityInstantPos − transformPoint(entityInstantPos) + cameraPos`; scale/rot pose wrap translate→scale→mulPose→untranslate; draw via `ip_myRenderEntity` + immediate `endBatch()` (inventory §3.4) | `IP:render/CrossPortalEntityRenderer.java:283-292` (derivation comments + code), `:316-337`, `:300-308` |
| A10 | FrustumCuller planes: `planeW = -origin·normal`, kept side `> 0`; extremal-corner tests (in-front tests min-along-normal corner `> 0`; behind tests max-along-normal corner `< 0`); outer culling = fully-behind-plane AND fully-inside-4-plane-frustum; Mirror flips vertex winding (inventory §2.10) | `IP:render/FrustumCuller.java:243-246` (comment), `:247-267`, `:229-240`, `:190-195` |
| A11 | Clipping plane sources: outer = this-side pos+normal ("'remaining' side" comment); inner = **other-side** pos+normal; view-area quad = `axisW·w/2`, `axisH·h/2`; `outputFullQuad` emits 2 triangles over (±1,±1) (inventory §2.3/§3.3) | `IP:portal/shape/RectangularPortalShape.java:111-118`, `:121-129`, `:143-158`; `IP:render/ViewAreaRenderer.java:211-223` |

### B. World-switch flow / IP mechanics

| # | Claim | Evidence |
|---|---|---|
| B1 | `renderWorldNew` pushes/pops `WorldRenderInfo` around `switchAndRenderTheWorld` (inventory §2.1/§3.1) | `IP:render/MyGameRenderer.java:96-112` |
| B2 | Recursive `client.gameRenderer.renderLevel(client.getTimer())` inside `invokeWrapper.accept` with profiler bracket (inventory §3.1 phase 3, api-map G1 IP side) | `IP:render/MyGameRenderer.java:229-235` |
| B3 | Projection save carries the view-bobbing/scale comment; scratch `visibleSections` list swap (`VisibleSectionDiscovery.takeList()` + `portal_setChunkInfoList`) protects same-instance renderers (inventory §3.1 phases 1, 2.1) | `IP:render/MyGameRenderer.java:159-161`, `:163-166` |
| B4 | `MyGameRenderer.init()` has zero call sites (dead cleanup registration) (inventory §2.1/§5) | repo-wide grep for `MyGameRenderer.init` over the IP checkout: no hits |
| B5 | `lateUpdateLight` iterates worlds NOT in `renderedDimensions` → `runLightUpdates()`; header comment "should not run before world rendering…"; `earlyRemoteUpload` per-other-dim `uploadAllPendingUploads()` with the never-completing-future javadoc; `applyMirrorFaceCulling` = `glCullFace(GL_FRONT)` (inventory §2.5/§3.5) | `IP:render/MyRenderHelper.java:437-451`, `:453-469`, `:471-477` |
| B6 | WIP-stale claim: `ViewAreaRenderer.java:11` imports removed `ShaderInstance`; `:84` reads `MyRenderHelper.portalAreaShader` which is commented out; migrated registrations `BLIT_SCREEN_NOBLEND`/`PORTAL_AREA`/`PORTAL_DRAW_FB_IN_AREA` via `CoreShadersAccessor.register` (inventory header caveat, §2.5) | `IP:render/ViewAreaRenderer.java:11`, `:84-101`; `IP:render/MyRenderHelper.java:65-81` (live), commented `portalAreaShader` in the dead block near `:168` |
| B7 | Camera-matrix hook = `@WrapOperation` of `Matrix4f.rotation(Quaternionfc)` inside `GameRenderer.renderLevel` → `TransformationManager.processTransformation` (inventory §2.6, api-map C36 IP side) | `IP:mixin/client/render/MixinGameRenderer.java:304-317` |
| B8 | Clip-uniform location mixin: `@Mixin(CompiledShaderProgram.class)`, inject `setupUniforms` RETURN, `Uniform.glGetUniformLocation(programId, "iportal_ClippingEquation")` (inventory §2.2) | `IP:mixin/client/render/shader/MixinShaderInstance.java:18-39` |
| B9 | C40 IP side: `getMinBuildHeight()`/`getMaxBuildHeight()` seeding; view distance degraded by `PerformanceLevel.getPortalRenderingDistance`; per-axis Chebyshev rejection (inventory §2.9/§3.6) | `IP:render/VisibleSectionDiscovery.java:82-87`, `:120`, `:144-150` |
| B10 | `ImmPtlViewArea extends ViewArea`; sections created via inner-class ctor `factory.new RenderSection(0, x, y, z)` (inventory §2.8) | `IP:render/ImmPtlViewArea.java:37`, `:258-263` |

### C. 26.2 verdicts (every cited signature re-opened in `mc262-ref`)

| # | Claim | Evidence |
|---|---|---|
| C-a | Fact 2 extract/render split: `extract` `:379-394`, `levelExtractor.extract(...)` `:389`, `render` `:396`, `renderLevel(DeltaTracker)` `:525` reading only extracted state `:531-535` | `262:net/minecraft/client/renderer/GameRenderer.java` (all lines exact) |
| C-b | Fact 3 reversed-Z: `DEFAULT = (GREATER_THAN_OR_EQUAL, true)`; `DEFAULT_DEPTH_CLEAR_VALUE = 0.0`; `clearDepthTexture(..., 0.0)`; `DepthStencilState` record has NO stencil fields (also backs C37's stencil note) | `262:com/mojang/blaze3d/pipeline/DepthStencilState.java:8-9`; `262:com/mojang/blaze3d/systems/RenderSystem.java:36`; `GameRenderer.java:439`, `:571` |
| C-c | Fact 4 Vulkan backend exists; queries are timestamp-only (`GpuDevice.createTimestampQueryPool(int)` is the sole factory) (also G36) | `262:com/mojang/blaze3d/vulkan/VulkanBackend.java` exists; `262:com/mojang/blaze3d/systems/GpuQuery.java`/`GpuQueryPool.java` exist; `GpuDevice.java:184` |
| C-d | Fact 5 + S1-S5 + C1: `public final LevelExtractor levelExtractor` `:280`, `public final LevelRenderer levelRenderer` `:281`, `level` `:335`, `player` `:336`, `crosshairPickEntity` `:340`, `hitResult` `:341`, `smartCull` `:354`; `getDeltaTracker()` `:2693` (C2) | `262:net/minecraft/client/Minecraft.java` (all lines exact) |
| C-e | Fact 6: vanilla `renderBuffers.endFrame()` at frame tail `:447`; field `:103` | `262:GameRenderer.java:447`, `:103` |
| C-f | GONE hunts G14/G16/G17: `Minecraft.getMainRenderTarget()`/`getProfiler()` absent (grep); `GameRenderer.getRenderDistance()` absent (grep); replacements `gameRenderer.mainRenderTarget()` `:673` and render distance at `Camera.update` `:94` | `262:Minecraft.java`, `262:GameRenderer.java:673`, `262:net/minecraft/client/Camera.java:94` |
| C-g | G18/C9: `renderItemInHand(CameraRenderState, float, Matrix4fc)` private `:336`, invoked unconditionally `:572` | `262:GameRenderer.java:336`, `:572` |
| C-h | G19/C35/G27/G28: `levelProjectionMatrixBuffer` `:129` pushed `:557`; `RenderSystem.setProjectionMatrix(GpuBufferSlice, ProjectionType)` `:180`; `backupProjectionMatrix` `:186` / `restoreProjectionMatrix` `:192` / `getProjectionMatrixBuffer()` `:198`; private `modelViewStack` `:60`, `getModelViewStack()` `:208`, `getModelViewMatrixCopy()` `:203`; `getSequentialBuffer` `:213` (G8); `bindDefaultUniforms` `:276` (G9); no `setShader`/`getShader` members (G5) | `262:GameRenderer.java`, `262:RenderSystem.java` (all lines exact) |
| C-i | C12: `LevelRenderer.render` 8-arg signature; vanilla call `GameRenderer.java:563-565`; declaration `LevelRenderer.java:156` | `262:` both files, exact |
| C-j | LevelRenderer internals: `renderBuffers` `:105` copied from GameRenderer `:145` (C16/G15); `visibleSections` `:119` final + getter `:964` + `nearbyVisibleSections()` `:968` + `clearVisibleSections()` `:873` (C15); `viewArea` `:121` / `entityOutlineTarget` `:122` (C19); `repositionCamera(CameraRenderState)` `:304-307` (C22 driver); main-pass anchors `renderGroup(OPAQUE)` `:409`, `setupFor(Entry.LEVEL)` `:410`, `executeSolid` `:419`, `executeTranslucent` `:434`, `renderGroup(TRANSLUCENT)` `:438`, `executeTranslucentAfterTerrain` `:440` (G37); `prepareChunkRenders(Matrix4fc)` `:509` (G38); private `compileSections(CameraRenderState)` `:608` (C14); `submitEntities` `:653` (G3); `resize` `:763-765` (C21); `invalidateCompiledGeometry` `:796` with `new ViewArea` at `:819` (C13/G25); per-frame `getTransparencyChain()` `:834` (C18); `hasRenderedAllSections` `:893` / `isSectionCompiledAndVisible` `:897` (S34); nullable `sectionRenderDispatcher()` `:908` (C20) | `262:net/minecraft/client/renderer/LevelRenderer.java` (all lines exact) |
| C-k | G25/C22-C27: ViewArea is a thin wrapper — public ctor `:19`, `RotatingSectionStorage` field `:15`, ValueCreator `(index, sectionNode) -> dispatcher.new RenderSection(index, sectionNode)` `:35-37`, `releaseAllBuffers` → `section.reset()` `:40-44`, `repositionCamera(SectionPos)` boolean + occlusion invalidate `:74-81`, `getRenderSectionAt(BlockPos)` `:87-89`, protected `getRenderSection(long)` `:91`; NO grid-size fields / `level` / `createSections` / `setDirty` | `262:net/minecraft/client/renderer/ViewArea.java:13-94` (whole file read) |
| C-l | G1/C17/C19/G23-G24 extractor side: `LevelExtractor` 3-arg ctor `:89`; `extract(DeltaTracker, Camera, float)` `:95`; `lastViewDistance` `:83` checked `:96-98`; **`camera.getCapturedFrustum() == null` gates `applyFrustum` at `:125-130`** (C17's skip claim verbatim); `extractVisibleEntities` `:221` / `isEntityVisible` `:251-257`; dispatcher prepares `:120-121` (G35/C38) | `262:net/minecraft/client/renderer/extract/LevelExtractor.java` (all lines exact) |
| C-m | G26/C26-C31: `fixedBuffers` `:46` from `renderBuffers.fixedBufferPack()` `:64`; `setCameraPosition(Vec3)` `:110`; `lock()` `:135` / `unlock()` `:139` / `uploadTerrainBuffersToGpu()` `:143`; inner `RenderSection(int index, long sectionNode)` `:216`; `getBoundingBox()` `:238`; `reset()` `:257`; `getRenderOrigin()` `:272` / `getSectionNode()` `:277` | `262:net/minecraft/client/renderer/chunk/SectionRenderDispatcher.java` (all lines exact) |
| C-n | G21/C6/C10/C17/C33/C34/C36/S2/S12/G31 camera side: `setup(...)` gone (no such method); `update(DeltaTracker)` `:93` (`setupPerspective(0.05F, depthFar, fov, w, h)` `:109`); private `alignWithEntity` `:249`; private `getMaxZoom` `:297`; `protected setRotation(float,float)` `:339`; `protected setPosition(double,double,double)` `:349` **and** `protected setPosition(Vec3)` `:353` (G21's `:349` and S12's `:353` are two real overloads — no contradiction); `position()` `:359`; `rotation()` `:380`; `getViewRotationMatrix(Matrix4f)` `:384`; `setEntity` `:411`; `setLevel(@Nullable ClientLevel)` `:491`; `isPanoramicMode()` `:318` + enable/disable `:495`/`:500`; `cullFrustum` `:65` / `getCullFrustum()` `:191` / `prepareCullFrustum` `:195` / `getCapturedFrustum` `:209` / `captureFrustum()` `:213` / `killFrustum()` `:217`; `isZZeroToOne()` read `:187`; `smartCull` extracted `:122-125`; `viewRotationMatrix` extracted `:135` and consumed as `modelViewMatrix` at `GameRenderer.java:533`; `attributeProbe` `:80`, ticked `:87`, getter `:423` | `262:net/minecraft/client/Camera.java` (all lines exact; note the file lives at `net/minecraft/client/`, not `client/renderer/` — the docs' bare `Camera.java` cites are line-accurate regardless) |
| C-o | G13/C37: `TextureTarget(@Nullable String label, int width, int height, boolean useDepth, GpuFormat format)` | `262:com/mojang/blaze3d/pipeline/TextureTarget.java:11` |
| C-p | G9/G40/S25: `DefaultVertexFormat.POSITION_COLOR` survives `:44`; **no `BLIT_SCREEN`** in the file — GONE confirmed | `262:com/mojang/blaze3d/vertex/DefaultVertexFormat.java` |
| C-q | C42: `TRANSLUCENT_PREMULTIPLIED_ALPHA = (ONE, ONE_MINUS_SRC_ALPHA, ONE, ONE_MINUS_SRC_ALPHA)`; 4-factor custom ctor available | `262:com/mojang/blaze3d/pipeline/BlendFunction.java:16-18`, `:33` |
| C-r | S14: `public float yBob; xBob; yBobO; xBobO;` | `262:net/minecraft/client/player/LocalPlayer.java:144-147` |
| C-s | G3/C38: `extractEntity` `:132`; `submit(state, cameraState, x, y, z, poseStack, collector)` `:147-148`; `prepare(Camera, Entity)` `:122`; `resetCamera()` `:192`; public `camera`/`crosshairPickEntity` `:51-52` | `262:net/minecraft/client/renderer/entity/EntityRenderDispatcher.java` (all lines exact) |
| C-t | G10/G11: `GlStateManager._glGetUniformLocation(int, CharSequence)` `:182`; `ShaderManager$CompilationCache.getShaderSource(Identifier, ShaderType)` `:248` (public delegate `:202-203`) | `262:com/mojang/blaze3d/opengl/GlStateManager.java`, `262:net/minecraft/client/renderer/ShaderManager.java` |
| C-u | G22: `SectionOcclusionGraph.addSectionsInFrustum` `:114` | `262:net/minecraft/client/renderer/SectionOcclusionGraph.java:114` |
| C-v | C4/C7/S6/S32: `gui.hud.getBossOverlay().shouldCreateWorldFog()` `:562`; `bossOverlayWorldDarkening(float)` `:653-655`; `extractOptions` `:602` snapshots `getCameraType` `:621` / `prioritizeChunkUpdates` `:624`; `renderLevel` called from `render` at `:425` (S32's redirect anchor) | `262:GameRenderer.java` (all lines exact) |
| C-w | G31 fog: instance `FogRenderer` on GameRenderer `:119`; `updateBuffer(cameraState.fogData)` `:559` + `getBuffer(FogMode.WORLD)` `:560`; `RenderSystem.setShaderFog(GpuBufferSlice)` `:112`; fog color rides into `LevelRenderer.render` `:565` | `262:GameRenderer.java`, `262:RenderSystem.java:112` |
| C-x | G5/G10/C41 mod proofs: `MOD:mixin/client/GlCommandEncoderClipMixin.java` injects at `trySetup(Lcom/mojang/blaze3d/opengl/GlRenderPass;Ljava/util/Collection;)Z` RETURN, queries `GL_CURRENT_PROGRAM`, per-program cache over `GlStateManager._glGetUniformLocation`, `GL20.glUniform4f` — the working per-draw clip-upload mechanism as described | `MOD:mixin/client/GlCommandEncoderClipMixin.java` (~`:30-60`, read) |

### D. Scope + cross-doc consistency

- **Directory listing check:** `IP:render/` root contains exactly 19 `.java` files, all covered by inventory §2.1-2.18 (LoadingIndicatorRenderer included); subpackages `context_management/`, `optimization/`, `renderer/` are explicitly declared out-of-slice. **No silent skips.**
- **Non-contradictions verified:** G21 `setPosition :349` vs S12 `setPosition(Vec3) :353` = two real overloads; inventory §3.1 `:229-235` vs G1 `:231-233` for the recursive render = outer wrapper vs inner call, both correct.
- **One real cross-doc contradiction found:** G29 vs C42 on IP's blend alpha factors (see R3; C42 is correct).

---

## Severity: **major**

Driven by R1: api-map G24 assigns GONE (with a redesign prescription) to a hook whose exact IP mixin target — `EntityRenderDispatcher.shouldRender(Entity, Frustum, DDD)Z` — survives signature-identical in 26.2 and sits on vanilla's own extraction path. R2 is the same error at its inventory source; R3 is a self-contradicted blend-function misquote whose correct form already exists in C42. Every other sampled claim — including all geometry/sign derivations, the reversed-Z/extract-split cross-cutting layer, and every re-opened 26.2 line citation — was exact.
