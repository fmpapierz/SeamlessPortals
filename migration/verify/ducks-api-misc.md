# Adversarial Verification — Ducks, API, Commands, Small Packages

Verifier pass over:
- `migration/inventory/ducks-api-misc.md` (inventory doc)
- `migration/api-map/ducks-api-misc.md` (api-map doc)

Method: re-derived geometry/sign claims from IP source (`C:/Users/warwa/ModDev/ImmersivePortalsMod/src/main/java/qouteall`), re-opened every cited 26.2 file (`C:/Users/warwa/ModDev/mc262-ref`), grep-hunted for renames behind GONE verdicts, and compared the inventory against an actual directory listing of the slice scope.

**Result: 34 claims checked, 33 CONFIRMED, 1 REFUTED (minor). Scope complete — no silently skipped files. No internal contradictions found.**

---

## 0. Scope completeness

| Claim | Verdict | Evidence |
|---|---|---|
| Slice = 59 files: 36 ducks + api(2) + example(1) + commands(7) + block_manipulation(2) + mc_utils(4) + miscellaneous(5) + debug(1) + redstone(1) | **CONFIRMED** | `ls` of all nine directories: 36 duck files counted, 7 command files, 4 mc_utils, 5 miscellaneous, 2+2+1+1+1 = 59 total. Every file has an inventory entry (§2.1–2.7). Nothing skipped. |

## 1. Geometry / sign / transform claims (re-derived from IP source)

| # | Claim (doc) | Verdict | Evidence |
|---|---|---|---|
| 1 | `getHitResultForPlacing`: global portals only; `normal·sideVec < −0.9`; `isBoxInPortalProjection`; plane distance < 0.6; transform `hitCenter + 0.501·sideVec`; returned face **flipped** (`side.getOpposite()`); retargeted to `portal.getDestDim()` (inventory §3.3, §2.4) | **CONFIRMED** | BlockManipulationServer.java:104-133 — `p.getNormal().dot(sideVec) < -0.9` (:112), `isBoxInPortalProjection(...new AABB(hitPos))` (:113-116), `getDistanceToPlane(hitCenter) < 0.6` (:116), `portal.transformPoint(hitCenter.add(sideVec.scale(0.501)))` (:123), `side.getOpposite()` (:128), `portal.getDestDim()` (:133). |
| 2 | `canPlayerReach`: base distance² = `6*6*4*4` × `computeBlockReachScale(player)²`; same-dim pass OR nearby portal with `getDestDim()==dimension` + `isInteractableBy` + `transformPoint(playerPos)` within distance² × portal-scale² (inventory §2.4) | **CONFIRMED** | BlockManipulationServer.java:79-97 — `distanceSquare = 6 * 6 * 4 * 4 * playerScale * playerScale` (:83); portal branch exactly as described (:89-97). |
| 3 | `placePortalShift` computes dest along `−axisW×axisH` (inventory §2.3) | **CONFIRMED** | PortalCommand.java:2331 — `portal.setDestination(portal.getOriginPos().add(portal.getAxisW().cross(portal.getAxisH()).scale(-dist)))`. Sign and cross-product order match the doc. |
| 4 | Client re-trace back-offsets start by `0.0015 × ray` "to avoid being considered inside block"; skips `PortalPlaceholderBlock` and `Blocks.BARRIER`; block-vs-fluid by nearer distance²; hit below `getMinBuildHeight()+0.1` coerced to DOWN face; only non-air sets `remotePointedDim` (inventory §3.2 step 2) | **CONFIRMED** | BlockManipulationClient.java:140-145 (skips), :152 `start.subtract(end.subtract(start).scale(0.0015))` (backward along ray ✓), :160-164 (nearer d²), :176-183 (DOWN coercion), :185-190 (non-air gate). |
| 5 | Pointing: portal hit accepted if nearer than `getCurrentTargetDistance() + 0.2`; vanilla `hitResult` replaced by synthetic MISS; remaining segment `[beginDistance,endDistance]` endpoints transformed with `portal.transformPoint` (inventory §3.2 step 1); `getCurrentTargetDistance` treats `PortalPlaceholderBlock` targets as infinitely far (inventory :91-106) | **CONFIRMED** | BlockManipulationClient.java:76 (`+ 0.2`), :77 (`createMissedHitResult`), :117-122 (both endpoints via `transformPoint`), :100-102 (placeholder → 23333). |
| 6 | `validateReach`: portal-aware raytrace max 32, `result.world() == targetWorld`, `distManhattan(targetPos) < 8` (inventory §2.4) | **CONFIRMED** | BlockManipulationServer.java:288-301, ClipContext.Block.COLLIDER (:295). |
| 7 | `rotate_portals_around` rotates origin+axes and composes the **conjugate** into the rotation transform (inventory §2.3 :1689-1729) | **CONFIRMED** | PortalCommand.java:1704-1711 — offset rotated around origin, axisW/axisH rotated, then `portal.setRotationTransformationD(portal.getRotationD().hamiltonProduct(quaternion.getConjugated()))`. |

## 2. IP-source structural / dependency / mixin claims

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 8 | `IEWorldRenderer.ip_myRenderEntity` signature uses `MultiBufferSource` (both docs, G1) | **CONFIRMED** | ducks/IEWorldRenderer.java:19-27, import :5. `PostChain` import present :6 (S10 claim also confirmed). |
| 9 | `IEShader` mixin is *named* `MixinShaderInstance` but targets `CompiledShaderProgram`; exposes GL uniform location for the clipping plane (inventory §2.1) | **CONFIRMED** | MixinShaderInstance.java:18 `@Mixin(CompiledShaderProgram.class)`, :19 `implements IEShader`, :33 `Uniform.glGetUniformLocation(programId, "iportal_ClippingEquation")` at `setupUniforms` RETURN. |
| 10 | Four dead ducks (`IEPlayerEntity`, `IEPlayerListEntry`, `IESimpleRegistry`, `IEWorldChunk`) have zero implementors/users (inventory §2.1 notes) | **CONFIRMED** | Repo-wide grep: each name matches only its own file. |
| 11 | `IEChunkMap` split across two mixins on the same target: `MixinChunkMap_C` `priority=1100` + `MixinChunkMap_E` (inventory §2.1) | **CONFIRMED** | MixinChunkMap_C.java:21-22 (`@Mixin(value = ChunkMap.class, priority = 1100)`, `implements IEChunkMap`); MixinChunkMap_E.java:27-28 (`@Mixin(ChunkMap.class)`, `implements IEChunkMap`). |
| 12 | `MixinEntity_U` injects into `setPosRaw(DDD)V` at INVOKE of `EntityInLevelCallback.onMove()V` and into `setRemoved` RETURN; dispatches `IPEntityEventListenableEntity`; `Portal` implements the interface (inventory §2.5) | **CONFIRMED** | MixinEntity_U.java:12-33 (both anchors exact); Portal.java:83-84 (`class Portal extends Entity implements PortalLike, IPEntityEventListenableEntity`). |
| 13 | `method_41930` synthetic lambda name pinned in a `@Redirect` at MixinMultiPlayerGameMode.java:42; `@ModifyArg` on `ClientPacketListener.send` inside `startPrediction`/`startDestroyBlock`/`stopDestroyBlock` (:82-113); `UseOnContext` ctor `@Redirect` in `performUseItemOn`; `ServerboundUseItemPacket` deliberately NOT redirected (inventory §3.2 step 3-4) | **CONFIRMED** | MixinMultiPlayerGameMode.java:41-50 (`method = "method_41930"` with comment "lambda in startDestroyBlock"), :82-113 (three ModifyArgs), :64-80 (UseOnContext NEW redirect to 5-arg ctor), :140 (comment "ServerboundUseItemPacket is not redirected"). RPC endpoint FQN strings at :124/:135 match the wire-protocol claim. |
| 14 | Pointing hook: `GameRenderer.pick(F)V` RETURN → `updatePointedBlock`; HEAD of same method cancelled while a portal-view renders (inventory §3.2 step 1) | **CONFIRMED** | MixinGameRenderer_B.java:16-23 (HEAD cancel gated on `WorldRenderInfo.isRendering()`), :25-30 (RETURN → `BlockManipulationClient.updatePointedBlock`). |
| 15 | `canDoCrossPortalInteractionEvent` is a Fabric `Event<Predicate<Player>>`, results ANDed, checked on both sides (inventory §2.4; api-map F5) | **CONFIRMED** | BlockManipulationServer.java:59-68 (AND fold), server check :75, client check BlockManipulationClient.java:64. |
| 16 | The two server replays are `@IPVanillaCopy`: `doProcessPlayerAction` → `handleBlockBreakAction(pos, action, dir, world.getMaxBuildHeight(), seq)` + `ackBlockChangesUpTo`; `doProcessUseItemOn` → `isItemEnabled`/`mayInteract`/`useItemOn`/`shouldSwing`/dual `ClientboundBlockUpdatePacket` with build-height check (inventory §3.3) | **CONFIRMED** | BlockManipulationServer.java:207-225, :236-286 (`offseted.getY() >= getMinBuildHeight() && < getMaxBuildHeight()` :279 — exclusive upper bound, matching the api-map's inclusive-`getMaxY` off-by-one warning). |
| 17 | `MixinMinecraft` wiring: FIELD inject at `Minecraft.fps` in `runTick` → `ClientPerformanceMonitor.updateEverySecond`; `addInitialScreens` RETURN append when `!initialScreenShown`; `ip_setFrameBuffer` sets `mainRenderTarget` (inventory §2.6, §5) | **CONFIRMED** | MixinMinecraft.java:144-154 (FIELD target `fps:I`, shift AFTER), :185-194, :196-199. |
| 18 | `ClientPerformanceMonitor` free memory = `maxMemory − (totalMemory − freeMemory)` (inventory §2.6) | **CONFIRMED** | ClientPerformanceMonitor.java:42-46. |
| 19 | `ExampleGuiPortalRendering` lazily creates `new TextureTarget(2, 2, true, true)` (inventory §2.2) | **CONFIRMED** | ExampleGuiPortalRendering.java:112 (comment "automatically resized when rendering" :110-111). |

## 3. 26.2 GONE verdicts (rename-hunted in mc262-ref)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 20 | G1: `MultiBufferSource` GONE | **CONFIRMED** | `grep -rl MultiBufferSource` over all of mc262-ref → 0 files. |
| 21 | G2: `CompiledShaderProgram` and `ShaderInstance` GONE | **CONFIRMED** | `find` → no `CompiledShaderProgram.java`, no `ShaderInstance.java`. |
| 22 | G3: `LightTexture` GONE, replaced by `Lightmap`; GameRenderer holds `private final Lightmap lightmap` (:112) + `LightmapRenderStateExtractor` (:113) + `UiLightmap` (:114) | **CONFIRMED** | No `LightTexture.java`; `net/minecraft/client/renderer/Lightmap.java` exists; GameRenderer.java:112-114 fields exactly as cited. |
| 23 | G4: `LevelRenderer.renderLineBox` GONE, no `ShapeRenderer.java` | **CONFIRMED** | `grep -rn renderLineBox` → 0 hits repo-wide; no ShapeRenderer.java. (Rename hunt: no `lineBox`-named survivor found.) |
| 24 | G5: `GuiGraphics` GONE, `GuiGraphicsExtractor` exists | **CONFIRMED** | No GuiGraphics.java; net/minecraft/client/gui/GuiGraphicsExtractor.java exists. |
| 25 | G6: `CommandSourceStack.hasPermission(int)` GONE; `permissions()` → `PermissionSet` (:377); `Commands.hasPermission(PermissionCheck)` (~:541); `LEVEL_ALL/MODERATORS/GAMEMASTERS/ADMINS/OWNERS` constants (:163-167); level 2 → `Permissions.COMMANDS_GAMEMASTER = HasCommandLevel(GAMEMASTERS)` (Permissions.java:7) | **CONFIRMED** | grep of CommandSourceStack.java: no `hasPermission`, `permissions()` at :377; Commands.java LEVEL_* constants and `hasPermission(PermissionCheck)` present; Permissions.java:6-9 constants exactly as mapped (COMMANDS_GAMEMASTER at :7). |
| 26 | G7: `InteractionResult.shouldSwing()` GONE; `record Success(SwingSource swingSource, ItemContext itemContext)` (InteractionResult.java:33) | **CONFIRMED** | grep: no `shouldSwing` in InteractionResult.java; record Success at :33. |
| 27 | G8: `ListTag.getElementType()` GONE | **CONFIRMED** | `grep -rn getElementType` → 0 hits in all of mc262-ref. |
| 28 | G9: 26.2 `AbstractClientPlayer` has no level field (fields = `playerInfo`, `showExtraEars`, `clientAvatarState`); only storage is `Entity.level` (:215) | **CONFIRMED** | AbstractClientPlayer.java:23-25 — exactly those three fields; Entity.java:215 `private Level level;`. |
| 29 | G12: no `renderHand`/`isRenderingPanorama` booleans; `renderItemInHand` gated inline on `cameraState.isPanoramicMode`/first-person/sleeping/HUD-hidden/spectator (GameRenderer.java:336-341); `Panorama` object writes `guiRenderState.panoramaRenderState` | **CONFIRMED** | GameRenderer.java:336-341 gate verbatim; `protected final Panorama panorama` field; Panorama.java writes `panoramaRenderState`. |

## 4. 26.2 CHANGED / SAME verdicts (signatures re-opened)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 30 | Global renames: `ResourceLocation`→`Identifier`; `ChunkPos.asLong/toLong`→`pack()` (instance/static/BlockPos); `getMinBuildHeight/getMaxBuildHeight`→`getMinY()/getMaxY()` with **`getMaxY()` INCLUSIVE** (`getMinY()+getHeight()-1`); `Util` → `net.minecraft.util.Util` (`backgroundExecutor()` → `TracingExecutor`) | **CONFIRMED** | `net/minecraft/resources/Identifier.java` exists, no ResourceLocation.java; ChunkPos.java:70-84 (`pack()` ×3); LevelHeightAccessor.java:9-13 (`getMaxY` default = minY+height−1 — inclusive semantic trap real); no `net/minecraft/Util.java`, net/minecraft/util/Util.java `backgroundExecutor()` returning `TracingExecutor` at ~:252. |
| 31 | C1/C2: `pick` moved to `Minecraft.pick(float)` (private, :2934, body `raycastHitResult` + `crosshairPickEntity`); `pickBlock` → `pickBlockOrEntity` (:2353); `startAttack` :1614 / `continueAttack` :1589 / `startUseItem` :1688 / `handleKeybinds` :1865 keep names | **CONFIRMED** | Minecraft.java: `private void pick(float partialTicks)` :2934 with `this.player.raycastHitResult(...)`; `pickBlockOrEntity` :2353; all four wrap-set methods at exactly the cited lines. |
| 32 | C3/C4: screen ownership moved to `Gui` — `Gui.screen()` :218, `Gui.setScreen` :222, `Gui.addInitialScreens` private :381 (called :364) | **CONFIRMED** | Gui.java:218/:222/:364/:381 all match. |
| 33 | C5/S5: `private static int fps` (~:350), write `fps = this.frames;` in runTick :1355; `Minecraft.level`/`hitResult` public `@Nullable`; `smartCull` public `= true`; `levelRenderer` now `public final` (+ `levelExtractor`) | **CONFIRMED** | Minecraft.java field block :278-356 (`public final LevelExtractor levelExtractor`, `public final LevelRenderer levelRenderer`, `public @Nullable ClientLevel level`, `public @Nullable HitResult hitResult`, `private static int fps`, `public boolean smartCull = true`); write at :1355. |
| 34 | C6: `Camera.getPosition()` → `position()` (:359); `eyeHeight`/`eyeHeightOld` unchanged (:62-63); `setPosition` protected | **CONFIRMED** | Camera.java:62-63, :349/:353 (protected setPosition ×2), :359. |
| 35 | C7: `mainRenderTarget` + `RenderBuffers` moved to GameRenderer (private final, :103-104) | **CONFIRMED** | GameRenderer.java:103 `private final RenderBuffers renderBuffers`, :104 `private final RenderTarget mainRenderTarget`. |
| 36 | C8: `TextureTarget(@Nullable String label, int width, int height, boolean useDepth, GpuFormat format)` (:11) | **CONFIRMED** | TextureTarget.java:11 exact signature. |
| 37 | C13: tickets moved to `TicketStorage` (SavedData, :33); `getTickets(long)` → `List<Ticket>` (:130); `DistanceManager.ticketStorage` (:40); `Ticket` no longer generic (:10), `getType()` → `TicketType` (:48); `inEntityTickingRange` survives (:135) | **CONFIRMED** | All six citations re-opened and exact. |
| 38 | C14: `ActiveProfiler.WARNING_TIME_NANOS` now `private static final long` = `Duration.ofMillis(100).toNanos()` (:27), consumed :120 behind `suppressWarnings` | **CONFIRMED** | ActiveProfiler.java:27, :120 exact. |
| 39 | C15/C16/C17: `lookupOrThrow` (RegistryAccess.java:21, `registryOrThrow` gone); `CompoundTag.keySet()` :193, one-arg `contains` :275, Optional `getString` :331; `ClientLevel` `Map<MapId, MapItemSavedData>` :160 + `private final TickRateManager` :155 | **CONFIRMED** | All re-opened; ClientLevel also has protected `getAllMapData` :992 / `addMapData` :996 (bonus confirmation of the duck's re-typing need). |
| 40 | C19/C20/C21: `new UseOnContext(player, hand, blockHit)` still constructed in `performUseItemOn` (:370); protected 5-arg ctor survives (:24); `startPrediction(ClientLevel, PredictiveAction)` (:297) with callers passing `this.minecraft.level` (:167/:188); in-lambda `player.level()` reads at :194/:256; `continueDestroyBlock` :229; sends: startPrediction :301, startDestroyBlock ~:177-178, stopDestroyBlock ~:221-222; `send` declared `ClientCommonPacketListenerImpl.send(Packet<?>)` :349 | **CONFIRMED** | MultiPlayerGameMode.java + UseOnContext.java + ClientCommonPacketListenerImpl.java all re-opened; every line matches (send-site cites off by ≤1 line due to decompiler line-wrapping of `this.connection\n.send(...)` — within noise). |
| 41 | C22: `ClientboundPlayerPositionPacket` now `record (int id, PositionMoveRotation change, Set<Relative> relatives)` (:12); `ServerboundMovePlayerPacket` still abstract class (:9) | **CONFIRMED** | Both files re-opened, exact. |
| 42 | C24: `handlePlayerAction` :1273 / `handleUseItemOn` :1343; `case STAB` (:1280, doc said :1281 — ±1 noise); reach = `isWithinBlockInteractionRange(pos, 1.0)` :1354; break-forward uses `getMaxY()` :1323; `ackBlockChangesUpTo` deferred-flush field :242/:294-296 | **CONFIRMED** | ServerGamePacketListenerImpl.java re-opened; all present. Vanilla now also sends `Action.STAB` from `MultiPlayerGameMode` :523 — the api-map's S15 note ("`isAttackingAction` whitelist still valid") holds since STAB is not in IP's attack set. |
| 43 | C27/S22: `ChunkMap.TrackedEntity` survives with fields `serverEntity/entity/range/lastSectionPos/seenBy` (:1320-1325), now `implements ServerEntity.Synchronizer`, broadcast `sendToTrackingPlayers(Packet<? super ClientGamePacketListener>)` (:1345); ChunkMap `entityMap` :146, `getUpdatingChunkIfPresent` :251, `getPlayerViewDistance` :817 (private), `size()` :841 | **CONFIRMED** | ChunkMap.java:1320-1348 and member lines exact. Note (non-refuting): `TrackedEntity` is declared `private class` in 26.2 — mixin targeting still works, but any direct-reference code must go through the mixin/duck. |
| 44 | S4/S13/S29/S44/S48/S50 spot checks: `ParticleEngine.setLevel` public :131 (field :31); `SGPLI.teleport(x,y,z,yaw,pitch)` :1257 + `PositionMoveRotation` overload :1261, `awaitingPositionFromClient` :259; `Entity.setPosRaw` :3786 with `levelCallback.onMove()` :3800, `setRemoved` public final :3912, `protected unsetRemoved()` :3926; `DimensionArgument.dimension` :47 / `getDimension` :51; `SnbtPrinterTagVisitor.java` exists; `inEntityTickingRange` :135 | **CONFIRMED** | All re-opened at the cited lines; the slice's two hardest anchors (`setPosRaw`@`onMove`, `setRemoved` RETURN) survive verbatim in 26.2 as S29 claims. |

## 5. REFUTED

| # | Claim | Verdict | Correction |
|---|---|---|---|
| R1 | **api-map G10**: "`Minecraft.useShaderTransparency()` (referenced by ClientDebugCommand render-mode toggles' surroundings) … Migration note: *Read it* through `GameRenderer.gameRenderState()`" | **REFUTED (attribution + migration note; the GONE verdict itself is correct)** | Grep of the entire IP tree: `ClientDebugCommand` never references `useShaderTransparency`. The two real IP usages are (a) a static read in `render/renderer/RendererUsingStencil.java:91` (render slice), and (b) — in this slice's own cited mixin file — `MixinMinecraft.java:178-184`, a **HEAD-cancellable `@Inject` INTO `Minecraft.useShaderTransparency()` that forces `false` while a portal view is rendering** ("avoid messing up rendering states in fabulous"). So the port item is not "read the flag through the new getter" — it is *re-anchor an override/suppression of the flag* onto its 26.2 home (`gameRenderer.gameRenderState().useShaderTransparency()`, read at LevelRenderer.java:835). As written, G10 would cause the fabulous-graphics suppression mixin to be dropped from the port checklist. |

## 6. Non-refuting notes

- Line-cite noise of ±1-2 lines in a handful of api-map cites (STAB :1280 vs :1281; startDestroyBlock send :177-178 vs :179) — decompiler line-wrapping; the claims themselves are correct.
- Inventory §2.6 says the fps inject fires "exactly once per second". The `@At` is an unqualified FIELD match on `fps:I` in `runTick` (no PUTFIELD opcode restriction, MixinMinecraft.java:144-151); in 26.2 `runTick` the only in-method access found is the write at :1355, so the behavior claim holds — but the port should keep the anchor tight (opcode PUTSTATIC now, since `fps` became static — the api-map's C5 note already flags the static change).
- 26.2 `TrackedEntity` is `private class` (ChunkMap.java:1320) — additive detail for the C27 duck re-map, not a contradiction.
