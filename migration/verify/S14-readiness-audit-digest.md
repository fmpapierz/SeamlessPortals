## VERIFY result (verdict=READY)
notes: Independent re-trace completed end-to-end for the S14 static-nether scenario; the auditor's READY stands. Chain verified: (1) SERVER ENCODE — DimIntIdMap/DimensionIntId/DimensionIdRecord are 1:1 with IP (full diff run; only documented 26.2 renames), server map built at MinecraftServer.createLevels RETURN (MixinMinecraftServer_Misc.java:51-54) with nether=-1 before any join; DimIdSyncPacket adds the R1 dimSeaLevelTag with symmetric encode/decode. (2) SEND TIMING — login send verified against mc262-ref PlayerList.java:142-222: inject anchor ClientboundChangeDifficultyPacket.<init>(Difficulty,Z)V matches :185 exactly (record ctor confirmed), after the login packet (:169, which delivers connection.levels() used by getServerDimensions), before difficulty/sendLevelInfo(:215, where MixinPlayerList's global-portal login sync fires)/addNewPlayer entity tracking — so DimIdSync precedes every portal-sync, global-sync, and redirected packet on the ordered stream; suspendFlushing/resumeFlushing preserves order. Config phase carries only the version handshake (ImmPtlNetworkConfig diff vs IP: only documented v6 renames + interface-injection casts). Dynamic-dim send leg is ledgered dead code (never-firing DimLib stub). (3) CLIENT DECODE/STORE — registration verified both dists flag-ON (MiscNetworking.init at SeamlessPortalsModFabric:91 via PayloadTypeRegistry.clientboundPlay; receiver at SeamlessPortalsClientFabric:59 via ClientPlayNetworking.registerGlobalReceiver); handle() runs synchronously inside the main-pass packet drain (SPIKE-R7 §4 empirical: fabric v6 receivers same-stack in the FIFO PacketProcessor drain — no separate queue), storing clientRecord + dimIdToDimTypeId + dimIdToDimSeaLevel wholesale; all three cleared on exit via the 2-arg updateLevelInEngines funnel. (4) CONSUMPTION — PortalSyncPacket/GlobalPortalSyncPacket handle() -> clientIntToDimKey (main thread, record present); redirected 'i:r' packets ride the R7 requeue (MixinClientboundCustomPayloadPacket netty-pass scheduleIfPossible into the SAME FIFO; main-pass inline) -> fromIntegerId -> withSwitchedWorldFailSoft -> getOptionalWorld -> getWorld CREATES the nether ClientLevel on demand (identical to IP — a redirected nether packet arriving before the portal sync still constructs the level with the synced sea level, no drop); TeleportPacket C2S built with clientDimKeyToInt on the main thread (ClientTeleportationManager.java:373-379) and resolved server-side via the always-initialized server map. Weave audit: defaultRequire=1 in all mixin configs (anchor mismatch = fatal at boot, and S13 rung-1 launches passed with these exact mixins, which also live-proved the login send/receive/consume loop with int-id 0); no second mixin targets ClientboundCustomPayloadPacket.handle or placeNewPlayer's difficulty anchor (collision scan clean); the SeamlessMixinConfigPlugin gates qouteall.* mixins and the flag is a single read-once source (EntityPortalsFlag) shared by the plugin and all runtime gates. Mixed flag-states across client/server degrade to IP's own missing-immptl config-handshake path (canSend=false -> warn/disconnect), not a crash. No BLOCKER/MAJOR/MINOR found; the three auditor NOTEs are accurate (one framing correction folded into finding 1) and one additional NOTE-severity latent item added for the C1/S19 ledger.

#### [NOTE] CONFIRMED Dynamic-dim ordering race makes fromIntegerId hard-throw (unreachable for S14; on the port the dynamic-dim resync leg is DEAD CODE, not merely racy)
- file: common/src/main/java/qouteall/q_misc_util/dimension/DimIntIdMap.java:43
- evidence: Auditor's mechanics verified line-exact: DimIntIdMap.fromIntegerId throws RuntimeException("Missing Dimension "+id) (DimIntIdMap.java:43-51, byte-identical to IP modulo location()->identifier()); DimensionIntId.getClientMap() Validate.notNull (DimensionIntId.java:60-65, 1:1 IP); PacketRedirectionClient.handleRedirectedPacket resolves via getClientMap().fromIntegerId on the client main thread (PacketRedirectionClient.java:51-53, byte-identical to IP). CORRECTION to the auditor's framing: on the port, onServerDimensionChanged (DimensionIntId.java:103-128) can NEVER run for ANY dim — it is registered on DimensionAPI.SERVER_DIMENSION_DYNAMIC_UPDATE_EVENT whose register()/addPhaseOrdering() are documented no-ops (common/src/main/java/qouteall/dimlib/api/DimensionAPI.java:44-52, the S04 §5 shipped never-firing DimLib stub; dynamic dims are C1/S19-gated). So there is no "refreshed DimIdSyncPacket lands late" race on the port — for a hypothetical dynamic dim there would be NO refresh at all. Unreachable today: no dynamic-dim provider exists, and the nether is static (int-id -1, filled at DimensionIntId.fillInVanillaDimIds:95-97, map built at createLevels RETURN via MixinMinecraftServer_Misc.java:51-54 before any player joins). S14 ordering is safe: the login DimIdSyncPacket is sent inside placeNewPlayer at the ClientboundChangeDifficultyPacket ctor (verified against mc262-ref PlayerList.java:185 — ctor signature (Difficulty,Z)V matches the @At target exactly, sole match in placeNewPlayer; login packet at :169 precedes, sendLevelInfo/global-portal sync at :215 and entity tracking follow), and SPIKE-R7 empirically proved Fabric v6 play receivers run synchronously inside the main-pass packet drain, same FIFO as the R7-requeued 'i:r' packets (SPIKE-R7-requeue.md §1 REQUEUE zero inversions, §4 bullet 1).
- IP ref: IP DimIntIdMap.java fromIntegerId throws identically; IP DimensionIntId.java:103-128 identical; the never-firing DimensionAPI stub is the ledgered approved deviation (DimensionAPI.java header, S10A-loader-seam.md §5, EXECUTION_PLAN S13 step 4); SPIKE-R1-sealevel.md §3/§5.
- fix: No action for S14. Pin to the C1/S19 DimLib work item: when a real dynamic-dimension provider lands, the whole DimensionIntId dynamic leg (event wiring, resync send, and a fail-soft fromIntegerIdNullable on the redirect consumer) must be revisited as one unit.

#### [NOTE] CONFIRMED Asymmetric absence handling: missing dim-type throws, missing sea-level fails soft (by design, cannot diverge)
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:556
- evidence: Verified: createSecondaryClientWorld throws IllegalStateException on a missing dim-type entry (ClientWorldLoader.java:551-561) and fails soft to CLIENT.level.getSeaLevel() with a rate-limited warn on a missing sea-level entry (ClientWorldLoader.java:586-598). Both compounds are filled in the SAME server.getAllLevels() loop (MiscNetworking.java:59-89) and travel in one packet with symmetric 3x writeNbt/3x readNbt (MiscNetworking.java:100-112), so dim-type presence implies sea-level presence. Also verified the maps are cleared in lockstep on client exit (ClientWorldLoader.java:128-132) via the CLIENT_EXIT_EVENT fired from the 2-arg updateLevelInEngines funnel (MixinMinecraft.java:188-207, the retarget that covers disconnect/kick), and clientRecord likewise (DimensionIntId.onClientExit).
- IP ref: SPIKE-R1-sealevel.md §3 'Fallback' (fail-soft, never throw); IP ClientWorldLoader.java:~420 getWorld/dim-type hard dependency; IP MixinMinecraft onSetWorld same isInitialized gate.
- fix: None — intentional and unreachable-by-construction; leave as-is.

#### [NOTE] CONFIRMED Sea-level protocol is the approved additive R1 extension, not an IP divergence (fidelity confirmation)
- file: common/src/main/java/qouteall/q_misc_util/MiscNetworking.java:85
- evidence: Verified against IP MiscNetworking.java:36-148: the port adds exactly one compound (dimSeaLevelTag, filled at MiscNetworking.java:85-88 with world.getSeaLevel(), the same server-side source vanilla's ServerPlayer.createCommonSpawnInfo passes — mc262-ref ServerPlayer chain per SPIKE-R1 §1.2), appended as the third writeNbt/readNbt pair; everything else in the packet is 1:1 with IP modulo documented 26.2 drift (lookupOrThrow, Identifier, getStringOr/getIntOr/keySet, ClientboundCustomPayloadPacket ctor replacing ServerPlayNetworking.createS2CPacket, PlatformHelper seams). Consumption verified: createSecondaryClientWorld passes dimIdToDimSeaLevel.get(dimension) as the 26.2 ClientLevel ctor's trailing final seaLevel (ClientWorldLoader.java:586-611); for the nether the synced value is its generator sea level (spike ground truth 32) and is bit-identical to what the vanilla respawn spawn-info would carry on crossing, so promotion of the cached secondary to mc.level cannot disagree with vanilla.
- IP ref: SPIKE-R1-sealevel.md §3 protocol design v1 + review-verdict PASS; API_RISKS R1 approved deviation.
- fix: None — verified faithful re-expression.

#### [NOTE] MISSED DimIntIdMap.removeUnused mutates the map while iterating its keySet (IP-verbatim latent bug, dead code until DimLib lands)
- file: common/src/main/java/qouteall/q_misc_util/dimension/DimIntIdMap.java:95
- evidence: removeUnused iterates toIntegerId.keySet() and calls remove(dimId) (which does toIntegerId.removeInt) inside the loop (DimIntIdMap.java:93-102) — structural modification of a fastutil Object2IntOpenHashMap during keySet iteration is undefined (skipped/repeated entries or ConcurrentModificationException). This is byte-identical to IP's own DimIntIdMap.removeUnused (fidelity says keep it), and on the port it is unreachable: its only caller is DimensionIntId.onServerDimensionChanged (DimensionIntId.java:119), which is registered solely on the never-firing DimensionAPI stub event. Zero S14 impact; becomes live the moment a real dynamic-dimension provider (C1/S19) makes the event fire with a removed dim.
- IP ref: IP DimIntIdMap.java removeUnused — identical loop; IP relies on the same pattern in production (rarely bites because removal only happens on dynamic dim unload).
- fix: No change now (IP-verbatim, dead code). Record on the C1/S19 DimLib ledger: when the dynamic leg is activated, snapshot the key set before removing (e.g. iterate a copied list), matching whatever upstream IP does at that time.

## VERIFY result (verdict=DEFECTS_FOUND)
notes: Adversarial re-trace performed independently end-to-end: client manageTeleportation (MinecraftFramePumpMixin flag-ON branch) -> tryTeleport -> teleportPlayer -> changePlayerDimension -> C2S TeleportPacket -> ImmPtlNetworking.TeleportPacket.handle -> ServerTeleportationManager.onPlayerTeleportedInClient -> validate -> teleportPlayer -> server changePlayerDimension, plus /portal make_portal. VERIFIED-CLEAN (no defect): server changePlayerDimension is line-for-line IP 1.21.3 (removePlayerImmediately + ip_unsetRemoved + setEyePos + setServerLevel + addDuringTeleport + vehicle re-mount + O_O.onPlayerTravelOnServer + portal_worldChanged; NO respawn packet — the client HandleRespawn path cannot fire on an IP crossing); MixinChunkMap_E untracking gate present and keyed on teleportingEntities; TeleportPacket registered both sides (ImmPtlNetworking.init:238-253 serverbound payload + handler; direct ServerboundCustomPayloadPacket construction is equivalent to IP's ClientPlayNetworking.createC2SPacket); fromDimension captured before the swap (no identity bug); rotation/velocity continuity IP-verbatim (managePlayerRotationAndChangeGravity bracketed by world-velocity save/restore, no vanilla teleport packet on the normal path); cooldown/dedup (teleportTickTimeLimit client, lastTeleportGameTime + ip_hasAwaitingTeleport server) IP-faithful; all ducks implemented and correctly anchored (MixinClientPacketListener.ip_setWorld:78-80, MixinEntity.ip_setWorld:407-409, MixinAbstractClientPlayer routes ip_setClientLevel through Entity.level — the declared-member @Shadow trap was explicitly dodged, MixinGameRenderer implements IEGameRenderer with @Mutable lightmap + ip_setLightmapTextureManager:243). The auditor's suspected miss candidate `getBlockEntityRenderDispatcher().setLevel` (IP :499, absent in port) was checked and is a VALID re-expression: 26.2 BlockEntityRenderDispatcher has no level field/setLevel (mc262-ref BlockEntityRenderDispatcher.java:31-68; level context flows per-extract), documented at MyGameRenderer.java:95 — deliberately not filed as a defect. startRiding(vehicle,true,false) third arg = sendEventAndTriggers (Entity.java:2414); false on the client path is defensible; vehicle crossings are S15 scope. Net: the auditor's BLOCKER and MINOR both stand exactly as filed (one citation corrected: ClientWorldLoader.getWorldExtractor is :375-380); one MAJOR was missed — the gameRenderer.setLevel tail (camera level + cardinal lighting), which should be fixed in the same patch as the extractor re-point. S14 must not launch before the BLOCKER + MAJOR fix.

#### [BLOCKER] CONFIRMED Client changePlayerDimension never re-points mc.levelExtractor on 26.2 — main view keeps extracting the SOURCE dimension after a cross-dim crossing (first-exercised at S14)
- file: common/src/main/java/qouteall/imm_ptl/core/teleportation/ClientTeleportationManager.java:500
- evidence: Independently re-traced and CONFIRMED at every joint. (1) 26.2 vanilla: GameRenderer.extract:389 drives `this.minecraft.levelExtractor.extract(...)`; LevelExtractor (mc262-ref .../renderer/extract/LevelExtractor.java) holds `private final LevelRenderer levelRenderer` (:74, FINAL, bound once at Minecraft.java:649 to the ORIGINAL renderer) and `private @Nullable ClientLevel level` (:75, set only via setLevel, called from Minecraft.updateLevelInEngines:2202); extract() reads ONLY these own fields (:106,117,120,123,136-137,152). (2) The port: changePlayerDimension does `client.level = toWorld` (direct write, bypasses updateLevelInEngines) + ip_setWorldRenderer (MixinMinecraft.java:222-224, sets ONLY the @Mutable Minecraft.levelRenderer). Nothing touches mc.levelExtractor: repo-wide grep shows NO mixin redirecting `minecraft.levelExtractor` in GameRenderer.extract, and O_O.onPlayerChangeDimensionClient (O_O.java:44-48) is RequiemCompat only. (3) Both rescue paths are dead flag-ON: MinecraftFramePumpMixin.java:60-87 runs the IP chain and `return`s before SeamlessClientTeleport.checkCameraCrossingPerFrame (:98), and the server path (ServerTeleportationManager.changePlayerDimension:423-487) sends NO respawn packet (removePlayerImmediately/setServerLevel/addDuringTeleport only), so HandleRespawnMixin->promoteToMain never fires. (4) Same-dim rung 1 never enters this method (`fromDimension != toDimension` guard, ClientTeleportationManager.java:351) — genuinely first-live at S14. (5) The mod's own PortalWorldManager.promoteToMain:1145-1152 documents this exact failure with a proven log DIAG (oldRenderer=1225 visibleSections, promoted=0) and shows the working fix pattern (:1163-1197: direct field re-point via LevelExtractorAccessor + lastViewDistance sync to dodge the allChanged mesh wipe + ClientLevel.levelExtractor identity re-point). Post-crossing: extract fills the shared LevelRenderState from the OW level/renderer while mc.levelRenderer is the nether secondary — stale/blank main terrain, and dest block edits never remesh (the nether-block-freeze class). One auditor citation corrected: ClientWorldLoader.getWorldExtractor's active-dim-returns-CLIENT.levelExtractor assumption is at ClientWorldLoader.java:375-380 (not :260).
- IP ref: IP 1.21.3 ClientTeleportationManager.changePlayerDimension:489-492 (verified in IP source): `client.level = toWorld` + ip_setWorldRenderer was the COMPLETE render cutover because the 1.21.3 LevelRenderer was itself the extractor. 26.2 split extraction into the single global final mc.levelExtractor (Minecraft.java:280,649; GameRenderer.extract:389); IP's semantic — cut the main render pipeline to the dest dim without wiping meshes — now requires re-pointing that extractor's fields.
- fix: In client changePlayerDimension, after ip_setWorldRenderer, replicate promoteToMain's extractor re-point using ClientWorldLoader's maps (NOT PortalWorldManager's — its renderers/levels/extractors maps are empty flag-ON): via LevelExtractorAccessor set mc.levelExtractor.levelRenderer = ClientWorldLoader.getWorldRenderer(toDimension), .level = toWorld, .sectionUpdateTracker = the tracker of WORLD_EXTRACTOR_MAP.get(toDimension) (the dest level's construction-bound extractor), .lastViewDistance = options.getEffectiveRenderDistance() (prevents the first-extract allChanged mesh wipe); via ClientLevelExtractorAccessor set toWorld's own levelExtractor field to mc.levelExtractor ITSELF; and demote the outgoing dim: set fromWorld's levelExtractor field back to WORLD_EXTRACTOR_MAP.get(fromDimension) so portal-view dirties of the now-secondary source dim land on its own renderer. Keep WORLD_EXTRACTOR_MAP coherent with getWorldExtractor's active-dim special case. Do NOT call LevelExtractor.setLevel (allChanged -> invalidateCompiledGeometry wipes the cached meshes the seamless swap exists to preserve). Best housed as a ClientWorldLoader helper called from changePlayerDimension.

#### [MINOR] CONFIRMED particleEngine.setLevel() clears all particles on cross-dim crossing — diverges from IP's ip_setWorld; the adjacent comment ('avoid clearing all particles') is factually inverted
- file: common/src/main/java/qouteall/imm_ptl/core/teleportation/ClientTeleportationManager.java:506
- evidence: CONFIRMED. 26.2 ParticleEngine.setLevel (mc262-ref ParticleEngine.java:131-135) = `this.level = level; this.clearParticles(); this.trackingEmitters.clear();` with clearParticles (:157-162) wiping particles+particlesToAdd+trackedParticleCounts. IP 1.21.3 (ClientTeleportationManager.java:494-497, verified) uses ((IEParticleManager) particleEngine).ip_setWorld(toWorld) under the same comment, precisely to avoid the clear. The port ALREADY ships the duck: MixinParticleEngine.ip_setWorld (:80-83) writes only the level field, and ClientWorldLoader.withSwitchedWorld:754 uses it — only changePlayerDimension inconsistently calls the clearing vanilla setLevel. Every crossing pops all particles. The block-era paths deliberately call setLevel (SeamlessClientTeleport.java:590 'match HandleRespawnMixin') but the IP path must match IP.
- IP ref: IP 1.21.3 ClientTeleportationManager.changePlayerDimension:494-497: ((IEParticleManager) client.particleEngine).ip_setWorld(toWorld) — 'avoid clearing all particles'.
- fix: Replace `client.particleEngine.setLevel(toWorld)` with `((IEParticleManager) client.particleEngine).ip_setWorld(toWorld)` (keep the null guard), matching IP verbatim and the port's own withSwitchedWorld. Correct the S08-teleportation.md port-note claim.

#### [NOTE] CONFIRMED make_portal (absolute) is one-way with no dest-side/reverse portal — matches IP; dim resolution and origin-level spawn correct
- file: common/src/main/java/qouteall/imm_ptl/core/commands/PortalCommand.java:2315
- evidence: CONFIRMED. placePortalAbsolute (:2315-2334): DimensionArgument 'to' -> setDestDim(nether), Vec3Argument 'dest' -> setDestination(0,70,0); PortalManipulation.placePortal (:343-397) ray-traces the player's look (100 blocks), returns null on miss (:362-364 — command silently returns 0), builds the Portal in `entity.level()` when no portal was ray-hit (:385-389), i.e. the player's current OW ServerLevel; McHelper.spawnServerEntity spawns into that same level. No PortalAPI.createReversePortal call — intentionally one-way, matching IP's LoganDark command. The mission's 'dest-side portal into the right ServerLevel' concern does not apply to this command.
- IP ref: IP 1.21.3 PortalCommand.placePortalAbsolute + PortalManipulation.placePortal — same structure, no reverse-portal creation.
- fix: No change. S14 tester notes: aim at a block when running the command (miss = silent no-op), and the portal is one-way — return via /portal goback or a second portal.

#### [NOTE] CONFIRMED RemoteCallables.updateEntityPos regular-entity re-expression (26.2 InterpolationHandler rewrite) needs an S15 runtime check
- file: common/src/main/java/qouteall/imm_ptl/core/teleportation/ClientTeleportationManager.java:705
- evidence: CONFIRMED as an observation. Port (:705-726): getPositionCodec().setBase(pos) + snapTo(pos, yRot, xRot) + getInterpolation().cancel() vs IP (:697-719): setPos + lerpTo(...,0) + setPos. This is the non-player cross-dim confirm path (fires at S14 only if a mob/minecart crosses; the S14 player test does not exercise it). Symbols are valid 26.2; whether snapTo+cancel fully reproduces IP's interpolation suppression is a runtime question.
- IP ref: IP 1.21.3 ClientTeleportationManager.RemoteCallables.updateEntityPos:697-719.
- fix: No S14 action; verify at S15 with entity traffic that a crossing mob/minecart lands exactly at the server position with no interpolation slide.

#### [MAJOR] MISSED Client changePlayerDimension never replays mc.gameRenderer.setLevel(toWorld) — the main Camera keeps the SOURCE ClientLevel and entity cardinal lighting stays on the old dimension after every cross-dim crossing
- file: common/src/main/java/qouteall/imm_ptl/core/teleportation/ClientTeleportationManager.java:502
- evidence: 26.2 GameRenderer.setLevel (mc262-ref GameRenderer.java:705-711) does two things vanilla's updateLevelInEngines (Minecraft.java:2204) relies on at a dim change: `this.lighting.updateLevel(level.dimensionType().cardinalLightType())` and `this.mainCamera.setLevel(level)`. Camera.setLevel (Camera.java:491-492) is the ONLY writer of Camera.level besides reset(); the per-frame mainCamera.update (GameRenderer.java:375) READS the cached field. Camera.level feeds: attributeProbe.tick(this.level, position) (Camera.java:84-87) — the environment-attribute source LevelExtractor consumes for cloud/fog/sky state (LevelExtractor.java:199-203); the submersion/fluid checks (Camera.java:442-458) — lava/water fog overlay; the third-person camera clip (Camera.java:306); and the spectator solid-render check (:123). After an OW->nether IP crossing the main camera permanently keeps the OVERWORLD ClientLevel (unloaded at nether coords): wrong fog/sky/cloud attributes, no lava-submersion fog, third-person camera clipping against nothing, and entity shading stuck on the overworld cardinal light type. This is pure 26.2 API drift: in 1.21.3 Camera.setup received minecraft.level per-frame so IP's swap set was complete — the port's own comment trail knows this (CrossPortalViewRendering.java:58-73 re-expresses setup->setLevel+setEntity+update for a FRESH camera, and MyGameRenderer.java:415-424 handles cardinalLightType for portal views) but the MAIN-camera crossing path was never given the tail. Both block-era swap paths replay it explicitly: SeamlessClientTeleport.java:589-591 ('Replay minimal mc.setLevel side-effects (match HandleRespawnMixin)': particleEngine.setLevel + gameRenderer.setLevel) and HandleRespawnMixin.java:530 (mc.gameRenderer.setLevel(level)). The flag-ON IP path bypasses both (MinecraftFramePumpMixin.java:87 return; no respawn packet), and changePlayerDimension ends after the particle swap — nothing re-points the camera or lighting. The portal-view render path swaps the whole Camera object and restores the ORIGINAL (MyGameRenderer.java:251,277,356), so it never repairs the stale field; CrossPortalViewRendering builds a NEW Camera (feature-gated) and doesn't touch mainCamera either.
- IP ref: IP 1.21.3 needed no such call: GameRenderer.renderLevel passed this.minecraft.level into Camera.setup every frame, so IP's client.level write auto-propagated to the camera; 26.2 caches the level on Camera (setLevel) and selects entity cardinal lighting at dim-change time (GameRenderer.setLevel), so the faithful re-expression of IP's dim cutover must include it.
- fix: In client changePlayerDimension, after the renderer/extractor swap, add `client.gameRenderer.setLevel(toWorld);` — on 26.2 it is side-effect-light (only lighting.updateLevel + mainCamera.setLevel, GameRenderer.java:705-711; no mesh/render-state invalidation), so it cannot wipe the preserved meshes. This lands naturally in the same patch as the BLOCKER extractor re-point. Record it in the port-note as the 26.2 re-expression of IP's implicit per-frame camera-level refresh (1.21.3 Camera.setup(minecraft.level,...) has no 26.2 equivalent).

## VERIFY result (verdict=DEFECTS_FOUND)
notes: Independent end-to-end re-trace performed (auditor report was a non-audit stub). Chain established: (1) IP 1.21.3 semantics — MixinFabricInvalidateRenderStateCallback cancels the Fabric event dispatch while ClientWorldLoader.isCreatingClientWorld (bracket at IP ClientWorldLoader.java:398/:480), protecting other mods' 'renderer reloaded' callbacks from firing mid-secondary-world construction. (2) 26.2 fire site — fabric-api 0.152.1+26.2 resolves fabric-rendering-v1 25.1.6+46a6d00c9c, whose LevelExtractorMixin fires the event at HEAD of LevelExtractor.allChanged on EVERY extractor instance. (3) Current port — the mixin is wholly absent (deferral recorded S13B-flip.md:295, S14_HANDOFF.md:46); one dispatch fires per secondary-world creation (setLevel→allChanged), zero on destruction; the S14 runtime has ZERO listeners (verified by binary scan of every fabric-api module jar at the pom-pinned versions + modmenu + the mod itself), so the dispatch is a no-op — no perf hiccup, no wrong-dim invalidation, no e817b57 extractor-identity exposure. RECOMMENDATION: KEEP DEFERRED for the S14 launch; land the corrected mixin (target lambda$static$1 — the 26.2 jar's void invoker; verbatim lambda$static$0 now names the NON-VOID factory and would crash the weave) at the C2/S18 third-party-compat tail, where listeners can first actually exist. Verdict is DEFECTS_FOUND solely for the MAJOR missed finding: the IP reload-cascade caller (allChanged TAIL → _onWorldRendererReloaded) was never landed and is recorded nowhere as a deviation — the callee sits fully ported but orphaned at ClientWorldLoader.java:684. That defect does not block the S14 test path (no resource reload involved); it manifests as stale portal-view render state after resource-pack/F3+A/graphics reloads. It may nominally belong to a sibling link (world-loader/reload family), but no other record covers it.

#### [MAJOR] MISSED Dead dispatch wiring: ClientWorldLoader._onWorldRendererReloaded has ZERO call sites — the IP reload cascade to secondary dims never fires
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:684
- evidence: Repo-wide grep for _onWorldRendererReloaded finds only the definition (ClientWorldLoader.java:684) and a comment (:643) that calls it 'the retained _onWorldRendererReloaded manual propagation' — no caller. No mixin in the port targets allChanged (grep 'method = \"allChanged\"' across all *.java = zero hits; seamlessportals-ip-client.mixins.json contains no reload mixin; the port's MixinLevelRenderer carries only the R4 ViewArea redirect + IEWorldRenderer duck, and MixinLevelExtractor only the R3 clip-context tagger). The migration's own api-map (migration/api-map/world-loader-root.md:62) explicitly plans: 'The _onWorldRendererReloaded cascade mixin retargets LevelExtractor.allChanged TAIL' — that mixin was never landed, and there is NO deferral record (EXCLUSIVITY_LEDGER.md grep for allChanged/cascade = no matches; port-notes grep for onReloadFinished = no matches; S10B-clientworldloader.md:83 shows the CALLEE body was ported and updated to WORLD_EXTRACTOR_MAP.get(dim).allChanged() at :716, so the callee is live-ready but orphaned). Consequence: when the MAIN extractor reloads (resource-pack change — vanilla calls mc.levelExtractor.allChanged() per Minecraft.java:1047 as cited in ClientDebugCommand.java:406-408; F3+A; graphics-option changes), secondary dims' extractors are never reloaded → stale tint caches and stale compiled meshes/models in portal views. Only render-distance changes self-heal (LevelExtractor.extract self-check, mc262-ref LevelExtractor.java:96-98). The per-secondary registerReloadListener at ClientWorldLoader.java:634 does NOT cover this: LevelExtractor.onResourceManagerReload only sets shouldResetSkyRenderer (LevelExtractor.java:389-391), it does not allChanged. Not a BLOCKER for the S14 test itself (no reload in the test path), but an unrecorded IP divergence.
- IP ref: IP MixinLevelRenderer.java:403-414 (@Inject at LevelRenderer.allChanged TAIL: return if getIsCreatingClientWorld(), Validate mc.levelRenderer == this, then ClientWorldLoader._onWorldRendererReloaded()) + IP ClientWorldLoader.java:503-538; migration/verify/world-loader-root.md row 16 documents both halves (HEAD-cancel while WorldRenderInfo.isRendering + TAIL cascade).
- fix: Land the planned cascade mixin: @Mixin(LevelExtractor.class) @Inject(method="allChanged", at=@At("RETURN")) → if (this == Minecraft.getInstance().levelExtractor) ClientWorldLoader._onWorldRendererReloaded(). The main-extractor identity filter replaces IP's swap-dependent Validate (vanilla binds CLIENT.levelExtractor once at Minecraft.java:649 — the port's own EXTRACTOR-IDENTITY comment at ClientWorldLoader.java:480-484; cascaded secondary allChanged calls then TAIL-fire but fail the filter, which with the existing isReloadingOtherWorldRenderers/isCreatingClientWorld/PortalRendering guards inside _onWorldRendererReloaded (:690-698) preserves IP's exact recursion semantics). Also decide/record the fate of IP's other allChanged anchor (HEAD-cancel while WorldRenderInfo.isRendering, IP MixinLevelRenderer.java:394-400): on 26.2 allChanged is cheap-deferred and S13H-driver-core-design.md:446 already mandates re-reading sectionUpdateTracker per frame, so it is plausibly design-absorbed — but that disposition is currently recorded nowhere; write it into the ledger either way. Register in seamlessportals-ip-client.mixins.json as its own commit so the S14 gate can bisect it.

#### [MINOR] MISSED Land-time trap on this link: verbatim IP MixinFabricInvalidateRenderStateCallback would hard-crash at startup on 26.2 — the compiled lambda index FLIPPED between Fabric API generations
- file: migration/port-notes/S13B-flip.md:295
- evidence: The deferral record says the '@Pseudo Fabric-API-internal lambda$static$0 target [is] unverifiable without the loader classpath'. Now verified against the ACTUAL S14 classpath: javap of fabric-rendering-v1-25.1.6+46a6d00c9c.jar (the module fabric-api 0.152.1+26.2 resolves per its pom) shows lambda$static$0 = 'private static InvalidateRenderStateCallback lambda$static$0(InvalidateRenderStateCallback[])' — the NON-VOID outer factory — while the void invoker IP means to cancel is lambda$static$1. In the 1.21-era jar (fabric-rendering-v1-5.1.0+ab4c25a019) the numbering is reversed (lambda$static$0 = the void invoker — IP's target was valid there); the source is byte-identical in both, so the flip is a pure compiler artifact. IP's handler is (InvalidateRenderStateCallback[] event, CallbackInfo ci) with cancellable=true — legal only on a void target — so a verbatim port would fail mixin application (CallbackInfo-vs-CallbackInfoReturnable descriptor mismatch → InvalidInjectionException) under required:true + defaultRequire:1 (seamlessportals-ip-fabric.mixins.json) = instant startup crash, i.e. a would-be BLOCKER exactly of the S13 'weave anchor' defect class. The current tree is unaffected because the mixin is entirely absent (Glob *InvalidateRenderState* in the port = no files).
- IP ref: IP MixinFabricInvalidateRenderStateCallback.java:14-23 (@Pseudo @Mixin(InvalidateRenderStateCallback.class, remap=false), @Inject method="lambda$static$0" HEAD cancellable, ci.cancel() while ClientWorldLoader.getIsCreatingClientWorld()); registered in IP imm_ptl_fabric.mixins.json:11.
- fix: KEEP DEFERRED through S14 (safe — see the runtime-census NOTE: the event has zero listeners in the S14 runtime, so the missing suppression is unobservable). When landing (recommended at the third-party-compat tail, C2 Sodium/S18, when listeners can actually exist): target method="lambda$static$1", keep @Pseudo + remap=false and the identical guard/cancel body, register under a new "client" array in seamlessportals-ip-fabric.mixins.json, and add a port-note rule that the lambda index is a compiler artifact to re-verify (javap) on every fabric-api bump.

#### [NOTE] MISSED Current-state census (the link's core question, resolved): exactly ONE unsuppressed InvalidateRenderStateCallback dispatch per secondary-world CREATION, none on destruction; no wrong-dim invalidation and no extractor-identity clobber possible in the S14 runtime
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:621
- evidence: Fire path: createSecondaryClientWorld:621 worldExtractor.setLevel(newWorld) → LevelExtractor.setLevel calls allChanged() for non-null level (mc262-ref LevelExtractor.java:393-396) → Fabric's LevelExtractorMixin @Inject(method="allChanged", at=HEAD) fires InvalidateRenderStateCallback.EVENT on EVERY extractor instance. The other creation-path call, worldExtractor.onResourceManagerReload (:626), does NOT reach allChanged (LevelExtractor.java:389-391 sets shouldResetSkyRenderer only). Destruction fires nothing: disposeWorldRenderer:276-279 calls setLevel(null), whose null branch skips allChanged (LevelExtractor.java:397-400). Listener census for the S14 dev runtime (this mod + fabric-api 0.152.1+26.2 + modmenu 20.0.0-beta.4, empty runs/client/mods): binary scan of ALL fabric-api module jars at the pom's pinned versions finds InvalidateRenderStateCallback referenced ONLY by fabric-rendering-v1 itself (the API class + the firing mixin — it registers no listener); modmenu clean; the port registers none (repo grep: only migration docs). Zero listeners → the dispatch is an empty-array loop: no perf hiccup, no invalidation of any dim's render state. Identity cross-check: the event carries no payload and touches no vanilla state; it cannot reach the e817b57 extractor router (which resolves the dest extractor BEFORE the world swap per the commit message) nor the WORLD_EXTRACTOR_MAP/ClientLevel-levelExtractor bindings (nether-block-freeze invariants). The IP divergence (IP would cancel this dispatch) is therefore unobservable until a third-party listener exists.
- IP ref: IP ClientWorldLoader.java:398/:480 (isCreatingClientWorld bracket) + MixinFabricInvalidateRenderStateCallback.java:19-23 (the cancel).
- fix: No S14 action. Record this census in the S14 port-note as the evidence closing the S13-B watch item, and carry the corrected-target landing (lambda$static$1) as a C2/S18 backlog entry.

#### [NOTE] MISSED Adjacent latent hazard from the SAME Fabric mixin: LevelExtractionEvents fire at RETURN of every secondary dest extract with a context that is only ever prepare()d for the MAIN extractor
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:350
- evidence: Fabric's LevelExtractorMixin also injects extract RETURN → LevelExtractionEvents.END_EXTRACTION and extractBlockOutline RETURN → AFTER_BLOCK_OUTLINE_EXTRACTION, passing a per-instance @Unique LevelExtractionContextImpl whose prepare() is invoked ONLY on minecraft.levelExtractor (Fabric GameRendererMixin:54). The port's per-frame cross-dim dest extract (SecondaryWorldRenderCore.java:350 destExtractor.extract(...)) will therefore fire END_EXTRACTION with an unprepared context (all fields null) on the SECONDARY extractor every portal frame at S14. Today this is inert — the same module-jar scan shows zero LevelExtractionEvents/LevelRenderEvents listeners in fabric-api itself and modmenu, and the mod's only LevelRenderEvents use is AFTER_TRANSLUCENT_TERRAIN (SeamlessPortalsClientFabric.java:103/:131, a different event fired elsewhere). But any future third-party listener gets an NPE or a wrong-dim state feed mid-portal-render — the same defect class (transform/state feed across a boundary) as this link's subject, from the very same Fabric mixin.
- IP ref: No IP analog (the LevelExtraction API is new on 26.2); the IP-doctrine analog is the same suppress-during-mod-owned-render-work family as MixinFabricInvalidateRenderStateCallback.
- fix: Park with the C2/S18 compat backlog alongside the InvalidateRenderStateCallback landing: when third-party render mods enter, either suppress the LevelExtraction event dispatch during PortalRendering/secondary extracts (IP-suppression idiom) or prepare the secondary extractor's context before dest extract.

#### REFUTED: test
- refutation: The auditor report is a placeholder stub, not an audit ('Test submission to isolate schema parsing'). Its single finding cites file 'a.java' with evidence 'e' — no such file exists anywhere in the repo (Glob/Grep verified), and no trace of the link was performed. Its READY verdict carries zero evidentiary weight; the link was re-traced from scratch below.

## VERIFY result (verdict=READY)
notes: Independent end-to-end re-trace of the cross-dim chunk-loading link performed against IP 1.21.3 and mc262-ref; verdict READY with NOTE-level items only. Chain verified: registry-phase ticket registration (flags=6 DRAGON shape) → ImmPtlChunkTracking END_SERVER_TICK driver (updateInterval 13, delayUnload 4 gens) → ChunkVisibility loaders (indirectLoadingRadiusCap=8, clamp 1..32 in IPConfig.onConfigChanged:170 — the graduated fade-in is faithful) → markForLoading/flushThrottling (throttle 4, entity-ticking-future gate satisfied by level 31 tickets) → TicketStorage.addTicketWithRadius (exact 1.21.3 33-radius arithmetic, mc262-ref TicketStorage.java:138-141) → generation in the unconditionally-ticking dest dim + the correctly-retargeted MixinServerLevel keep-ticking redirect → vanilla sender fully suppressed (all PlayerChunkSender/ChunkMap/PlayerTicketTracker/DistanceManager anchors verified declared-in-target on mc262-ref; all registered in the mixins JSONs) → IP batch sender with ack wiring → UNCONDITIONAL Payload wrap under withForceRedirect (meaning rung 1 already live-proved the client wrap/unwrap/requeue machinery; S14's genuinely-first-contact pieces are secondary-ClientLevel creation and real dest generation latency) → client R7-requeue → withSwitchedWorldFailSoft swaps mc.level AND listener.level (ip_setWorld) so replaceWithPacketData and the queued light lambda bind to the secondary; the lambda drains inside tickRemoteWorld's withSwitchedWorld (IP-identical), with the block-era light-guard mixins active-by-ledger-decision (S13B-flip §4 'NO gate') and pass-through when captured==current. Walk-away collapse fully wired (purge → redirected forget + removeTicketWithRadius; onDimensionRemove's removeTicketIf-by-type is level-agnostic, strictly more robust than IP's per-level removal; the 26.2 DistanceManager.addPlayer direct PLAYER_SIMULATION ticket remains vanilla-managed exactly as planned in EXECUTION_PLAN F13(iii)). Auditor quality: all five findings confirmed at NOTE; finding 5's evidence was materially incomplete (missed IP's MixinServerLevel empty-dim redirect and its correct port — the strongest fidelity fact on that question) — corrected in the confirmed entry. One missed NOTE added (26.2 debug-subscription chunk streaming inert under the IP tracking takeover). Nothing found that can crash, mis-route, or visibly break the S14 first cross-dim launch on this link.

#### [NOTE] CONFIRMED C8 recheck — 'portal's own chunk not loading until relog' has no plausible cause on the IP-ported path
- file: common/src/main/java/qouteall/imm_ptl/core/chunk_loading/ChunkLoader.java:48
- evidence: Independently re-derived: foreachChunkPos (:48-59) iterates dx,dz in [-radius,+radius] with distanceToSource=max(|dx|,|dz|) — center chunk (distance 0) always included. ChunkVisibility.foreachBaseChunkLoaders (:189-191) accepts playerDirectLoader UNCONDITIONALLY before any portal predicate; the broadcastToPlayer predicate gates only portal discovery. Dest frame chunk covered by getGeneralDirectPortalLoader centered on ChunkPos.containing(portal.getDestPos()) (:135). markForLoading→queue→addTicket covers distance 0 first (per-distance queues drained in ascending order, flushThrottling :220-238). The ChunkVisibility file is byte-identical to IP 1.21.3 except four mechanical ChunkPos.containing translations (full-file diff). The block-era flake was tied to the replaced seamlessportals_chunk_residency mechanism which does not exist flag-ON.
- IP ref: IP 1.21.3 ChunkLoader.foreachChunkPos + ChunkVisibility.foreachBaseChunkLoaders (byte-identical modulo 26.2 renames)
- fix: None. Statically benign on the IP path; confirm live via EXECUTION_PLAN S14(d) item 5 (/portal debug report_chunk_loaders).

#### [NOTE] CONFIRMED R10 FLAG_SIMULATION dest tickets are the correct IP-faithful shape (mob-spawn regression watch only)
- file: fabric/src/main/java/com/warwa/seamlessportals/fabric/SeamlessPortalsModFabric.java:78
- evidence: Verified: registration at :78-79 is UNCONDITIONAL (before the entityPortals branch at :81), flags = FLAG_LOADING|FLAG_SIMULATION = 6 (mc262-ref TicketType.java:13-14; DRAGON=register("dragon",0L,6) at :19 is the exact vanilla precedent), NO_TIMEOUT; TicketTypeInvoker @Invoker("register") matches the private static TicketType.register(String,long,int) at TicketType.java:27. With IPGlobal.activeLoading=true (IPGlobal.java:54, both trees) getLoadingRadius()=2 → ticket level 33-2=31 = ENTITY_TICKING (mc262-ref ChunkLevel.java:53), which is what lets flushThrottling's getEntityTickingChunkFuture gate drain. No LOADING-only leftover on the IP path. Nuance on the auditor's spawn claim: NaturalSpawner still requires a player within spawn range IN the dest dimension, so portal-view alone does not flood; the R10 watch item matters once players cross (S14/S17 as planned).
- IP ref: IP 1.21.3 ImmPtlChunkTickets.TICKET_TYPE = TicketType.create("imm_ptl", ...) used via addRegionTicket (load+entity-tick semantics)
- fix: None — required IP fidelity. Keep the planned S14/S17 liveness-vs-flood observation; do not revert to LOADING-only.

#### [NOTE] CONFIRMED ImmPtlClientChunkMap eviction machinery un-driven flag-ON is IP-faithful (server-forget-driven unloading; C8/F20 gate)
- file: common/src/main/java/qouteall/imm_ptl/core/chunk_loading/ImmPtlClientChunkMap.java:420
- evidence: Confirmed: IP 1.21.3 ImmPtlClientChunkMap.updateViewCenter/updateViewRadius are do-nothing (IP source :213-220) with NO eviction — unloading is server-driven. Port's server half fully wired: ImmPtlChunkTracking.purge (:262-273) sends redirected ClientboundForgetLevelChunkPacket → client handleForgetLevelChunk under withSwitchedWorldFailSoft → secondary store drop() (:122-139, with SOG removal deltas) + ImmPtlChunkTickets.purge removeTicketWithRadius (:279-281). The block-era driver PortalWorldManager.evictUnboundedStores is never registered flag-ON (EXCLUSIVITY_LEDGER row 16: all five END_CLIENT_TICK pumps in the else branch). The port's updateViewCenter storing a center (:271-277) is a documented storage-only R13f carriage that never gates — harmless.
- IP ref: IP 1.21.3 ImmPtlClientChunkMap.updateViewCenter (do-nothing) + server-side ImmPtlChunkTracking forget-packet lifecycle
- fix: None for S14. C8/F20 empirical gate at S14/S17 as documented in the file's PORT-FORWARD note.

#### [NOTE] CONFIRMED flushThrottling's unused mainThreadExecutor local is verbatim-IP dead code with a valid 26.2 accessor
- file: common/src/main/java/qouteall/imm_ptl/core/chunk_loading/ImmPtlChunkTickets.java:193
- evidence: Confirmed: IP 1.21.3 has the identical dead local (IP ImmPtlChunkTickets.java:185). The chunk_sync IEDistanceManager @Accessor("mainThreadExecutor") targets a field declared in DistanceManager (mc262-ref DistanceManager.java:46, private final Executor mainThreadExecutor) — declared-in-target, weave-valid; registered in seamlessportals-ip-core-common.mixins.json:17. Exercised live at S13 rung 1 (flushThrottling runs per tick + per runAllUpdates RETURN).
- IP ref: IP 1.21.3 ImmPtlChunkTickets.flushThrottling:185 (same unused local)
- fix: None — zero-deviation discipline says leave verbatim.

#### [NOTE] CONFIRMED imm_ptl ticket omits FLAG_KEEP_DIMENSION_ACTIVE — harmless, but the operative safeguard is the ported MixinServerLevel keep-ticking retarget (auditor's evidence corrected)
- file: common/src/main/java/qouteall/imm_ptl/core/mixin/common/MixinServerLevel.java:47
- evidence: The auditor's conclusion is right but the cited mechanism is incomplete. Confirmed: MinecraftServer.tickChildren (mc262-ref :1108-1113) ticks getAllLevels() unconditionally, and ServerLevel.tick runs chunkSource.tick (:400 — runDistanceManagerUpdates + generation, ServerChunkCache.java:315-333, no activity gate) BEFORE the hasActiveTickets check (:408), so dest chunk loading/generation never depends on keep-active. What hasActiveTickets actually gates is the emptyTime<300 ENTITY-ticking block (:408-417) — and IP 1.21.3 explicitly patches that gate (MixinServerLevel.redirectIsEmpty on List.isEmpty(): returns false when ImmPtlChunkTracking.shouldLoadDimension(dim)), which the port re-expresses EXACTLY as redirectHasActiveTickets (:47-60) — forcing a watched dest dim active so its entities keep ticking, the precise 1.21.3 semantics. Anchor verified: ServerLevel.tick invokes ServerChunkCache.hasActiveTickets()Z at :408; registered at seamlessportals-ip-core-common.mixins.json:15. The auditor's statement 'IP 1.21.3 had no keep-active concept' overlooks this IP mixin; had the port missed it, dest entities would freeze after 300 ticks — it did not miss it.
- IP ref: IP 1.21.3 mixin/common/MixinServerLevel.redirectIsEmpty (the empty-dimension keep-ticking redirect)
- fix: None — correctly ported. FLAG_KEEP_DIMENSION_ACTIVE stays unnecessary for vanilla dims; the DimLib runtime-unloadable-dim edge remains out of S14 scope as the auditor noted.

#### [NOTE] MISSED 26.2 debug-subscription CHUNK value streaming is structurally inert under the IP chunk-tracking takeover (dev-only tooling gap; no S14 impact)
- file: common/src/main/java/qouteall/imm_ptl/core/chunk_loading/PlayerChunkLoading.java:194
- evidence: 26.2 vanilla PlayerChunkSender.sendChunk gained a tail the IP re-implementation does not replay: level.debugSynchronizers().startTrackingChunk(connection.player, chunk.getPos()) (mc262-ref PlayerChunkSender.java:83) — sendChunkPacket (:194-211) sends the packet + Fabric attachment sync only, even though the sibling suppression MixinChunkMap_C deliberately replays its parallel debug tail (registerChunk, MixinChunkMap_C.java:90). Deeper, the gap is systemic: TrackingDebugSynchronizer.addSubscriber (:57) seeds from player.getChunkTrackingView() and sendToPlayersTrackingChunk (:69) filters by chunkMap.isChunkTracked — both keyed on the vanilla tracking view, which the IP driver leaves permanently EMPTY (applyChunkTrackingView cancelled at HEAD, MixinChunkMap_C.java:61-70). So chunk-scoped debug values (the new 26.2 F3 debug-subscription protocol) never stream to a player under the IP driver even with the tail added; the entity half still works (it rides TrackedEntity.seenBy, which IP's entity_sync populates). IP 1.21.3 has no analog (the debug-synchronizer system is new in 26.x), so this is vanilla-26.2-parity drift on a dev-only subsystem, not an IP divergence. Zero effect in normal play: with no active debug subscriptions every debugSynchronizers call is a no-op over an empty synchronizer set.
- IP ref: No IP 1.21.3 analog — the LevelDebugSynchronizers/TrackingDebugSynchronizer system is new in 26.x; IP's suppression precedent is MixinChunkMap_C's deliberate registerChunk replay
- fix: Record as a known-inert 26.2 periphery in the S14 port-note (alongside the ledger's debug-tail rationale). If dev debug-value streaming is ever wanted, the correct re-source is redirecting ChunkMap.isChunkTracked + the addSubscriber tracking-view seed to ImmPtlChunkTracking watch records and adding the startTrackingChunk call after onSendPacket in sendChunkPacket — S19-periphery material, not S14.

## VERIFY result (verdict=DEFECTS_FOUND)
notes: Independently re-traced the full link: port createSecondaryClientWorld (:495-668) vs IP 1.21.3 ClientWorldLoader (:389-487) vs the real 26.2 ClientLevel ctor (mc262-ref ClientLevel.java:238-263). Auditor's positive claims all verified: 10-arg ctor match incl. trailing seaLevel and LevelExtractor-for-LevelRenderer; ClientLevelData ctor unchanged (Difficulty, hardcore, isFlat — :1149-1161) with isFlat cloned via IEClientLevelData (registered, targets the real private final field); seaLevel protocol end-to-end (server fill from world.getSeaLevel() = vanilla's own spawn-info source, wholesale client cache swap, fail-soft fallback, nulled on exit; sent at placeNewPlayer AND on dim change); dim-type Holder from the login-synced registryAccess with the same packet hard-gating construction; MixinClientLevel <init>@RETURN chunk-map swap descriptor matches the 10-arg ctor exactly and covers every client world (rung-1 weave-proven); all @Mutable shadows (connection/chunkSource/levelExtractor/tickRateManager/mapData) target declared 26.2 fields; ctor side effects audited clean (Level super = field-only; ClientChunkCache/LevelEventHandler/setRespawnData/EnvironmentAttributeSystem/updateSkyBrightness are level-local; LevelRenderer ctor allocates only its own outline target; LevelExtractor ctor pure; setLevel→allChanged reads mainCamera same as IP and defers geometry to first extract); teardown total via the 2-arg updateLevelInEngines funnel; link reachable flag-ON (PortalRenderer:292, ImmPtlNetworking:183/222). Both auditor NOTEs are factually right (second needs scope widened to per-world-join accumulation). VERDICT DOWNGRADED READY→DEFECTS_FOUND on two missed runtime couplings that only a cross-dim secondary can trigger: (1) BLOCKER — 26.2 tickTime feeds the per-connection ClientClockManager, so the IP-verbatim remote tick (secondary gameTime starts at 0) telescopes every render-visible clock to authoritative−(M−S): the overworld's sun/moon/sky jump the moment the nether secondary is created and stay wrong (SPIKE-R1 §4 missed this coupling); fix is a small isClientRemoteTicking-guarded tickTime mixin. (2) MAJOR — lateUpdateLight's IP-verbatim !isDimensionRendered gate assumes 1.21.3's renderLevel-embedded light pair, which 26.2 moved to Minecraft.renderFrame (main level only) and the dest pass never re-expressed: the rendered nether's light publication stalls exactly while the user stares at the portal; fix = gate on world != mc.level (frame-END, swap-safe). Plus one latent identity NOTE (getWorldExtractor short-circuit under world swap — constrains HOW the MAJOR is fixed) and one S18-deferred MINOR (remote tickWeatherEffects). Neither missed defect could fire in rung 1 (no secondaries existed), consistent with the S13 sign-off.

#### [NOTE] CONFIRMED Secondary LevelRenderer shares the main FeatureRenderDispatcher at construction (26.2-only concept; RenderBuffers sharing itself is IP-faithful)
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:523
- evidence: Independently verified: the 26.2 LevelRenderer ctor (mc262-ref LevelRenderer.java:142-153) binds this.renderBuffers = gameRenderer.renderBuffers() and this.featureRenderDispatcher = gameRenderer.featureRenderDispatcher() (:145-146), and also seeds levelRenderState from gameRenderer.gameRenderState().levelRenderState (:151) — which the port then swaps to an isolated instance via LevelRendererAccessorMixin.seamlessportals$setLevelRenderState (@Mutable @Accessor on the real levelRenderState field; registered in seamlessportals-common.mixins.json:27, weave-proven since rung 1). Only the FeatureRenderDispatcher (and RenderBuffers, per-frame pool-swapped elsewhere) remain shared; RenderBuffers sharing matches IP 1.21.3 (IP ClientWorldLoader.java:404-409 passed CLIENT.renderBuffers()).
- IP ref: IP 1.21.3 ClientWorldLoader.java:404-409 (shared RenderBuffers is IP behavior); FeatureRenderDispatcher has no 1.21.3 analog. Contrast: PortalWorldManager.createRenderer:578-619.
- fix: No change in this link (construction is IP-faithful). Hand to the render-slice audit: confirm the dest pass either never drives the secondary renderer's featureRenderDispatcher or handles its mutable per-frame state; if driven, port the PortalWorldManager.createRenderer isolation (per-dim FeatureRenderDispatcher via accessor).

#### [NOTE] CONFIRMED Reload listeners (extractor + cloudRenderer) registered per secondary with no unregister — but the leak scope is per WORLD-JOIN, not only dynamic-dim churn
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:632
- evidence: Verified: registration mirrors vanilla Minecraft.java:649-651 exactly (extractor + levelRenderer.cloudRenderer()), and 26.2 ReloadableResourceManager exposes only registerReloadListener. However the auditor (and the in-code note at :636-646) under-scope the leak: it is not bounded to dynamic-dim churn. ClientWorldLoader.cleanUp() (:251-268, fired on every world exit via the 2-arg updateLevelInEngines funnel in MixinMinecraft:188-203) also never unregisters, while the ReloadableResourceManager is client-lifetime — so every world join that creates a secondary permanently leaks 2 listeners retaining the dead extractor+renderer graph, and a later F3+T/resource-pack reload runs prepare/apply on a CLOSED CloudRenderer (worldRenderer.close() → cloudRenderer.close() ran in disposeWorldRenderer :280-283).
- IP ref: No IP analog — IP 1.21.3 registered no reload listeners in createSecondaryClientWorld (relied on worldRenderer.onResourceManagerReload + _onWorldRendererReloaded propagation, both retained by the port).
- fix: No action for S14 (single-session test; the dead-listener path needs a world exit + reload to fire, and the extractor side is a harmless flag write). Post-S14: add a vanilla accessor into ReloadableResourceManager's private listeners list and unregister in BOTH disposeDimensionDynamically and cleanUp; until then note the per-join accumulation in the port note, not just the churn case.

#### [BLOCKER] MISSED Remote-ticking the secondary corrupts the shared per-connection ClientClockManager — the MAIN world's day-night clock jumps and stays wrong the moment the first cross-dim secondary exists
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:203
- evidence: 26.2 ClientLevel.tickTime (mc262-ref ClientLevel.java:439-443) now does clockManager().tick(gameTime) — and ClientLevel.clockManager() (:752-754) returns connection.clockManager(), the ONE ClientClockManager per ClientPacketListener (:426,:456,:2876-2878) shared by ALL client levels. ClientClockManager.tick (ClientClockManager.java:22-32) advances every clock by delta = fedGameTime - lastTickGameTime — a telescoping feed that assumes a single monotonic gameTime source. The port's tickRemoteWorld (:203, IP-verbatim newWorld.tick(() -> true)) runs the secondary's tickTime every client tick (remote ticking default-on, IPCGlobal.java:26; driven from MixinMinecraft.onAfterClientTick:146, i.e. right AFTER the main level.tick), and the secondary's fresh ClientLevelData starts at gameTime=0 (:574-578, IP-faithful). Alternating feeds — main M, then secondary S with D = M-S ≈ the world's entire game time — leave every render-visible totalTicks at authoritative-D (the deltas telescope; the remote tick is the last clock write before each frame; ClientboundSetTime's absolute reset via handleUpdates, ClientPacketListener.java:1134, is re-shifted by the very next remote tick). The overworld's sun/sky/fog/moon are timeline-driven off this clock (AttributeTrackSampler.java:40 samples clockManager.getTotalTicks; Level.getDefaultClockTime:808-813), so the instant the nether secondary is created the OW time-of-day jumps by -(M-S) mod 24000 and stays wrong while the secondary exists. Could never fire in rung 1 (same-dim dests resolve to the main level; CLIENT_WORLD_MAP has one entry, the remote loop no-ops) — S14 is its first exercise. SPIKE-R1 §4's remote-tick role-transfer analysis tracked only removeBlockBreakingProgress and missed this coupling.
- IP ref: IP 1.21.3 ClientLevel.tickTime wrote only per-level ClientLevelData gameTime/dayTime — no shared object — so IP's tickRemoteWorld (IP ClientWorldLoader.java:154-174) was cross-level inert; the separate-fresh-ClientLevelData model ('day time is not shared between worlds', IP :438-444) is only safe on 26.2 if the shared connection clock is excluded from remote ticks.
- fix: Preserve IP semantics (remote tickTime advances ONLY the remote level's own gameTime): in MixinClientLevel add @Inject(method="tickTime", at=@At("HEAD"), cancellable=true) that, when ClientWorldLoader.isClientRemoteTicking is true (the flag already brackets exactly the remote loop, ClientWorldLoader.java:145/158), does clientLevelData.setGameTime(getGameTime()+1) and cancels — skipping the shared clockManager().tick. (Equivalent: @WrapOperation on the ClientClockManager.tick call inside tickTime.) Needs @Shadow @Final ClientLevel.ClientLevelData clientLevelData (declared in ClientLevel, mc262-ref :154). Main-level clock driving and handleSetTime stay untouched.

#### [MAJOR] MISSED The rendered dest dimension's light updates never run while it is being rendered — lateUpdateLight keeps IP's !isDimensionRendered gate but 26.2 removed the renderLevel-embedded light pair the gate depended on
- file: common/src/main/java/qouteall/imm_ptl/core/render/MyRenderHelper.java:281
- evidence: IP 1.21.3's contract: LevelRenderer.renderLevel itself ran level.pollLightUpdates() + runLightUpdates() for WHATEVER level it rendered (proven by IP's own MixinLevelRenderer.redirectRunQueuedChunkUpdates targeting ClientLevel;pollLightUpdates INSIDE renderLevel, IP MixinLevelRenderer.java:478-489), so rendered portal-dest dims published light every frame and lateUpdateLight (IP MyRenderHelper.java:446-451) only had to cover NON-rendered dims. On 26.2 that pair moved OUT of the per-level render into Minecraft.renderFrame's this.level.update() (mc262-ref Minecraft.java:1286-1288; ClientLevel.update = pollLightUpdates + runLightUpdates, ClientLevel.java:496-503) — main level only — and the flag-ON dest pass (SecondaryWorldRenderCore / MyGameRenderer) contains NO pollLightUpdates/runLightUpdates/update() call (grep: zero matches). The port kept the gate verbatim (:281), and renderedDimensions IS populated flag-ON (PortalRendering.onEndPortalWorldRendering:108-110), so on every frame where the nether view renders, the nether is SKIPPED by lateUpdateLight and nothing else runs its light engine. tickRemoteWorld's pollLightUpdates (:209) only drains packet lambdas into the engine queues — publication (runLightUpdates, which also fires onLightUpdate → section remesh) happens only on frames where the portal is NOT rendered. In the S14 stare-at-the-portal posture, dynamic light changes and chunk-edge light propagation of the still-streaming dest chunks queue indefinitely — the exact dark/stale-light signature of memory portalview-light-engine-half-port, resurrected for the rendered case.
- IP ref: IP 1.21.3: vanilla renderLevel light pair (redirect-proven at IP MixinLevelRenderer.java:478-489) + IP MyRenderHelper.lateUpdateLight:446-451 gate — together total over all dims; the port's verbatim gate (MyRenderHelper.java:275-285) is total only if the dest pass re-expresses the renderLevel half, which it does not.
- fix: 26.2 re-expression of IP's semantics: vanilla now covers ONLY mc.level, so change lateUpdateLight's gate from !RenderStates.isDimensionRendered(world.dimension()) to world != Minecraft.getInstance().level — run every non-main world's runLightUpdates at frame END (outside any world swap, so ImmPtlClientChunkMap.onLightUpdate's getWorldExtractor routing stays correct). Do NOT instead run the pair inside the swapped dest pass without also fixing the getWorldExtractor short-circuit (see the companion NOTE).

#### [NOTE] MISSED Latent identity bug: getWorldExtractor's main-dim short-circuit returns the MAIN extractor for a dim whose level is currently swapped in as CLIENT.level (onLightUpdate routing)
- file: common/src/main/java/qouteall/imm_ptl/core/chunk_loading/ImmPtlClientChunkMap.java:302
- evidence: ImmPtlClientChunkMap.onLightUpdate routes via ClientWorldLoader.getWorldExtractor(level.dimension()), whose short-circuit (ClientWorldLoader.java:378-380) keys off CLIENT.level.dimension() — a field the port swaps in withSwitchedWorld (:753) and in the dest render pass (MyGameRenderer per SecondaryWorldRenderCore.java:187-198 comment). If onLightUpdate ever fires for the nether while CLIENT.level is swapped to the nether, it returns CLIENT.levelExtractor (the MAIN OW-bound extractor) and the dirty lands on the wrong SectionUpdateTracker (nether section never remeshes; spurious OW dirt). Currently unreachable: all live runLightUpdates call sites run with CLIENT.level = true main (vanilla Minecraft.renderFrame:1287; lateUpdateLight at frame END outside any swap), and tickRemoteWorld's in-swap pollLightUpdates only enqueues. S13-H already fixed the identical short-circuit trap in SecondaryWorldRenderCore (DEFECT-1, routing by RenderStates.originalPlayerDimension); this call site still carries it latently and becomes live the moment anyone runs dest light updates inside the swapped context (e.g., a naive fix of the MAJOR light finding).
- IP ref: IP 1.21.3 MyClientChunkManager.onLightUpdate → ClientWorldLoader.getWorldRenderer(dim).setSectionDirty — getWorldRenderer has no main-dim short-circuit (pure map lookup), so IP was identity-safe under switching by construction.
- fix: Either route onLightUpdate by the true main dim like the S13-H fix (use RenderStates.originalPlayerDimension for the CLIENT.levelExtractor short-circuit, or prefer WORLD_EXTRACTOR_MAP.get(dimension) with the main-dim entry being CLIENT.levelExtractor — which initializeIfNeeded:484 already guarantees), or document the invariant that runLightUpdates must never run under a world swap. Pair this with whichever fix is chosen for the light-update MAJOR.

#### [MINOR] MISSED Remote worlds now run tickWeatherEffects (26.2 moved weather particle/sound spawning into ClientLevel.tick) — IP divergence, inert for the S14 nether
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:151
- evidence: The port's tick() role-transfer note (:151-157) records only removeBlockBreakingProgress as ClientLevel.tick's absorbed renderer-side duty, but 26.2 ClientLevel.tick also calls tickWeatherEffects (mc262-ref ClientLevel.java:304, method :341+), which spawns rain particles/sounds positioned by this.minecraft.gameRenderer.mainCamera().position() (:347) evaluated against THIS level's heightmap — so the IP-verbatim tickRemoteWorld (newWorld.tick(() -> true), :203) now runs weather spawning for secondaries: main-camera coordinates sampled in the remote world's terrain, particles created into the shared engine while it is world-swapped. In IP 1.21.3 weather particles were driven only for the main camera/renderer (IP's remote loop ran tickEntities/tick/pollLightUpdates + LevelRenderer.tick, none of which spawn weather; 1.21.3 ClientLevel.tick had no weather-particle code). For S14 this is inert: the nether has no precipitation (getRainLevel 0 → early return :344-345), and rung-1 same-dim portals create no secondaries.
- IP ref: IP 1.21.3 tickRemoteWorld (IP ClientWorldLoader.java:154-174) spawned no weather; 1.21.3 weather spawning lived renderer-side and ran only for the main camera. 26.2 relocated it into ClientLevel.tick, silently extending IP's remote tick.
- fix: Defer to S18 (weather is already ledger-deferred there: dest weather render omitted). At S18, suppress tickWeatherEffects for remote-ticked worlds (skip when ClientWorldLoader.isClientRemoteTicking, matching IP's main-only weather spawning) or re-express it with the portal-transformed camera position like tickRemoteWorldRandomTicksClient does for animateTick. Record in the deviation ledger until then.

## VERIFY result (verdict=DEFECTS_FOUND)
notes: Independent re-trace performed end-to-end on the live flag-ON chain: PortalRenderer.renderPortalContent (:314-326, renderDistance/doRenderSky feeds) -> MyGameRenderer.switchAndRenderTheWorld (camera config :222-229 BEFORE the lightmap prime :298-300; lightmap swap :265; fog swappingManager push/pop :269/:358; recursion-safe projection locals :314-315/:347) -> SecondaryWorldRenderCore.renderDestWorld Step 6 (setupFog compute-only verified against mc262 FogRenderer.java:167-186; writeFogSlice byte-matches the std140 layout of updateBuffer:209-229; the WORLD ring slot is written only at GameRenderer.renderLevel:559 and the dest pass never calls updateBuffer; setShaderFog saved/restored in finally :455/543-547 with vanilla's later passes rebinding their own slices anyway — the main-world fog-corruption signature is closed) -> Row-16 fill (RendererUsingStencil.replaceFrameBufferClearing:82-102 consuming the Step-6 publish) -> renderPortalSky (nether = Skybox.NONE at mc262 DimensionTypes.java:80 -> early return; vanilla sky uses the same terrainFog slice, LevelRenderer.java:208, so binding destFogBuffer matches) -> lightmap bind at draw (ChunkSectionsToRender.renderGroup:51 and FeatureRenderDispatcher.prepareFrame:70 both read gameRenderer.lightmap() = the @Shadow @Mutable field, MixinGameRenderer:93-96/243-245; GameRendererLightmapMixin's override is null flag-ON so no collision; useUiLightmap is false during world render). Nether fog color: fresh virtual Camera per pass, newCamera.tick() primes the probe (Camera.java:83-88), ValueProbe ctor sets lastValue==newValue (EnvironmentAttributeProbe.java:48-52) so no smoothing lag; FOG_COLOR/FOG_START/END are data-driven on the nether DimensionType (DimensionTypes.java:83-84). Mixin weave checked: both FogRenderer mixins registered (seamlessportals-ip-client.mixins.json:26,47), no anchor or collision issues; StaticFieldsSwappingManager pushSwapping cannot NPE (contextMap populated by per-frame FogRendererContext.update + computeIfAbsent for fresh dims; onPlayerTeleport wired at ClientTeleportationManager:532). VERDICT RATIONALE: all four auditor findings CONFIRMED at NOTE severity (finding 1 corrected: getFogColorOf is caller-less in IP 1.21.3 too, so retention is IP-faithful), but the auditor's READY does not survive: two MAJOR misses — (1) the post-crossing stale look-back lightmap (main-born helper's renderState==null early-return vs IP's unconditional updateLightTexture(0); manifests directly in the S14 cross-then-look-back loop as time passes/effects change), and (2) the CUTOVER_SPEC §3-item-3-designed but unimplemented rainFogMultiplier snapshot/restore (shared static AtmosphericFogEnvironment mutated by every dest setupFog; visible whenever it rains with a portal on screen). Neither is a BLOCKER — no crash, and a short clear-weather S14 session right after crossing looks correct — but both are reachable in normal S14 play and are IP divergences, so DEFECTS_FOUND. Recommend fixing M1 before the S14 launch (small, contained in DimensionRenderHelper) and M2 before sign-off (needs one accessor mixin); the three missed NOTEs are backlog/test-expectation items.

#### [NOTE] CONFIRMED FogRendererContext swap machinery (getFogColorOf, StaticFieldsSwappingManager, vestigial fog fields, no-op copy hooks) is dead code in the live 26.2 path
- file: common/src/main/java/qouteall/imm_ptl/core/render/context_management/FogRendererContext.java:116
- evidence: Verified: getFogColorOf has zero callers in the port (grep: only self-references in comments); the live Row-16 fill color flows SecondaryWorldRenderCore.java:398 setCurrentRenderedFogColor(destFogData.color) -> MixinFogRenderer (mixin/client/multiworld_awareness/MixinFogRenderer.java:48) wiring getCurrentFogColor -> RendererUsingStencil.replaceFrameBufferClearing:95. The copy hooks are no-op lambdas (MixinFogRenderer.java:39-40) and 26.2 FogRenderer has no statics to mirror, so pushSwapping/popSwapping (MyGameRenderer.java:269/358) swap nothing. CORRECTION to the auditor: getFogColorOf is ALSO caller-less in IP 1.21.3 itself (IP tree grep: only the definition at IP FogRendererContext.java:65), so carrying it dead is IP-faithful — the auditor's S20 delete suggestion would itself be a shape deviation.
- IP ref: IP:imm_ptl/core/render/context_management/FogRendererContext.java:65 (definition, no callers in IP either); IP:84-105 swapped the six vanilla fog statics, GONE on 26.2 (API_RISKS R9).
- fix: No action for S14. At S20, if pruned at all, record it as a deliberate shape deviation; retention is equally defensible since IP carries the same dead method.

#### [NOTE] CONFIRMED Dest fog distances bypass MixinRenderSystem_Fog.transformFogDistance — fuse-view (and hasFog=false) fog-off is inert on the live dest path (latent, not S14)
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:403
- evidence: Verified end-to-end: the only vanilla caller of FogRenderer.updateBuffer(FogData) is the MAIN pass (mc262 GameRenderer.renderLevel:559, where PortalRendering.isRendering()==false so transformFogDistance is identity — MyRenderHelper.java:342-360). The dest pass serializes FogData via writeFogSlice (SecondaryWorldRenderCore.java:749-766) and never hits updateBuffer, so MixinRenderSystem_Fog (mixin/client/render/MixinRenderSystem_Fog.java:32-41) never fires with isRendering()==true. This also covers the WorldRenderInfo.isFogEnabled()==false branch (hasFog / debugDisableFog *23333). Standard non-fuse S14 portals are unaffected (transformFogDistance returns value unchanged, MyRenderHelper.java:356-359).
- IP ref: IP:imm_ptl/core/mixin/client/render/MixinRenderSystem_Fog.java:11-23 scaled setShaderFogStart/End during the recursive dest render (fuse-view *23333).
- fix: Defer to S18 (fuse-view periphery). Apply MyRenderHelper.transformFogDistance to the four FogData distances inside writeFogSlice (or on destFogData before the write) so the IP per-dest scaling re-attaches to the live serialization point.

#### [NOTE] CONFIRMED FogRendererContext.currentRenderedFogColor static is not restored after nested-portal recursion (harmless by construction)
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:398
- evidence: Verified: the Step-10.3 fill (IPCGlobal.renderer.replaceFrameBufferClearing, SecondaryWorldRenderCore.java:461) consumes the static BEFORE the Step-10.10 recursion (onBeforeTranslucentRendering, :507) at every layer, so no fill ever reads a value published by an inner layer; after the recursion returns the outer layer performs no further fill. S14 rung 2 involves no nesting.
- IP ref: IP read the dest fog color from the live (swapped) fog statics during each recursive renderLevel, so each layer's redirectClearing fill inherently saw its own dim's color.
- fix: Optional robustness only: save/restore currentRenderedFogColor around the Step-10.10 call, or add a one-line comment stating the fill-before-recursion ordering is the safety invariant.

#### [NOTE] CONFIRMED Dest setupFog uses darkenWorldAmount=0f — boss-fight world darkening does not tint the portal-view fog
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:384
- evidence: Verified: renderDestWorld passes 0f (SecondaryWorldRenderCore.java:384-386); computeFogColor only applies the darken lerp when darkenWorldAmount>0 (mc262 FogRenderer.java:127-131). IP's LIVE dest path (the recursive renderLevel -> setupColor with getDarkenWorldAmount) did apply boss darkening, so this is a genuine cosmetic micro-divergence, carried verbatim from the runtime-proven block core (PortalContextSwitch.java:1478-1483, '0f // no boss darkening for portal view'). Non-blocking for S14.
- IP ref: IP's recursive dest renderLevel ran vanilla FogRenderer.setupColor with gameRenderer.getDarkenWorldAmount(pt); also IP:FogRendererContext.java:84-105.
- fix: For exact parity pass client.gameRenderer.bossOverlayWorldDarkening(RenderStates.getPartialTick()); low priority, pair it with the same fix in DimensionRenderHelper (see missed NOTE on lightmap parity).

#### [MAJOR] MISSED Post-crossing look-back lightmap is permanently stale: main-born DimensionRenderHelper can never re-render its Lightmap once its dim is no longer the current main dim
- file: common/src/main/java/qouteall/imm_ptl/core/render/context_management/DimensionRenderHelper.java:86
- evidence: updateAndRender early-returns when renderState==null (DimensionRenderHelper.java:86), and renderState is null exactly for helpers constructed while client.level==world (the ctor branch :56-60 that reuses the GameRenderer's own Lightmap). That nullity is decided at BIRTH, but the 'vanilla drives it' premise dies at the first cross-dim crossing: ClientTeleportationManager.changePlayerDimension:495-497 and RenderStates.onTotalRenderEnd:299-303 re-point the swapped GameRenderer.lightmap field to the NEW main dim's helper, and vanilla renders only that field (mc262 GameRenderer.render:423 this.lightmap.render(...)). After crossing OW->nether, helper(OW).lightmapTexture (the original GameRenderer Lightmap object) is driven by NOBODY: vanilla drives helper(nether)'s, and the shell's dest-pass update (MyGameRenderer.java:298-300, gate !isDimensionRendered passes for OW) calls updateAndRender which early-returns. Failure scenario = the core S14 loop: cross into the nether at day, play (or toggle gamma / night vision / darkness / day-night passes), look back through the portal — the overworld view renders with the lightmap frozen at the crossing moment (wrong sky-light tint, no flicker, stale gamma/effects), worsening indefinitely until relog. Rung 1 never hit this (same-dim gate blocks the call entirely), and the block era never hit it (its helper always owned a private Lightmap+renderState and always rendered — com/warwa/seamlessportals/render/DimensionRenderHelper.java:43-46,63-117).
- IP ref: IP:MyGameRenderer.java:223-226 calls helper.lightmapTexture.updateLightTexture(0) UNCONDITIONALLY at the gated call site — 1.21.3 LightTexture.updateLightTexture is a full instance self-recompute reading the (already swapped) client.level, so IP's main-born helper texture stayed fresh when rendered as a dest. IP:DimensionRenderHelper.java:29-33 guards only tick()/cleanUp() by live-field identity, never by birth-time role.
- fix: Re-express IP's unconditional update: allocate `renderState = new LightmapRenderState()` for EVERY helper (keep the ctor's main-dim Lightmap-identity reuse and the cleanUp identity guard), and delete the `renderState == null` early-return in updateAndRender. Safety holds by the existing call-site gate: isDimensionRendered(originalPlayerDimension) is always true (RenderStates.java:321-323), so updateAndRender is never invoked for the CURRENT main dim and therefore never re-drives the Lightmap vanilla renders at GameRenderer.render:423; each Lightmap has its own UBO ring so the mid-frame render is the block-era-proven mechanic.

#### [MAJOR] MISSED AtmosphericFogEnvironment.rainFogMultiplier shared-mutable cross-dimension leak — the CUTOVER_SPEC-designed snapshot/restore bracket for FogEnvironment state was never implemented
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:384
- evidence: FogRenderer.FOG_ENVIRONMENTS is a STATIC list of shared instances (mc262 fog/FogRenderer.java:39-46) and AtmosphericFogEnvironment carries the only mutable field, `private float rainFogMultiplier` (:26). Every dest pass hits it: MixinCamera.getSubmergedFluidState forces FogType.NONE while rendering (MixinCamera.java:94-99) -> getFogType returns ATMOSPHERIC -> setupFog runs updateRainFogState against the DEST level+camera (AtmosphericFogEnvironment.java:68-69,89-98), lerping the SHARED multiplier toward the dest target (nether: sky light 0 -> target 0) by deltaTicks*0.2 PER PORTAL PER FRAME, with no snapshot/restore anywhere in Step 6 (SecondaryWorldRenderCore.java:379-403). Failure scenario: raining in the overworld with a nether portal on screen — each frame the dest pass(es) drag the multiplier toward 0 and the main pass drags it back, settling roughly halfway (multiple portals amplify), so main-world rain fog (start -160/end -256 offsets) is visibly weakened/unstable while the window renders; symmetrically the dest view inherits residual OW rain-fog offsets. CUTOVER_SPEC.md:304-315 (§3 item 3) explicitly designs 'snapshot-and-restore around the dest render ... so a dest-world rain-fog value never bleeds into the main-world's next pass' — designed, cited by the S11B port-note, never implemented; not an approved deviation (the S11B note's claim that the virtual Camera isolates FogEnvironment state is wrong — the camera isolates only the probe). Clear-weather S14 tests won't show it; the first rainy session will.
- IP ref: IP:FogRendererContext.java:84-105 + StaticFieldsSwappingManager swapped ALL per-dim fog smoothing statics around every dest render, so neither direction leaked; API_RISKS.md R9 names AtmosphericFogEnvironment.rainFogMultiplier as the 'same-shaped cross-dimension leak, new home'.
- fix: Implement CUTOVER_SPEC §3 item 3 in Step 6: reach the shared AtmosphericFogEnvironment instance (accessor mixin exposing FogRenderer.FOG_ENVIRONMENTS or an @Accessor on rainFogMultiplier), keep a per-dim multiplier map (the StaticFieldsSwappingManager pattern applied to the one field): before fr.setupFog store the current (outer) value under the source dim and install the dest dim's stored value; after the call store the updated value under the dest dim and restore the outer value — inside the existing Step-6/10 try/finally so a throw cannot leave dest state installed. (A plain snapshot/restore is the minimal acceptable form; the per-dim map matches IP's per-dim smoothing fidelity.)

#### [NOTE] MISSED Dest lightmap parity gaps vs IP: bossOverlayWorldDarkening forced to 0 and blockLightFlicker advanced per-frame instead of per-tick
- file: common/src/main/java/qouteall/imm_ptl/core/render/context_management/DimensionRenderHelper.java:134
- evidence: renderState.bossOverlayWorldDarkening = 0.0F (:134) where IP's dest lightmap recompute (LightTexture.updateLightTexture) read the live getDarkenWorldAmount — the lightmap twin of the auditor's fog finding 4. Also blockLightFlicker advances inside updateAndRender (:91-93), i.e. once per FRAME per dest dim, while vanilla/IP advance flicker once per game TICK (mc262 LightmapRenderStateExtractor.tick, driven from GameRenderer.tick:258) — frame-rate-dependent flicker cadence, most visible in the S14 nether dest view (all block light). Both carried verbatim from the runtime-proven block-era helper (com/warwa .../DimensionRenderHelper.java:70-72,113), so the look is user-accepted; cosmetic only.
- IP ref: IP's LightTexture.updateLightTexture included getDarkenWorldAmount; vanilla 1.21.3/26.2 both advance flicker per tick, and IP:DimensionRenderHelper.java:29-33 ticked secondaries' flicker per game tick via tick().
- fix: Polish backlog (task #9): pass bossOverlayWorldDarkening = client.gameRenderer.bossOverlayWorldDarkening(pt) (paired with the fog finding-4 fix), and move the flicker advance into DimensionRenderHelper.tick() (called per game tick from ClientWorldLoader.tick:172) storing it per-helper.

#### [NOTE] MISSED renderPortalSky END branch omits vanilla's end-flash pass (dest=END only, not S14)
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:604
- evidence: The END branch calls only sr.renderEndSky() (:604-605); vanilla addSkyPass additionally renders renderEndFlash when state.endFlashIntensity > 1.0E-5F (mc262 LevelRenderer.java:332-337). An end portal-view during an end flash would miss the flash. Irrelevant to S14 (nether dest = Skybox.NONE early return at :592, verified against mc262 data/worldgen/DimensionTypes.java:80).
- IP ref: IP's dest sky ran the full vanilla sky path via the recursive renderLevel, including all END sub-passes.
- fix: At S18 periphery: add the endFlashIntensity>1e-5 -> sr.renderEndFlash(poseStack, sky.endFlashIntensity, sky.endFlashXAngle, sky.endFlashYAngle) half to the END branch (SkyRenderState carries the fields).

#### [NOTE] MISSED Dest fog radius is the FULL render distance (IP-faithful) while dest chunk loading is graduated — expect the block-era fog-convergence polish to be absent in the S14 nether view
- file: common/src/main/java/qouteall/imm_ptl/core/render/renderer/PortalRenderer.java:337
- evidence: getPortalRenderDistance returns client.options.getEffectiveRenderDistance() for a normal portal (:337-351, IP-verbatim), which Step 6 feeds into setupFog via WorldRenderInfo.getRenderDistance() (SecondaryWorldRenderCore.java:384-386) — the SecondaryWorldRenderCore comment calling this 'IP's graduated dest radius' is mislabeled. Dest chunk loading is graduated/capped (~8 chunks default, memory ip-dest-loading-model), so beyond the loaded radius the nether view shows the Row-16 flat atmosphere fill rather than fog-converged terrain. The block era deliberately deviated here (smoothedDestFogRadius ~1.5s convergence + loaded-radius fog, PortalContextSwitch.java:1466-1484, the 'far chunks disappear' fix) — that polish is dropped by the faithful re-port. This is authentic IP behavior, NOT a defect; recorded so the S14 live loop doesn't mis-triage it as a regression.
- IP ref: IP's dest fog used Math.max(gameRenderer.getRenderDistance(), 32) — the full render distance — while loading ~8 dest chunks; IP's redirectClearing fill provided the atmosphere backdrop beyond loaded terrain.
- fix: No code change (IP wins). Add a line to the S14 test-expectations / polish backlog: distant nether-view backdrop = flat atmosphere color at graduated-load boundaries is authentic IP; the block-era smoothed-radius polish is a candidate approved-deviation for task #9 if the user prefers the old look.

## VERIFY result (verdict=DEFECTS_FOUND)
notes: Independent end-to-end trace of secondary-world tick + light liveness, cross-dim (S14) path. VERIFIED SOUND: (1) ClientWorldLoader.tick() dispatch — MixinMinecraft.onAfterClientTick anchors at ClientLevel.tick(BooleanSupplier) INVOKE+AFTER in Minecraft.tick, identical to IP, weave proven live in S13; (2) the deleted worldRenderer.tick() loop is a correct 26.2 role transfer (removeBlockBreakingProgress now in ClientLevel.tick:305, corpus-confirmed); (3) IPCGlobal.isClientRemoteTickingEnabled / lateClientLightUpdate defaults match IP (both true); (4) lateUpdateLight IS wired for a newly-created secondary (iterates CLIENT_WORLD_MAP, frame-END anchor after renderLevel matches IP) — the defect is the gate semantics (missed finding 1), not the wiring; (5) chunk-source swap for the fresh secondary: MixinClientLevel ctor RETURN-inject retargeted to the 26.2 descriptor (LevelExtractor + seaLevel), 26.2 ctor leaks no pre-swap chunkSource reference (ClientLevel.java:238-263), so the secondary's light engine identity is the ImmPtl one; onLightUpdate correctly routes to the per-dim extractor's setSectionDirty with the main-dim identity guard; (6) renderedDimensions lifecycle: cleared every frame in updatePreRenderInfo, driven from the flag-ON MinecraftFramePumpMixin branch; (7) DimensionRenderHelper: per-dim Lightmap identity + first-visit-per-frame updateAndRender gate matches IP's updateLightTexture gate; nether no-skylight/fixed-time now rides the dest virtual camera's EnvironmentAttributeProbe (camera primed with dest level BEFORE the lightmap prime, MyGameRenderer:215-229 ordering correct); (8) entity ticking: tickEntities exists on 26.2 (:453) and entity animation state (tickCount/walkAnimation) is tick-fed for window-visible entities; (9) particle path (shared engine tick under swapped world + camera repositioned, animateTick %2) is verbatim IP; (10) WorldInfoSender weather half survives via PacketRedirection; the time-half deletion (F1) is directionally sound for DAY time (26.2 day time is the connection-global WorldClock, synced by vanilla to every player) — but it left the secondary's LOCAL gameTime unseeded, which feeds missed finding 2. ANSWER to the link's time question: the secondary receives NO per-dim time updates (no SetTime redirection anymore); day time arrives via the shared connection ClockManager, gameTime is purely local (starts at 0). CAVEAT: none of this code ran in rung 1 — same-dim portals create no secondary world, so tickRemoteWorld, createSecondaryClientWorld, lateUpdateLight-for-secondaries, and the dest light pipeline are all live-unproven; the three missed findings are first-launch surface. Findings 1 and 2 should be fixed before the user's first S14 runClient; finding 3 can ride the same commit.

#### [BLOCKER] MISSED Rendered cross-dim secondary NEVER gets runLightUpdates while visible through the portal — the prior shipped dark-portal-view bug re-created by the isDimensionRendered gate
- file: common/src/main/java/qouteall/imm_ptl/core/render/MyRenderHelper.java:281
- evidence: lateUpdateLight (MyRenderHelper.java:275-285) runs runLightUpdates ONLY for worlds where !RenderStates.isDimensionRendered(dim). The flag-ON stencil driver marks the dest dim rendered every frame the portal draws (PortalRenderer.java:328 renderPortalContent -> PortalRendering.onEndPortalWorldRendering -> RenderStates.renderedDimensions.add(destDim), PortalRendering.java:107-111), and lateUpdateLight fires AFTER renderLevel (MixinGameRenderer.java:222-238), so the visible dest dim is always skipped. In IP 1.21.3 that gate was sound because vanilla LevelRenderer.renderLevel itself ran pollLightUpdates+runLightUpdates for every rendered dim including nested portal renders (proven by IP's own redirect of that call: IP MixinLevelRenderer.java:478-489). On 26.2 that block moved to ClientLevel.update() (mc262 ClientLevel.java:496-503), called ONLY for the main level from Minecraft.renderFrame (Minecraft.java:1287). The port's dest render pass (SecondaryWorldRenderCore.renderDestWorld, invoked from MyGameRenderer.switchAndRenderTheWorld:321-339) contains NO light-run (grep: zero pollLightUpdates/runLightUpdates in SecondaryWorldRenderCore; LevelExtractor.java touches no light engine), and no other code path covers a rendered secondary (only tick-side pollLightUpdates, ClientWorldLoader.java:209 — the POLL half only; block-era lateUpdateSecondaryLight iterates PortalWorldManager.levels which is empty flag-ON). Net: while the user looks at the S14 nether portal, queued chunk-light for the dest is fed into the engine but never published (LightEngine publication + onLightUpdate section-dirty reports happen only in runLightUpdates), so the nether view meshes compile dark and stay dark/stale until the player looks AWAY for a frame — the exact mechanism of the prior shipped bug (memory portalview-light-engine-half-port), violating the S14 requirement that portal-view lighting be correct BEFORE any crossing. Rung 1 could not catch this: same-dim portals have no secondary world.
- IP ref: IP MyRenderHelper.java:441-451 (lateUpdateLight covers NOT-rendered dims only) + IP MixinLevelRenderer.java:478-489 (renderLevel's per-rendered-dim pollLightUpdates, world-switched) — IP's invariant is 'every dim with a live view runs light updates every frame'. 26.2 relocation: mc262 ClientLevel.java:496-503 (update = poll+run), Minecraft.java:1286-1288 (main level only). The port's own corpus prescribed the fix: migration/api-map/mixin-client.md row ⑱ 'Redirect there / run secondaries alongside' — not implemented for rendered dims.
- fix: Run the dest level's light pass in the dest render, restoring the 1.21.3 invariant: in SecondaryWorldRenderCore.renderDestWorld (before the extract), or in MyGameRenderer.switchAndRenderTheWorld immediately before invokeWrapper.accept (MyGameRenderer.java:321), add: newWorld.pollLightUpdates(); newWorld.getChunkSource().getLightEngine().runLightUpdates(); — the 26.2 re-expression of vanilla 1.21.3 renderLevel's light block / 26.2 ClientLevel.update(). Equivalent alternative: drop the isDimensionRendered gate in lateUpdateLight (run ALL secondaries at frame end — the mod's runtime-proven 3a2c14e form) and record it as a required 26.2 re-expression (the vanilla rendered-dim light-run no longer exists inside the render pass on 26.2). Either way, document in the port-note that the S11-B 'exactly IP's own form' claim was unsound on 26.2.

#### [BLOCKER] MISSED Remote-world tick corrupts the connection-global ClientClockManager: tickTime() now has a cross-world side effect keyed to the secondary's unseeded (0-based) gameTime
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:203
- evidence: tickRemoteWorld runs newWorld.tick(() -> true) (ClientWorldLoader.java:203) per client tick for each secondary. On 26.2, ClientLevel.tick -> tickTime() (mc262 ClientLevel.java:299-306, 439-443) does clientLevelData.gameTime+1 THEN clockManager().tick(gameTime), and clockManager() is the CONNECTION-scoped ClientClockManager (ClientLevel.java:752-754) shared with the main world (the secondary is built on the same mainNetHandler, ClientWorldLoader.java:600-611). ClientClockManager.tick is DELTA-based (ClientClockManager.java:22-32: delta = gameTime - lastTickGameTime; every clock += delta*rate). The secondary's ClientLevelData is constructed fresh (ClientWorldLoader.java:574-578) with gameTime = 0 (mc262 ClientLevel.java:1153-1161 — no init) and is never seeded/synced (the port deleted the per-dim SetTime redirect as deviation F1, WorldInfoSender.java:47-59, and vanilla handleSetTime targets only the main level, mc262 ClientPacketListener.java:1129-1134). So each Minecraft.tick: main tickTime advances the shared clocks by +1, then the remote tickTime slams them by (secondaryGT - mainGT) ≈ -(main gameTime at secondary creation) — and since renders always follow the remote tick, every frame renders with all WorldClock-driven state (overworld day time, sun/moon position, sky color, dayTime consumers) offset by that huge negative constant. Visible effect at S14: the moment the cross-dim secondary starts ticking, the main world's time-of-day snaps to a wrong value (roughly world-start time) and stays wrong while the portal exists, with per-tick ±1 sawtooth and a snap-back when the secondary is disposed. IP 1.21.3's identical-looking newWorld.tick(()->true) was safe because 1.21.3 tickTime wrote ONLY per-level fields (gameTime/dayTime) — no shared-state write existed; keeping the line verbatim on 26.2 is an unsound re-expression. Ground truth in-repo: the block-era system avoided exactly this by never calling ClientLevel.tick on secondaries and force-syncing gameTime from the active level (PortalWorldManager.java:1503-1513 syncTimeToCachedLevels).
- IP ref: IP ClientWorldLoader.java:160 (newWorld.tick(() -> true)) — safe on 1.21.3 (per-level tickTime, no shared clock); IP WorldInfoSender.java:48-60 additionally kept remote gameTime/dayTime absolutely correct via the redirected per-dim ClientboundSetTimePacket. 26.2 divergence surface: mc262 ClientLevel.java:439-443 + :752-754; ClientClockManager.java:22-42.
- fix: Two parts, both required. (a) Suppress the cross-world clock side effect during remote ticking: in MixinClientLevel, @WrapOperation (or @Redirect) the INVOKE of ClientClockManager.tick(J) inside ClientLevel.tickTime, no-op when ClientWorldLoader.isClientRemoteTicking is true — this exactly restores IP 1.21.3 tickTime semantics (remote tickTime touches only its own level data; the shared clock is advanced solely by the main level and by handleSetTime). (b) Seed the secondary's absolute gameTime at creation: in createSecondaryClientWorld, after the ctor, newWorld.setTimeFromServer(CLIENT.level.getGameTime()) (or properties.setGameTime(...) before the ctor) so gameTime-consuming logic in dest worlds (e.g. portal-animation timing, %20 expiry windows) sees real time — the 26.2 replacement for the F1-deleted per-dim SetTime redirect; it then stays in lockstep (+1/tick under the shared TickRateManager). Optionally mirror handleSetTime's absolute correction to all secondaries for drift-proofing.

#### [MAJOR] MISSED Secondary worlds never tick block entities on 26.2 — tickBlockEntities was hoisted out of tickEntities and the remote tick didn't pick it up
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:202
- evidence: tickRemoteWorld calls newWorld.tickEntities() + newWorld.tick(() -> true) (ClientWorldLoader.java:202-203), the verbatim IP 1.21.3 pair. On 1.21.3, ClientLevel.tickEntities tail-called this.tickBlockEntities(), so IP's secondaries got client block-entity ticking for free. On 26.2 that call was HOISTED out: Minecraft.tick invokes this.level.tickEntities() and this.level.tickBlockEntities() as SEPARATE calls (mc262 Minecraft.java:1797-1799 — pinned by the port's own corpus at migration/verify/portal-animation.md row C44), and 26.2 ClientLevel.tickEntities (mc262 ClientLevel.java:453-459) no longer touches block entities; ClientLevel.tick doesn't either (:299-339). Grep confirms ZERO tickBlockEntities call sites in the whole mod source. Result: block entities in the S14 nether view never client-tick — campfire smoke/particle tickers, spawner spin, chest lid animation, enchanting table books, bell swings are frozen through the portal window, and Level.pendingBlockEntityTickers never drains into blockEntityTickers for the secondary (unbounded pending accumulation, Level.java:465-470). Wrong behavior vs IP (IP portal views show animated block entities); does not crash. The port DID catch the sibling role-transfer for LevelRenderer.tick (ClientWorldLoader.java:151-157) but missed this one.
- IP ref: IP ClientWorldLoader.java:159 (newWorld.tickEntities() — on 1.21.3 this included the tickBlockEntities tail call). 26.2 hoist: mc262 Minecraft.java:1797-1799; mc262 ClientLevel.java:453-459; Level.java:465-485 (public, self-gated on tickRateManager().runsNormally() and shouldTickBlocksAt).
- fix: In ClientWorldLoader.tickRemoteWorld, add newWorld.tickBlockEntities(); immediately after newWorld.tickEntities(); (inside the same try/withSwitchedWorld block), mirroring 26.2 Minecraft.tick's entities->blockEntities pairing — a required 26.2 re-expression of IP's tickEntities semantics, not a deviation. Record in the port-note alongside the existing LevelRenderer.tick role-transfer comment.

#### REFUTED: t (auditor placeholder finding, a.java:1)
- refutation: Content-free placeholder: no file 'a.java' exists anywhere in the repo (Glob/Grep verified), the evidence field ('e') cites nothing, and no defect claim is stated. Nothing to confirm; the auditor report carries zero substantive findings. Its DEFECTS_FOUND verdict is coincidentally correct but unsupported.

## VERIFY result (verdict=DEFECTS_FOUND)
notes: Independent end-to-end re-trace of the packet-redirection + entity/world-sync link (the auditor report was an empty stub — everything below is from my own verification). SERVER half: EntitySync.update/tick byte-matches IP (profiler rename, toLong->pack) and is driven from ImmPtlChunkTracking.tick (:396/:399, same as IP :395/:398), init-wired flag-ON via SeamlessPortalsModFabric (MiscNetworking.init -> IPModMain.init at :91-93; PacketRedirection.init at IPModMain.java:69). All FOUR 26.2 entity-packet send paths are redirect-covered: sendToTrackingPlayers + the NEW sendToTrackingPlayersFiltered (per-send withForceRedirect), sendToTrackingPlayersAndSelf (sendRedirectedPacket), addPairing/removePairing (@Redirects, incl. the addPairing bundle — createRedirectedMessage is bundle-aware with a nesting guard), and everything else via the ambient force-redirect + MixinServerGamePacketListenerImpl_Redirect ModifyVariable, whose 26.2 retarget I verified line-exact (send(Packet,ChannelFutureListener) at ServerCommonPacketListenerImpl.java:160, 1-arg funnel :157, Connection.send 3-arg :281); ServerEntity.sendChanges has exactly one vanilla caller (ChunkMap.java:1203, HEAD-cancelled), so validateForceRedirecting cannot fire spuriously; the item-frame map-update send inside sendChanges (:109) is covered by the ambient wrap. CLIENT half: MixinClientboundCustomPayloadPacket implements the SPIKE-R7-proven §A shape (packetProcessor().isSameThread() guard handles the netty double-pass; netty pass scheduleIfPossible(outer)+cancel lands in the same FIFO vanilla re-queues use — drained before scheduledExecutables, Minecraft.java runTick — so order-faithful; main pass inline+cancel; Fabric never sees it; channelRead0 normal-return vs throw paths are behaviorally identical, Connection.java:146-165; RejectedExecutionException-after-close matches vanilla's own path). Bundles: handleBundlePacket re-queues whole then handles sub-packets on main (ClientPacketListener.java:2441-2447) -> mixin main branch inline; order preserved. handleRedirectedPacket -> withSwitchedWorldFailSoft -> getOptionalWorld -> getWorld CREATES the secondary ClientLevel on demand (IP behavior), with dim-type + R1 seaLevel from DimIdSyncPacket (sent in placeNewPlayer BEFORE ChangeDifficulty — before any redirected packet can exist; fail-soft fallback present); the swap set (mc.level, netHandler.level, particleEngine) covers every implicit current-dim consumer I enumerated in the touched 26.2 handlers (handleAddEntity/SetEntityData/SetEntityMotion/EntityPositionSync/TeleportEntity/LevelChunkWithLight/GameEvent all consume this.level; ClientLevel routes render dirties through its constructor-bound extractor per the EXTRACTOR-IDENTITY doctrine, so no extractor swap needed). Every ImmPtl mixin on the receive path is netty-double-run safe: all MixinClientPacketListener injects anchor AFTER ensureRunningOnSameThread (retargeted to the PacketProcessor 3-arg descriptor, verified present in all five targeted handlers) or at RETURN (unreachable on the netty pass); the one weave collision with a block-era ClientPacketListener mixin (W1) is suppressed by SeamlessMixinConfigPlugin flag-ON. MixinMinecraft_RedirectedPacket's scheduleExecutables @IPVanillaCopy re-derivation is exact (ReentrantBlockableEventLoop.java: runningTask() || super.scheduleExecutables(); Minecraft declares no own override; wrapRunnable survives at :2668); registration verified in the ACTIVE ip-client/ip-core-common mixins.json + fabric.mod.json. F1/WorldInfoSender: weather-only is the approved R13j forced deviation, correctly documented in-code; cadence (gameTime%100==42) and dim selection match IP; client apply verified — handleGameEvent writes this.level.setRainLevel/setThunderLevel (ClientPacketListener.java:1537-1578), i.e. the swapped secondary. VERDICT RATIONALE: no BLOCKER/MAJOR anywhere on the link — nothing found will crash or visibly break the S14 live test; DEFECTS_FOUND is driven solely by the one MINOR (debug-subscription entity bookkeeping gap, cheap fix or ledger entry) plus three NOTEs. If the parent workflow treats MINOR-only links as launchable, this link is launch-ready as-is.

#### [MINOR] MISSED ip_updateEntityTrackingStatus re-derivation misses 26.2's new debugSynchronizers entity bookkeeping
- file: common/src/main/java/qouteall/imm_ptl/core/mixin/common/entity_sync/MixinTrackedEntity.java:214
- evidence: The @IPVanillaCopy takeover of TrackedEntity.updatePlayer adds/removes pairings directly (seenBy.add(player.connection) -> addPairing at :214-221; seenBy.removeIf -> removePairing at :194-206) without the bookkeeping vanilla 26.2 added to the same code: ChunkMap.java:1394-1400 (registerEntity when seenBy.size()==1 + startTrackingEntity(player, entity) on every pairing-add) and ChunkMap.java:1374-1381 (removePlayer's dropEntity when seenBy empties). With updatePlayer/updatePlayers @Overwrite'd to no-ops and ChunkMap.tick() HEAD-cancelled, these events NEVER fire for any entity. The port's own chunk-side twin treats exactly this omission class as a defect and replays the new vanilla tail (MixinChunkMap_C.onChunkReadyToSend :86-91 replays onChunkReadyToSend + debugSynchronizers().registerChunk with an explicit javadoc saying a blind no-op 'would suppress the broadcast-set registration') — the entity-side equivalent was not replayed. Impact is confined to the opt-in vanilla debug-subscription system (TrackingDebugSynchronizer/LevelDebugSynchronizers: incremental register/startTracking/drop events lost; subscription-start resync via forEachEntityTrackedBy still works because seenBy is maintained). Zero effect on the S14 live test or normal play.
- IP ref: IP 1.21.3 MixinTrackedEntity.ip_updateEntityTrackingStatus (identical body — the calls did not exist in 1.21.3 vanilla; this is a 26.2-base re-derivation gap, same class as the R12 're-derive every @IPVanillaCopy line-by-line from 26.2' rule and the MixinChunkMap_C precedent)
- fix: In ip_updateEntityTrackingStatus: in the add branch, after this.serverEntity.addPairing(player), replay `if (seenBy.size() == 1) level.debugSynchronizers().registerEntity(entity);` and `level.debugSynchronizers().startTrackingEntity(player, entity);` (matching ChunkMap.java:1394-1400); in the removeIf branch, after removePairing, replay `if (seenBy would become empty) level.debugSynchronizers().dropEntity(entity);` (matching :1377-1379 — compute on the post-removal size). Alternatively record it as an accepted deviation in the polish ledger with the debug-only impact stated.

#### [NOTE] MISSED EntitySync.tick gate omits 26.2's new entity.needsSync clause (IP-faithful; ledger-worthy vanilla divergence)
- file: common/src/main/java/qouteall/imm_ptl/core/chunk_loading/EntitySync.java:63
- evidence: The port gates ip_sendChanges() on distanceManager.inEntityTickingRange(chunkPos) only. Vanilla 26.2's replaced loop gates on `sectionPosChanged || trackedEntity.entity.needsSync || inEntityTickingRange` (ChunkMap.java:1202); needsSync is a NEW 26.2 Entity flag (Entity.java:274, set on hurt-marking/projectile deflection/etc., consumed and cleared in ServerEntity.sendChanges :123/:219). Consequence: an entity in a loaded but non-entity-ticking chunk that sets needsSync will not flush changes until it enters ticking range — in ALL dimensions, since EntitySync replaces vanilla wholesale. This exactly preserves IP 1.21.3 semantics (IP already dropped the 1.21.3 `sectionPosChanged ||` clause from the same gate), so under the IP-wins discipline it is NOT a defect — but it is an inherited-plus-widened vanilla divergence that should be recorded in the deviations/polish ledger.
- IP ref: IP EntitySync.java:61-64 (gate is inEntityTickingRange only; 1.21.3 vanilla ChunkMap.tick gate was sectionPosChanged || inEntityTickingRange)
- fix: No code change for S14 (IP fidelity). Add a ledger entry; optionally revisit at S15 (entity traffic stage) whether to add `|| ieTrackedEntity.ip_getEntity().needsSync` to the gate as a documented vanilla-26.2-parity addition.

#### [NOTE] MISSED ChunkMap.tick() HEAD-cancel silently depends on the applyChunkTrackingView cancel (hidden coupling, currently sound)
- file: common/src/main/java/qouteall/imm_ptl/core/mixin/common/entity_sync/MixinChunkMap_E.java:82
- evidence: onTickEntityMovement cancels ALL of ChunkMap.tick(). On 1.21.3 that method was entity-tracking only; on 26.2 it ALSO opens with the relocated per-player chunk-tracking refresh `for (player : playerMap.getAllPlayers()) updateChunkTracking(player)` (ChunkMap.java:1181-1184; updateChunkTracking :1097-1107 was called from move() in 1.21.3). The suppression is currently harmless ONLY because updateChunkTracking's sole effect is applyChunkTrackingView (:1105), which MixinChunkMap_C.onUpdateChunkTracking (:61-70) HEAD-cancels anyway (PlayerChunkLoading replaces vanilla chunk sending). Two independent cancels thus mask each other; if the applyChunkTrackingView cancel is ever narrowed or removed (e.g., during S20 cleanup or a vanilla-parity pass), the tick()-cancel would silently strand player ChunkTrackingViews at EMPTY. Verified end-to-end sound for S14.
- IP ref: IP MixinChunkMap_E.java:82-85 (identical cancel; 1.21.3 tick() contained no chunk-tracking loop to suppress)
- fix: Document the coupling in both mixins' javadoc (one line each: 'tick()-cancel also suppresses the 26.2-relocated updateChunkTracking loop; covered by MixinChunkMap_C.onUpdateChunkTracking — keep in lockstep'). No behavior change needed.

#### [NOTE] MISSED Redirection payload codec (encode/decode on netty thread) still runtime-unexercised before S14 — singleplayer passes packets by reference
- file: common/src/main/java/qouteall/imm_ptl/core/network/PacketRedirection.java:252
- evidence: Payload.CODEC via PLACEHOLDER_PROTOCOL_INFO (GameProtocols.CLIENTBOUND_TEMPLATE.bind, :234-241 — the single most version-sensitive line, ported unchanged and compiling) only executes on remote connections; singleplayer local channels deliver the packet object by reference, so S13 rung-1's live proof of the transport (which did exercise wrap/handleRedirectedPacket/withSwitchedWorldFailSoft, since EntitySync wraps ALL entity packets in withForceRedirect even same-dim) never ran the wire codec, and S14 singleplayer runClient won't either. This is SPIKE-R7-requeue §6's recorded honest residue; the threading path probed is byte-identical for remote connections. First dedicated-server session is the first live exercise of inner-packet encode/decode on the netty thread.
- IP ref: SPIKE-R7-requeue.md §6 (unproven/out-of-scope list); API_RISKS R7 ('the redirection wire format itself survives 1:1')
- fix: No S14 action. Keep the existing plan item: exercise a dedicated-server session (S13+ scripts / before S17 default flip) and watch for codec errors on the 'i:r' channel.

#### REFUTED: test
- refutation: The auditor report is a parse-test stub, not an audit: the single finding names file 'a.java' which does not exist anywhere in the repo (verified by glob), its evidence/'proposedFix' are single letters, and the summary literally says 'Test summary to isolate the findings parse failure'. No trace evidence exists behind its READY verdict. Discarded in full; the link was re-audited from scratch below.

## VERIFY result (verdict=DEFECTS_FOUND)
notes: Independent re-trace performed from source on both sides (ported tree + mc262-ref + block-era driver + IP-era design docs). Both auditor findings CONFIRMED (none refuted; the MAJOR gains the un-drained SubmitNodeStorage accumulation and the in-repo self-documentation at PerEntityClipBracket.java:49-51/372-390 and MixinPreparedFrame.java:32-34 as additional proof). Auditor sub-claims spot-verified true: SectionBufferBuilderPool.allocate Math.max(1,...) clamp (:24-25 — though acquire() returning null → the caught NPE IS the requeue mechanism, so \"no acquire() NPE\" is loosely worded but the no-crash conclusion holds); renderGroup output-target fallback for secondaries (ChunkSectionLayerGroup.java:30-38: TRANSLUCENT reads mc.levelRenderer.translucentTarget() — null on the swapped-in secondary → mainRenderTarget; OPAQUE → mainRenderTarget directly); sectionUpdateTracker AW present (common/src/main/resources/seamlessportals.accesswidener:14); e817b57 DEFECT-1 routing correct pre-crossing (RenderStates.originalPlayerDimension = cameraEntity.level().dimension() at updatePreRenderInfo:144); coherence re-point no-op by construction (extractor+renderer share one LRS, ClientWorldLoader.java:534-540); LevelRenderState.reset() (:34-46) does NOT touch cameraRenderState so the core's Step-4-mutations-before-Step-5-extract order is safe; extract's shared EntityRenderDispatcher.prepare re-point is restored by the shell (MyGameRenderer.java:374-378, IP-faithful); the upload half is covered flag-ON (MyRenderHelper.earlyRemoteUpload:302-322 iterates WORLD_EXTRACTOR/RENDERER maps for every non-active dim); ImmPtlViewArea reposition is the IP coord-pinned non-destructive preset swap; SOG delta-window identity guard composes across flips and A→B→A nesting. The verdict driver is the missed BLOCKER: the flag-ON cross-dim crossing (never executed at rung 1 — both changePlayerDimension call sites are fromDim!=toDim-gated) lacks the entire 26.2 extract-driver promote/demote that the corpus itself declared CRITICAL PORT-FORWARD top-risk §11.1; S14's own test script (EXECUTION_PLAN.md:1248 step 2 \"Cross into the nether\") hits it on the first walk-through. Recommended fix order for the S14 audit cycle: (1) BLOCKER promote/demote re-derivation (design-task-sized, weave-audit + Fable-verify gates), (2) auditor's MAJOR (dedicated FeatureRenderDispatcher + endFrame lifecycle), (3) missed MAJOR (dispatcher construction-buffers identity — also fixes the frame-1 empty window), (4) the two NOTEs ride along. The dest-clouds/weather/world-border omissions are ledgered deviations (not defects); no exclusivity/double-drive violation found on this link — the block-era drivers are correctly gated, which is precisely why their promote plumbing needs a flag-ON replacement.

#### [MAJOR] CONFIRMED Cross-dim portal views render NO entities/block-entities/particles — the secondary shares the main FeatureRenderDispatcher whose single PreparedFrame is open during AFTER_TRANSLUCENT_TERRAIN (plus an unbounded SubmitNodeStorage accumulation the auditor missed)
- file: common/src/main/java/qouteall/imm_ptl/core/ClientWorldLoader.java:540
- evidence: Independently re-verified every link: (1) 26.2 LevelRenderer ctor binds featureRenderDispatcher = gameRenderer.featureRenderDispatcher() (mc262-ref LevelRenderer.java:146) and createSecondaryClientWorld (ClientWorldLoader.java:523-540) isolates ONLY the LevelRenderState — the dispatcher stays the ONE main instance. (2) FeatureRenderDispatcher owns a single PreparedFrame (mc262 FeatureRenderDispatcher.java:35); begin() throws IllegalStateException("PreparedFrame already in use") when context!=null (:187-190), and it is the FIRST statement of prepareFrameWithContext (:78) — before drainPhases. (3) The main pass opens the frame at LevelRenderer.render:176 and closes it at :253, with frame.execute at :239 in between; the flag-ON dispatch fires from Fabric LevelRenderEvents.AFTER_TRANSLUCENT_TERRAIN (fabric/src/main/java/com/warwa/seamlessportals/fabric/SeamlessPortalsClientFabric.java:103-115) inside that execute — two in-repo runtime proofs that the frame is open there: the block-era comment "the main renderer's buffers are in use during AFTER_TRANSLUCENT_TERRAIN" (PortalWorldManager.java:573-577) and PerEntityClipBracket.java:375-377 ("its single PreparedFrame is in use during the main pass; begin() throws"). (4) SecondaryWorldRenderCore.renderPortalEntities (cross-dim only, :492-496) calls the shared dispatcher's renderAllFeatures (:699) → deterministic throw, swallowed by the log-less catch(Throwable) at :703-705. (5) Worse than the auditor stated: invokeSubmitFeatures (:694) fills the dest renderer's own SubmitNodeStorage (per-renderer, LevelRenderer.java:107) and the throw happens BEFORE drainPhases, so the storage is never drained — it accumulates submit nodes EVERY cross-dim frame with entities in view (unbounded growth for the dim's lifetime), on top of destLRS.entityRenderStates being extracted, cleared (submitFeatures clears at LevelRenderer.java:282/284/286) and never drawn. (6) The implementation violates its own design contract: S13H-driver-core-design.md §1 Step 10.8 states "the dest renderer owns its OWN SubmitNodeStorage/FeatureRenderDispatcher/PreparedFrame" and MixinPreparedFrame.java:32-34 assumes "a nested dest LevelRenderer pass uses a DIFFERENT FeatureRenderDispatcher/PreparedFrame instance" — the SubmitNodeStorage is own, the dispatcher is NOT. Severity MAJOR confirmed (no crash — the throw is swallowed; wrong behavior + IP divergence + per-frame memory growth; may be invisible on a bare fresh-nether FLAG_LOADING first light).
- IP ref: IP 1.21.3 secondary LevelRenderers each carried their own render state/dispatch (4-arg ctor); the qouteall port re-pointed only LevelRenderState. The 26.2-proven form is the block-era separate RenderBuffers+FeatureRenderDispatcher (PortalWorldManager.createRenderer:578-619, accessor LevelRendererAccessorMixin.java:98-101 kept precisely "to restore that isolation"); design contract S13H-driver-core-design.md §1 Step 10.8 / §6.1 ("Cross-dim portals render entities fully").
- fix: Mirror the runtime-proven block-era isolation (PortalWorldManager.createRenderer:578-619: separate RenderBuffers(4) + FeatureRenderDispatcher "has mutable per-frame state", installed via the accessors) in createSecondaryClientWorld right after the LevelRenderState re-point (~:536): RenderBuffers destBuffers = new RenderBuffers(4); FeatureRenderDispatcher destDisp = new FeatureRenderDispatcher(destBuffers, CLIENT.getModelManager(), CLIENT.getAtlasManager(), CLIENT.font, CLIENT.gameRenderer.gameRenderState()); ((LevelRendererAccessorMixin) worldRenderer).seamlessportals$setFeatureRenderDispatcher(destDisp); — ctor signature verified against mc262 FeatureRenderDispatcher.java:37-58 and the identical live call at PortalWorldManager.java:584-590. MUST add the per-frame endFrame for destBuffers' StagedVertexBuffer (memory gpu-buffer-leak-endframe): extend MyGameRenderer.endFramePooled or a ClientWorldLoader-side endFrame walk over mod-created buffers (the block-era counterpart is PortalWorldManager.endSecondaryRenderFrames:1993-2027), and close destBuffers/destDisp in disposeWorldRenderer/disposeDimensionDynamically. Do NOT share the main RenderBuffers into the new dispatcher: PreparedFrame.close() calls stagedVertexBuffer.endDraw() (FeatureRenderDispatcher.java:302) which would end the MAIN pass's staged draw mid-frame.

#### [NOTE] CONFIRMED First cross-dim frame renders an empty terrain window — viewArea (and the SectionRenderDispatcher) don't exist until the first in-pass extract builds them
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:231
- evidence: Verified end-to-end: createSecondaryClientWorld:621 worldExtractor.setLevel(newWorld) → LevelExtractor.setLevel (mc262 LevelExtractor.java:393-404) sets shouldResetLevelRenderData=true and allChanged() sets shouldInvalidateCompiledGeometry=true — both are only CONSUMED inside extract() (:105-108, :122-124), which for a secondary first runs at SecondaryWorldRenderCore Step 5 (:350). So on frame 1 the core reads viewArea=null at :231-232 (guards at :286 and :434 skip reposition + discovery) AND destRenderer.sectionRenderDispatcher()==null at :292-294 (setCameraPosition skipped — an additional frame-1 detail the auditor missed, also benign: visibleSections is empty so nothing draws). The ViewArea is created inside extract → invalidateCompiledGeometry (LevelRenderer.java:796-832) through the R4 redirect (qouteall MixinLevelRenderer.java:111-139, gated on IPCGlobal.useHackedChunkRenderDispatcher) → ImmPtlViewArea, so the (ImmPtlViewArea) cast at :231 is safe from frame 2 on and a null cast is a no-op. ImmPtlViewArea.repositionCamera is the IP coord-pinned preset swap (non-destructive to compiled meshes — verified in ImmPtlViewArea.java:50-96), so the per-frame :286-288 reposition is not a mesh-wipe hazard. Benign async cold-start, correctly filed as a NOTE so a 1-frame-empty (in practice multi-second fade-in per the graduated-loading model) nether window is not misread at the S14 launch.
- IP ref: IP 1.21.3 LevelRenderer.setLevel→allChanged built the dispatcher+grid EAGERLY at world creation; 26.2 defers both into the first extract (LevelExtractor.java:105-124) — the deferral, not the port, causes the empty first frame.
- fix: No correctness change required. If frame-1 population is wanted, re-read viewArea (and the dispatcher, for setCameraPosition) after Step 5's extract; note the eager-creation fix proposed for the missed pooled-RenderBuffers finding below would ALSO make frame 1 fully populated — prefer solving both together.

#### [BLOCKER] MISSED NO 26.2 extract-driver promote/demote at the flag-ON cross-dim crossing — changePlayerDimension swaps mc.level/mc.levelRenderer but mc.levelExtractor stays bound to the FROM world and the boot renderer, visibly breaking the main render on the first walk-through (S14 test step 2)
- file: common/src/main/java/qouteall/imm_ptl/core/teleportation/ClientTeleportationManager.java:499
- evidence: The link mission names "promote/demote at a cross-dim crossing"; the auditor never traced it. changePlayerDimension (:459-535, reached ONLY when fromDimension != toDimension — both call sites :351/:441 — so S13 rung-1 same-dim crossings NEVER executed it) does the IP-verbatim swap: client.level = toWorld (:499) + ip_setWorldRenderer(getWorldRenderer(toDimension)) (:500-502, MixinMinecraft.java:222-224 = a bare field write). NOTHING re-points the extract driver: vanilla drives the per-frame extract on Minecraft.levelExtractor (GameRenderer.extract:389, mc262), a public FINAL field bound once at Minecraft.java:649 to the BOOT main renderer + gameRenderState().levelRenderState, whose private level field still holds the FROM world (setLevel is never called). Post-crossing frame: extract() extracts the OVERWORLD (entities, sky, chunk-delta flip consumed from the OW ClientChunkCache, one-shot dirty flags consumed) into the main LRS driving the BOOT renderer's visibleSections — while GameRenderer.renderLevel:563-565 renders mc.levelRenderer = the NETHER SECONDARY from ITS OWN isolated LRS (re-pointed at creation, ClientWorldLoader.java:534-536), whose cameraRenderState/entity states are only ever portal-pass-fed (stale transformed matrices) and whose visibleSections nothing repopulates for a main view. This is byte-for-byte the block era's PROVEN failure: "swapping only mc.levelRenderer leaves extract() populating the OLD renderer's visibleSections while render() uses the promoted one with 0 → blank terrain after teleport (PROVEN: log DIAG #1)" (PortalWorldManager.java:1145-1152). Knock-on identity corruption: WORLD_EXTRACTOR_MAP seeds the boot dim's entry AS CLIENT.levelExtractor (ClientWorldLoader.java:484), so after crossing, e817b57's routing invariant ("originalPlayerDimension ↔ mc.levelExtractor; every other dim ↔ its map instance") is broken from both ends: an OW-view portal from the nether resolves destExtractor = CLIENT.levelExtractor via the map → destLRS = main LRS → sharedState=true → the whole Step-5 extract/SOG/drain silently skipped for a genuinely-cross-dim portal; and a nether-view portal resolves mc.levelExtractor whose level is the OW. The block-era solution exists and is fully characterized — promote: raw accessor re-points on mc.levelExtractor (setLevelRenderer/setLevel/setLastViewDistance/setSectionUpdateTracker, NOT setLevel() which allChanged-wipes meshes; PortalWorldManager.java:1145-1184), level.levelExtractor → mc.levelExtractor ITSELF (:1196-1197, memory nether-block-freeze-orphaned-extractor), promoted renderer's LRS → the shared main LRS, SOG re-prime + clearVisibleSections + compile-guard clear (:1047-1121); demote: the reverse per-dim re-point — but it is SUPPRESSED flag-ON (EXCLUSIVITY_LEDGER rows 12/16) and its replacement never received the plumbing. The migration corpus itself declared this exact gap: current-mod-core.md §5 PortalWorldManager row — "CRITICAL PORT-FORWARD (no IP source): the 26.2 render-split plumbing — mc.levelExtractor field re-pointing, SectionUpdateTracker identity, lastViewDistance sync ... must be re-derived INTO the ported ClientWorldLoader (top risk §11.1)" — and §11.1 "the mod's promote/demote extractor plumbing (PortalWorldManager.java:1145-1197, :1688-1693, :1993-2027) has no IP source and must be re-derived". It was never re-derived; no port-note or ledger entry registers it as an approved deviation. S14's live script step 2 is "Cross into the nether; play 2 minutes; break/place blocks; cross back" (EXECUTION_PLAN.md:1248) — this fires on the first crossing.
- IP ref: IP 1.21.3 ClientTeleportationManager.changePlayerDimension:458 needed only the mc.level/mc.levelRenderer swap because 1.21.3 renderLevel read those fields directly; 26.2 split the extract driver onto the final Minecraft.levelExtractor (Minecraft.java:280/649, GameRenderer.extract:389), so the faithful re-expression REQUIRES the promote/demote plumbing — proven live in the block era (PortalWorldManager.java:1145-1197 + demote :1688-1693), declared CRITICAL PORT-FORWARD top-risk §11.1 in migration/api-map/current-mod-core.md, suppressed flag-ON per EXCLUSIVITY_LEDGER rows 12/16 with the replacement never wired.
- fix: Re-derive the block-era-proven promote/demote into the flag-ON crossing (in ClientWorldLoader as the api-map prescribes, invoked from changePlayerDimension after the :499-502 swap). PROMOTE(toDim): via LevelExtractorAccessor raw re-points on mc.levelExtractor — seamlessportals$setLevelRenderer(WORLD_RENDERER_MAP[toDim]), $setLevel(toWorld), $setLastViewDistance(options.getEffectiveRenderDistance()) (blocks the extract:96-98 allChanged mesh-wipe trip), $setSectionUpdateTracker(the per-dim extractor's current tracker); re-point the promoted renderer's levelRenderState → gameRenderState().levelRenderState (LevelRendererAccessorMixin.seamlessportals$setLevelRenderState) so extract-writes and render-reads share one object; re-point toWorld's ClientLevel.levelExtractor → mc.levelExtractor ITSELF (ClientLevelExtractorAccessor — KEEP'd for exactly this, current-mod-core.md §9); update WORLD_EXTRACTOR_MAP[toDim]; clear SecondaryWorldRenderCore.portalCompileScheduled/lastAppliedDeltaWindow for toDim; re-prime the promoted renderer (clearVisibleSections + SOG invalidate + needsFrustumUpdate force — PortalWorldManager.java:1057-1121 — coordinated with IP's vanillaTerrainSetupOverride which the crossing already sets). DEMOTE(fromDim): give the demoted (boot) renderer a fresh isolated LevelRenderState, bind a per-dim extractor for fromDim (raw level/tracker writes, never setLevel), re-point fromWorld.levelExtractor → that per-dim extractor, update WORLD_EXTRACTOR_MAP[fromDim] (the demote-side half of memory nether-block-freeze-orphaned-extractor). This is a design-task-sized change: run it through the S13-C weave-audit + Fable-verify gates before the S14 launch; S14 CANNOT go READY without it (or without explicitly descoping walk-through from the rung-2 script, which contradicts EXECUTION_PLAN S14(d) items 2-4/7-9).

#### [MAJOR] MISSED Cross-dim SectionRenderDispatcher is constructed DURING the portal pass and permanently captures the transient pooled RenderBuffers(0) — a 1-pack section-builder pool instead of IP's full shared pool, plus a pool-object identity leak
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:350
- evidence: Sequence verified from source: the shell resolves/creates the dest world+renderer at MyGameRenderer.java:206 (dispatcher NOT created — 26.2 defers it), then swaps the dest renderer's renderBuffers field AND GameRenderer.renderBuffers to a pooled `new RenderBuffers(0)` at :279-289 (acquireRenderBuffersObject :141-153, IPGlobal.useSecondaryEntityVertexConsumer=true default, IPGlobal.java:82) BEFORE the invoke. The first-ever extract for the secondary then runs inside the invoke (SecondaryWorldRenderCore.java:350) → consumes shouldResetLevelRenderData (disposing any pre-existing dispatcher, LevelExtractor.java:105-108 → resetLevelRenderData LevelRenderer.java:878-891) → consumes shouldInvalidateCompiledGeometry (:122-124) → invalidateCompiledGeometry constructs `new SectionRenderDispatcher(Util.backgroundExecutor(), this.renderBuffers, ...)` (LevelRenderer.java:804-807) where this.renderBuffers == the POOLED RenderBuffers(0). The dispatcher permanently captures its fixedBufferPack + sectionBufferPool (SectionRenderDispatcher.java:64-65); RenderBuffers(0) → SectionBufferBuilderPool.allocate clamps to Math.max(1, min(0, maxBuffers)) = 1 pack (SectionBufferBuilderPool.java:23-25). Consequences: (a) every async section compile for the nether view serializes on ONE SectionBufferBuilderPack forever (contending workers spin the acquire-NPE-requeue path, SectionRenderDispatcher.java:90-102) — a fresh nether at render distance N has hundreds of sections, so the window fills far slower than IP; (b) the pooled object is returned to MyGameRenderer.secondaryRenderBuffers (:363-367) and reused as the active swap buffer of every later portal pass — and a SECOND cross-dim secondary created later inside a pass captures the SAME 1-pack pool, coupling all secondaries' compile pipelines to one transient object; (c) IP divergence: IP 1.21.3's secondary allChanged ran eagerly at world creation (before any pooled swap) with the renderer's construction RenderBuffers — the main shared pool at full concurrency; the block era likewise gave secondaries RenderBuffers(4) at creation (PortalWorldManager.java:578) and its pool used BUFFER_BUDGET=4 (PortalRenderBuffersPool.java:50). S13 rung 1 never saw this (the main renderer's dispatcher predates any swap); it fires exactly and only on the never-run cross-dim path — the constructor/init-landmine class the mission names. Functional (compiles still complete; the NPE is caught) — MAJOR, not BLOCKER.
- IP ref: IP 1.21.3 ClientWorldLoader passed client.renderBuffers() (the main instance) to the 4-arg secondary LevelRenderer ctor and setLevel→allChanged created the dispatcher EAGERLY from it at creation time, outside any portal pass; MyGameRenderer's pooled RenderBuffers(0) swap (B5 verbatim fold) only ever fed 1.21.3 entity bufferSources, never dispatcher construction. 26.2's creation deferral into the first extract (LevelExtractor.java:95-124) moves construction inside the swap window — the re-expression must restore IP's construction-buffers identity.
- fix: Ensure the dest dispatcher is created with the renderer's CONSTRUCTION RenderBuffers (IP semantics: secondaries share the main RenderBuffers' section pool). Cleanest: in MyGameRenderer.switchAndRenderTheWorld, skip (or defer) the :283 ip_setRenderBuffers pooled swap of the DEST renderer's field when `worldRenderer.sectionRenderDispatcher() == null` (first frame for a new secondary) — one pass on the construction buffers is exactly IP's permanent arrangement, and the pooled swap resumes from frame 2. Alternative: eagerly create the dispatcher+grid in createSecondaryClientWorld (call worldRenderer.invalidateCompiledGeometry(newWorld, CLIENT.options, CLIENT.gameRenderer.mainCamera(), CLIENT.getBlockColors()) after :626) AND clear the extractor's shouldResetLevelRenderData/shouldInvalidateCompiledGeometry one-shots via two new LevelExtractorAccessor rows — without clearing BOTH flags the first in-pass extract disposes and recreates the dispatcher under the pooled buffers again (LevelExtractor.java:105-108/122-124), which is why the eager call alone is insufficient. Either fix also populates the first cross-dim frame (see the confirmed NOTE). If the auditor's FeatureRenderDispatcher fix installs a dedicated RenderBuffers on the secondary, that does NOT cover this defect — the renderer field is still pool-swapped before the first extract.

#### [NOTE] MISSED Step-2 tracker read goes stale within the pass when extract() trips its render-distance allChanged — armed discovery then arms a REPLACED SectionUpdateTracker for that frame
- file: common/src/main/java/qouteall/imm_ptl/core/render/SecondaryWorldRenderCore.java:233
- evidence: `sut = destExtractor.sectionUpdateTracker` is read at :233, BEFORE Step 5's extract (:350). If the user changed the effective render distance since the extractor's lastViewDistance, extract() immediately calls allChanged() (LevelExtractor.java:96-98), which REPLACES sectionUpdateTracker with a new instance (:411) — the design's own identity rule (S13H-driver-core-design.md §2.1: "recreated by allChanged inside the extractor — always re-read from the extractor, never cache across frames") is honored across frames but violated WITHIN the pass. Step 9 then arms VisibleSectionDiscovery with the dead tracker (:438-439), so that frame's compile scheduling consults stale dirty states; the next pass re-reads and self-heals. One-frame wrongness on a settings-change frame only (the first frame is safe: setLevel-time allChanged already synced lastViewDistance). Same class of hazard as the shipped nether-block-freeze bug but transient — NOTE.
- IP ref: No 1.21.3 analog (SectionUpdateTracker is 26.2-only); the governing rule is the port's own §2.1 identity row derived from memory nether-block-freeze-orphaned-extractor (tracker identity is everything; allChanged REPLACES the tracker).
- fix: Move the `sut` read (or re-read it) after Step 5's extract, immediately before the Step-9 arm: `SectionUpdateTracker sut = destExtractor.sectionUpdateTracker;` at :434. One-line hardening, no behavior change in steady state.

## COMPLETENESS CRITIC
assessment: Coverage of the cross-dim chain is substantially complete: walking EXECUTION_PLAN S14(d) items 1-9 against the nine tracks, every item maps to audited code or to a verified symmetry/triviality argument. Items verified in THIS pass (no gap): (i) GPU endFrame pairing on the cross-dim path — GameRendererMixin.seamlessportals$endSecondaryFrames (common/src/main/java/com/warwa/seamlessportals/mixin/client/GameRendererMixin.java:38-60) runs at GameRenderer.render TAIL in BOTH flag states and drains PortalRenderBuffersPool.endFramePooled + (flag-ON) qouteall MyGameRenderer.endFramePooled, so the recorded gpu-buffer-leak-endframe root cause is NOT regressed; (ii) /portal debug report_chunk_loaders exists (PortalDebugCommands.java:293-308, a trivial foreachBaseChunkLoaders dump over the chunkloading-audited chain) and all IP commands register flag-ON via IPModMain.java:104; (iii) portal creation for S14 is by COMMAND (make_portal — teleport-track-audited), NOT nether-portal ignition (that is S16), so no generation-chain gap; (iv) HandleRespawnMixin is deliberately ungated (EXCLUSIVITY_LEDGER B4) but flag-ON both its promotion branches require PortalWorldManager caches that are empty flag-ON (HandleRespawnMixin.java:382-405, 443-447 fall through to vanilla ctor + mc.setLevel), and the port's 2-arg updateLevelInEngines funnel (MixinMinecraft.java:188-207) then fires ClientWorldLoader.cleanUp — IP-identical death-respawn semantics; (v) SeamlessClientChunkMap has NO flag-ON installer (only PortalWorldManager.java:671 and the dead promotion branch), so no chunk-map identity collision with ImmPtlClientChunkMap; (vi) items 4 (negative coords), 6 (weather state), 7 (long-walk/C8), 9 (relog-into-nether) ride audited machinery (chunk pack/unpack, F1 weather-only, purge lifecycle, dim-symmetric login chains). All three S14 watch items (C8, R10 FLAG_SIMULATION, MixinFabricInvalidateRenderStateCallback) are covered by track findings, and the other recorded root causes (extractor identity, isSameThread, lateUpdateLight, crossing-resend storm) are each re-audited on the cross-dim path by at least one track. The real completeness exposure is not a missing chain link but the FIX STAGE: the digest contains the same two BLOCKER defects filed twice with materially divergent fix prescriptions (extract-driver promote/demote: drivercore vs teleport; ClientClockManager: clientworld vs ticklight) plus a lateUpdateLight fix pair where one track's proposed variant trips the latent identity bug the other track documented — implementing these as independent per-track patches risks a double-fix or a wrong-variant fix that itself breaks the S14 launch. Secondary gaps: the scripted rain test (item 6) fires the fog track's rainFogMultiplier MAJOR during the script (the verifier assumed clear weather and deprioritized it), and the script's cross-back dependency (items 2-4) rests on complete_bi_way_portal/goback whose cross-dim leg no track audited. Verdict: no additional BLOCKER-class chain link is missing; one MAJOR coordination gap, two MINORs, two NOTEs filed below.

#### GAP [MAJOR] The six render-identity fixes must be reconciled into ONE coordinated change set — the digest's duplicate BLOCKER filings carry divergent, partially conflicting fix prescriptions
- whatToAudit: Before any fix lands, merge into a single design doc and resolve three prescription conflicts explicitly: (1) extract-driver promote/demote — drivercore's fix (LevelExtractorAccessor raw re-points + lastViewDistance sync + WORLD_EXTRACTOR_MAP updates BOTH directions + ClientLevel.levelExtractor identity re-points + SOG re-prime/clearVisibleSections/compile-guard clears + demote-side fresh isolated LRS, coordinated with vanillaTerrainSetupOverride) is a strict superset of teleport's; implement drivercore's shape, housed in ClientWorldLoader, PLUS teleport's missed-MAJOR tail (client.gameRenderer.setLevel(toWorld) for camera level + cardinal lighting) and teleport's MINOR (ip_setWorld not particleEngine.setLevel), all invoked from ClientTeleportationManager.changePlayerDimension after :499-502. (2) ClientClockManager — clientworld's tickTime-HEAD-cancel and ticklight's WrapOperation are equivalent for the clock corruption, but ticklight's part (b) — seeding the secondary's absolute gameTime at creation (createSecondaryClientWorld) — is load-bearing on its own: without it dest-world gameTime consumers (portal-animation timing, %20 expiry windows) run 0-based; whichever mixin shape is chosen, the seed must ship too. (3) lateUpdateLight — implement clientworld's frame-END gate change (world != Minecraft.getInstance().level in MyRenderHelper.java:281), NOT ticklight's first option (poll+run inside the dest pass), which trips the latent getWorldExtractor short-circuit identity bug clientworld documented at ImmPtlClientChunkMap.java:302 unless that is fixed simultaneously. Also audit the knock-ons of drivercore's FeatureRenderDispatcher isolation fix: (a) the new per-secondary RenderBuffers MUST be added to the flag-ON endFrame walk (extend MyGameRenderer.endFramePooled or a ClientWorldLoader walk — GameRendererMixin.java:38-60 is the host) and closed in disposeWorldRenderer/cleanUp; (b) decide the promoted renderer's dispatcher identity at the promote/demote cutover (a promoted secondary would carry its isolated dispatcher into the main-pass role while vanilla paths read gameRenderer.featureRenderDispatcher()); (c) the fix first-brings-live PerEntityClipBracket's multi-storage PassState machinery (PerEntityClipBracket.java:120-177 — statically designed for exactly this, never live; note passStates/phaseRegistry entries for disposed storages are never evicted — bounded, but confirm at dispose). Run the merged set through the S13-C weave-audit + Fable-verify gates as one commit series.
- why: Four of the digest's five launch-gating defects were filed twice by different tracks with different fixes. Implemented independently: a double-fix on tickTime (HEAD-cancel AND WrapOperation) or on the extractor re-point corrupts state differently than the original bug; taking ticklight's in-pass light-run variant re-creates the S13-H DEFECT-1 extractor mis-routing under world swap; taking clientworld's clock fix without ticklight's gameTime seed leaves dest-world time 0-based; installing the isolated dispatcher without the endFrame walk regresses the recorded gpu-buffer-leak (multi-second GL stalls). Any of these would consume an S14 launch attempt on a defect manufactured by the fix stage itself.

#### GAP [MINOR] Scripted test item 6 (/weather rain) fires the fog track's un-fixed rainFogMultiplier leak DURING the S14 script — the verifier's 'clear-weather S14 won't show it' deprioritization is wrong for this script
- whatToAudit: Reconcile the fog track's M2 (AtmosphericFogEnvironment.rainFogMultiplier shared-static leak, SecondaryWorldRenderCore.java:384 Step 6 — the CUTOVER_SPEC §3 item 3 snapshot/restore never implemented) against S14-CROSS-DIM-TEST.md step 6, which has the user run /weather rain and observe through the window from the nether (raining OW = the DEST pass) — exactly the trigger condition. Either pull the M2 fix into the pre-launch set (fog track already prescribes the accessor + per-dim map form), or amend the script: add a PRE-ANSWERED entry ('rain-fog intensity may flicker/weaken while a portal is on screen during rain — known audited defect M2, fix queued') so the user doesn't file it as a new bug. While editing the script, also close its pre-answer 6 '[PENDING AUDIT]' marker — the packets track confirmed F1 weather-only with handleGameEvent writing the swapped secondary — and append the audit-outcomes section the script header promises.
- why: The digest's own remediation ordering says fix M2 'before sign-off' on the assumption clear-weather testing won't surface it; the governing test script contradicts that assumption at step 6. Left unreconciled, the first S14 run either burns a diagnosis cycle on a known defect or the user observes unstable main-world fog and (per the confirm-visual-interpretations rule) the session stalls on interpreting it.

#### GAP [MINOR] The return-crossing mechanism for script items 2-4 (complete_bi_way_portal / goback cross-dim) was audited by no track, and the script does not name which to use
- whatToAudit: make_portal is one-way/one-sided (script pre-answer 8, teleport-track-confirmed), so 'cross back' in items 2-4 requires /portal complete_bi_way_portal (PortalCommand.java:539) or /portal goback (PortalCommand.java:1456). Trace the cross-dim leg of PortalManipulation.completeBiWayPortal (:79) → createReversePortal (:88): dest ServerLevel resolution for the reverse portal (must be server.getLevel(portal.dimensionTo) = the nether, not the command source's level), the reverse transform under a cross-dim dest, McHelper.spawnServerEntity into the non-player dim, and the resulting portal entity's sync to the OW-side player (PortalSyncPacket int-dim encode + redirected entity sync for an entity in a watched dest chunk — the machinery is audited, but this is its first producer from a command). Also glance at goback's stored-position dim handling. Then amend S14-CROSS-DIM-TEST.md step 2 to name the intended mechanism explicitly (e.g. 'run /portal complete_bi_way_portal while looking at the portal BEFORE crossing').
- why: Items 2 (cross back), 3 (double-crossing stress — the R7 historical signature test), 4 (cross both large/negative portals), and 7 (long-walk both dims) all stall if the return path is broken; rung 1 only ever exercised completeBiWayPortal same-dim, and the reverse-portal spawn into a non-player ServerLevel plus its cross-dim entity sync is genuinely first-run at S14. A five-minute targeted read closes the only unaudited command in the script's critical path.

#### GAP [NOTE] Death-respawn in the nether during item 2 is an untested but structurally safe interaction — add it to the script's pre-answers
- whatToAudit: Verified this pass: flag-ON, HandleRespawnMixin (deliberately ungated, EXCLUSIVITY_LEDGER §1.2 B4) degrades to pure vanilla on a genuine death respawn — both its reuse/promotion branches gate on block-era PortalWorldManager caches that are empty flag-ON (HandleRespawnMixin.java:382-405 falls through to the vanilla ClientLevel ctor; :443-447 falls through to mc.setLevel), vanilla setLevel routes through the 2-arg updateLevelInEngines funnel (MixinMinecraft.java:188-207) firing CLIENT_CLEANUP + ClientWorldLoader.cleanUp() (IP-identical), and the fresh level gets ImmPtlClientChunkMap via the ctor hook exactly as at login. Residual to confirm: nothing — but since the S14 world is deliberately NOT peaceful (R10 watch) and the nether has ghasts/lava, a death is plausible; add a script pre-answer: 'dying and respawning disposes ALL portal-view secondaries (cleanUp) — views re-fade-in afterward; this is IP behavior, not a bug', so the reset isn't misread as the C8 flake or a persistence loss.
- why: The respawn path is the one live flag-ON consumer of an entire block-era mixin no track traced; the verification here closes it, and the pre-answer prevents a plausible mid-test event from being misfiled against R11 persistence or C8 during the first launch.

#### GAP [NOTE] Residual test-item code covered by symmetry/triviality arguments rather than end-to-end traces (accepted; recorded for the parent)
- whatToAudit: Three items rest on arguments rather than dedicated traces, all checked this pass to be low-risk: (1) item 5's report_chunk_loaders body (PortalDebugCommands.java:293-308) is a trivial dump over the chunkloading-audited ChunkVisibility.foreachBaseChunkLoaders — no 26.2 drift surface; (2) item 8's R13k gates (MixinCamera initialized-flag via the CameraInvokerMixin seam + the extract-RETURN view-rotation post-process in MixinGameRenderer) landed at S12, ran every frame of rung 1 on the main pass, and the fluid-submersion branch is dim-agnostic vanilla plus the fog-track-audited dest-camera force-NONE — the fluid-fog scenario itself is precisely what the live item exists to prove; (3) item 9's relog-INTO-nether is the dim-symmetric application of the audited login chains (DimIdSync is dim-agnostic; ClientWorldLoader.initializeIfNeeded keys the main-dim entry off the current level's dimension). No further static work recommended; if any of the three misbehaves live, the audit trail above says the defect is in the named seam, not the audited chains.
- why: Makes the coverage basis explicit so a live failure on items 5/8/9 routes directly to the un-traced seam instead of re-opening the nine audited tracks; prevents false confidence that these had full end-to-end traces.

