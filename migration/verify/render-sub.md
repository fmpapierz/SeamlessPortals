# Adversarial Verification: Render Context / Optimization / Renderer Family

Verified documents:
- `migration/inventory/render-sub.md` (inventory)
- `migration/api-map/render-sub.md` (api-map)

Method: re-derived every geometry/sign/transform claim from IP source (`C:/Users/warwa/ModDev/ImmersivePortalsMod/src/main/java/qouteall`), re-opened every cited 26.2 file (`C:/Users/warwa/ModDev/mc262-ref`) for SAME/CHANGED verdicts, and hunted for renames behind every GONE verdict (grepped member names, parameter types, distinctive call bodies across the 26.2 tree). Scope compared against a live directory listing. 52 load-bearing claims checked. **All 52 CONFIRMED. Zero refuted. Severity: clean.**

---

## 0. Scope audit

| Claim | Verdict | Evidence |
|---|---|---|
| Slice = 14 files, ~2,220 LOC across context_management/, optimization/, renderer/ | **CONFIRMED** | Directory listing: exactly 14 .java files; `wc -l` total 2217 (RenderStates 294, PortalRendering 228, WorldRenderInfo 279, FogRendererContext 115, StaticFieldsSwappingManager 162, DimensionRenderHelper 41, CloudContext 83, GLResourceCache 50, SharedBlockMeshBuffers 9, PortalRenderer 360, RendererUsingStencil 313, RendererUsingFrameBuffer 136, RendererDebug 98, RendererDummy 49). No file silently skipped — every file has an inventory section (§2.1–§2.14). |

No internal contradictions found between the two docs; the api-map's verdict tallies (16/19/16) match its own entries.

---

## 1. Geometry / sign / transform claims (re-derived from IP source)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 1 | `getRenderingCameraPos()` starts from `RenderStates.originalCamera.getPosition()` and folds `portal.transformPoint(pos)` over the layer stack bottom→top | **CONFIRMED** | `PortalRendering.java:105-111` — `for (Portal portal : portalLayers) pos = portal.transformPoint(pos);` (`java.util.Stack` iterates insertion order = bottom→top). |
| 2 | `applyAdditionalTransformations` iterates bottom→top; on `overwriteCameraTransformation` sets pose AND normal to identity; multiplies `cameraTransformation` into pose; normal matrix copy scaled by `pow(1/|det|, 1/3)` | **CONFIRMED** | `WorldRenderInfo.java:120-139` — identity at :123-124, `pose().mul(matrix)` :129, `normalMatrixMult.scale((float) Math.pow(1.0 / Math.abs(determinant()), 1.0/3))` :131-136. |
| 3 | `getPortalRotationMatrix` = **conjugated** `portal.getRotation()` quaternion → Matrix4f | **CONFIRMED** | `PortalRenderer.java:271-279` — `rot.conjugate(); return rot.get(new Matrix4f());`. |
| 4 | `getPortalScaleMatrix` = uniform `1/portal.getScale()` only when `shouldApplyScaleToModelView(portal)` ≡ `hasScaling() && isFuseView()`; rationale = fog vs depth correctness | **CONFIRMED** | `PortalRenderer.java:289-304` — `float v = (float)(1.0 / portal.getScale())` :296, gate :295, `shouldApplyScaleToModelView` :302-304, rationale comment :291-294. |
| 5 | `getPortalTransformation` = rotation · (mirror · scale), each nullable, `combineNullable` | **CONFIRMED** | `PortalRenderer.java:258-268` — `combineNullable(rot, combineNullable(mirror, scale))` :267; `combineNullable` does `m1.mul(m2)` :282-287. |
| 6 | Recursion `WorldRenderInfo`: `overwriteCameraTransformation=false` (stacked), `cameraTransformation = portal.getAdditionalCameraTransformation()`, `doRenderSky = !portal.isFuseView()`, description = `getDiscriminator()` = `getUUID()` | **CONFIRMED** | `PortalRenderer.java:210-222` (:215 overwrite false, :214 transformation, :220 sky, :216 discriminator); `Portal.java:1036-1039` (`getDiscriminator() { return getUUID(); }`); `Portal.java:1576-1580` (`getAdditionalCameraTransformation() { return PortalRenderer.getPortalTransformation(this); }`). |
| 7 | `getActiveClippingPlane`: innermost `getInnerClipping()`; if null with ≥2 layers, walk **outward** for nearest non-null plane and transform inward per layer via `portal1.transformPoint(pos)` / `portal1.transformLocalVecNonScale(normal)` | **CONFIRMED** | `PortalRendering.java:178-214` — outward walk :185-209, inward transform loop :197-203 (`new Plane(portal1.transformPoint(outerPlane.pos()), portal1.transformLocalVecNonScale(outerPlane.normal()))`). |
| 8 | `isInvalidRecursionRendering`: ≥2 layers, `toRender == secondLast && Portal.isReversePortal(toRender, last)` | **CONFIRMED** | `PortalRendering.java:216-227`. |
| 9 | Mirror parity: `updateCache` counts `Mirror` instances, odd → flip; consumed at MixinLevelRenderer ~:203-205 and sky wrap ~:434-436 | **CONFIRMED** | `PortalRendering.java:43-53`; `MixinLevelRenderer.java` — `isRenderingOddNumberOfMirrors() → applyMirrorFaceCulling()` in the section-layer bracket (~:203-205) and inside the sky-pass wrap (~:434-436), both sighted. |
| 10 | `shouldSkipRenderingInsideFuseViewPortal`: inside a fuse-view portal, skip when `portal.transformPoint(renderingPortal.transformPoint(cameraPos))` round-trips (`distanceToSqr < 0.1`) | **CONFIRMED** | `RendererUsingStencil.java:294-312`. |

## 2. Stencil choreography (inventory §3.2 — re-read step by step)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 11 | Carve: `glStencilFunc(GL_EQUAL, outer, 0xFF)` + `glStencilOp(KEEP, KEEP, INCR)` + `glStencilMask(0xFF)`; `FrontClipping.updateInnerClipping(modelView)` first; `renderPortalArea(portal, Vec3.ZERO, modelView, RenderSystem.getProjectionMatrix(), true,true,true,true)` | **CONFIRMED** | `RendererUsingStencil.java:171-197` (func :177, op :180, mask :185, clipping :188, call :190-196). |
| 12 | `ViewAreaRenderer.renderPortalArea` signature = `(portal, fogColor, modelViewMatrix, projectionMatrix, doFaceCulling, doModifyColor, doModifyDepth, doClip)` | **CONFIRMED** | `ViewAreaRenderer.java:25-30`. |
| 13 | Depth clear: stencil-limited, color writes off, `glDepthFunc(GL_ALWAYS)`, `glDepthRange(1, 1)`, screen triangle, restore saved `glGetInteger(GL_DEPTH_FUNC)` + `glDepthRange(0, 1)`; skipped for fuse-view | **CONFIRMED** | `RendererUsingStencil.java:199-225`; fuse-view skip `:146-150`. |
| 14 | Depth restore: `renderPortalArea(..., false, false, true, true)` under `GL_ALWAYS`, final `doClip=true` for scale-box-from-inside depth; pop-before-restore ordering deliberate | **CONFIRMED** | `RendererUsingStencil.java:227-247` (comment :243); pop-before-restore + comment `:156-157`. |
| 15 | Clamp: `glStencilFunc(GL_LESS, outer, 0xFF)` — passes where ref(outer) < stencil — with `glStencilOp(KEEP, REPLACE, REPLACE)`, depth+color writes off, depth test off, screen triangle | **CONFIRMED** | `RendererUsingStencil.java:249-278` (GL ref-vs-stencil semantics note :254-255). GL spec: `GL_LESS` passes iff ref < stencil — the doc's reading is correct. |
| 16 | `myFinishRendering`: `glStencilFunc(GL_ALWAYS, 2333, 0xFF)`, ops KEEP, disable stencil test; done here (not `finishRendering`) because outer translucency renders after | **CONFIRMED** | `RendererUsingStencil.java:111-117`; branch + comment `:63-70`; `finishRendering` empty :107-109. |
| 17 | Occlusion prediction: last-frame query result per rendering-description used as prediction; same-frame fetch (stall, counted) when no prior result / frequently mispredicted / `queryStallCounter <= 3` | **CONFIRMED** | `PortalRenderInfo.java:212-257` — `noPredict = isFrequentlyMispredicted() || QueryManager.queryStallCounter <= 3`; same-frame fetch on `lastFrameQuery == null` or `!lastFrameVisible && noPredict`. See nit 3. |

## 3. Flow / dependency / wiring claims (IP)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 18 | `onBeforeTranslucentRendering` anchored at `Sheets.translucentItemSheet()` INVOKE inside `method_62214` (the addMainPass lambda) — the recursion point | **CONFIRMED** | `MixinLevelRenderer.java:137-150` — `@Inject(method = "method_62214", at = @At(value="INVOKE", target=".../Sheets;translucentItemSheet()..."))` → `IPCGlobal.renderer.onBeforeTranslucentRendering(modelView)`. |
| 19 | Camera position delivery: `WorldRenderInfo.adjustCameraPos` at `Camera.setup(BlockGetter,Entity,ZZF)` RETURN | **CONFIRMED** | `MixinCamera.java:41-51`; `WorldRenderInfo.java:113-118` (`IECamera.portal_setPos`). |
| 20 | Rotation delivery: `@WrapOperation` on `Matrix4f.rotation(Quaternionfc)` in `renderLevel` → `TransformationManager.processTransformation`; projection replay via `@Redirect` on `getProjectionMatrix(F)` ordinal 0 capturing/replaying `basicProjectionMatrix` | **CONFIRMED** | `MixinGameRenderer.java:275-317` — redirect body (replay when `PortalRendering.isRendering()`, capture otherwise) and wrap both sighted verbatim. |
| 21 | `renderWorldNew` brackets with `WorldRenderInfo.pushRenderInfo`/`popRenderInfo` around `switchAndRenderTheWorld` | **CONFIRMED** | `MyGameRenderer.java:96-112`. |
| 22 | `RenderStates.updatePreRenderInfo`: captures dim/pos/lastTickPos from `cameraEntity`, game mode via `PlayerInfo.getGameMode()`, rotates `portalRenderInfos`→`lastPortalRenderInfos`, runs `FogRendererContext.update()`, nulls `basicProjectionMatrix`, captures `originalCamera`, expands `originalPlayerBoundingBox` backwards by velocity | **CONFIRMED** | `RenderStates.java:87-132` — all sighted, incl. `expandTowards(-velocity.x, -velocity.y, -velocity.z)`. |
| 23 | `isLaggy`: set when >10 portals last frame AND (avg FPS < 8 OR min FPS < 6); cleared at min FPS > 15; gated by `IPGlobal.lagAttackProof` | **CONFIRMED** | `RenderStates.java` `updateIsLaggy()` (~:135-160) — thresholds verbatim. |
| 24 | `FogRendererContext.getFogColorOf` temporarily reassigns `Minecraft.level = destWorld`, builds throwaway `new Camera()` via `portal_setPos`/`portal_setFocusedEntity(client.cameraEntity)`, restores in finally | **CONFIRMED** | `FogRendererContext.java:83-108` — `client.level = destWorld` (~:84), throwaway camera (~:86-88), `client.level = oldWorld` in the finally block. |
| 25 | `MixinFogRenderer` static initializer assigns `copyContextFromObject`/`copyContextToObject`/`getCurrentFogColor` over six `@Shadow` FogRenderer statics, then `FogRendererContext.init()` | **CONFIRMED** | `MixinFogRenderer.java:24-47` — static block sighted verbatim. |
| 26 | `RendererDebug`/`RendererDummy` declare `@Override onAfterTranslucentRendering(Matrix4f)` which does NOT exist in `PortalRenderer` (bit-rot) | **CONFIRMED** | `RendererDebug.java:24`, `RendererDummy.java:24` declare it; full read of `PortalRenderer.java:42-360` — no such method; repo-wide grep hits ONLY those two files (no mixin calls it either). The "port intent, not text" advice is sound. |
| 27 | `MixinLevelRenderer_Clouds` entirely commented out with "TODO re-implement" → CloudContext inert | **CONFIRMED** | `MixinLevelRenderer_Clouds.java:27` `// TODO re-implement`, shadows/injects all `//`-commented. |
| 28 | Wiring: `IPCGlobal` holds the renderer refs (dummy/debug eagerly constructed); stencil+framebuffer constructed on the render thread via `Minecraft.getInstance().execute` in `IPModMainClient.init`, stencil initial active; init order `ClientWorldLoader → ClientTeleportationManager → (render-thread block) → GLResourceCache → PortalRenderInfo → CloudContext → SharedBlockMeshBuffers` | **CONFIRMED** | `IPCGlobal.java:16-20`; `IPModMainClient.java:71-99` — order and `IPCGlobal.renderer = IPCGlobal.rendererUsingStencil` sighted. |
| 29 | `MixinGlStateManager`: `_enableCull` HEAD force-disable via `RenderStates.shouldForceDisableCull`; `_glGenBuffers`/`_glGenVertexArrays` HEAD substitutes gated by `IPGlobal.cacheGlBuffer` | **CONFIRMED** | `MixinGlStateManager.java` (~:22-56) — all three injects sighted. |
| 30 | Portal discovery: globals + `entitiesForRendering()`, filter `shouldSkipRenderingPortal` (validity → visibility unless maxLayer 0 → count cap → `isRoughlyVisibleTo(isometric-adjusted camPos)` → `cannotRenderInMe` → range → frustum unless <0.1 → recursion guard → Fabric predicate event), sort near→far | **CONFIRMED** | `PortalRenderer.java:84-176` — every step in the claimed order; `PORTAL_RENDERING_PREDICATE` = `EventFactory.createArrayBacked` predicate-AND `:48-59`. |
| 31 | `getRenderRange`: RD*16; 16 when laggy/reduced; ÷layer for layer>1; ×outer scale capped 32*16 when scale>2. `getPortalRenderDistance`: scale>2 → `destAreaRadiusEstimation*1.4` capped 32*16, /16, min vanilla RD; reduced → RD/3 | **CONFIRMED** | `PortalRenderer.java:178-195, 233-247`. |

## 4. 26.2 claims — GONE verdicts (rename-hunted) and the reversed-Z headline

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 32 | **Reversed-Z**: vanilla clears depth to 0.0; default depth test GREATER_THAN_OR_EQUAL | **CONFIRMED** | `GameRenderer.java:404-408` `clearColorAndDepthTextures(..., 0.0)`, `:439` `clearDepthTexture(..., 0.0)`; `LevelRenderer.java` clear pass `clearColorAndDepthTextures(..., new Vector4f(fogColor...), depthTexture, 0.0)` (~:198-206); `DepthStencilState.java:9` `DEFAULT = new DepthStencilState(CompareOp.GREATER_THAN_OR_EQUAL, true)` (doc cites :8 = the record line; DEFAULT is :9 — 1-line nit, substance exact). |
| 33 | Extract/render split: `extract(DeltaTracker,boolean)` :379; `update(DeltaTracker)` :372-377 → `Camera.update`; `render(DeltaTracker,boolean)` :396 | **CONFIRMED** | `GameRenderer.java:372-377, 379-394, 396` (grep-pinned `:396`); extract calls `lightmapRenderStateExtractor.extract` :386 and `levelExtractor.extract` as claimed. |
| 34 | G1: `Camera.setup` GONE; `update(DeltaTracker)` :93-112; `extractRenderState` writes `pos` :127; `setLevel` public :491; `setEntity` public :411; private `initialized` set in `update()` :110; `getFluidInCamera` returns NONE when `!initialized` :437-440; `getPosition()` → `position()` :359 | **CONFIRMED** | `Camera.java` — grep for `void setup` hits only `setupPerspective`/`setupOrtho`; every cited line verified at the exact number. (Nit: `initialized` is also reset to false at :488 in the reset path — doesn't change the "mod-built Camera must set it" implication.) |
| 35 | G2: `FogRenderer.setupColor` + six statics GONE; instance class at `renderer/fog/FogRenderer.java`; only static state = `fogEnabled` :47 (+ shared `FOG_ENVIRONMENTS` :39-46); `setupFog(Camera, int, DeltaTracker, float, ClientLevel)` :167; NONE = pre-built empty buffer with `Float.MAX_VALUE` distances (ctor :51-63); `endFrame()` rotates the single WORLD ring slot :72-74; `getBuffer` :76-85; `updateBuffer(FogData)` maps the current WORLD slot :188-202 | **CONFIRMED** | All sighted at exact lines in `net/minecraft/client/renderer/fog/FogRenderer.java`. `renderLevel` confirms the consumption pattern: `fogRenderer.updateBuffer(cameraState.fogData)` then `getBuffer(FogMode.WORLD)` → passed into `levelRenderer.render` (GameRenderer.java ~:558-565); `setShaderFog(getBuffer(NONE))` after hand — the mid-frame single-slot corruption design note is architecturally sound. |
| 36 | G4: `LightTexture` GONE → `Lightmap` (no-arg ctor, 16×16 RGBA8 + UBO, :44-50; `getTextureView` :52; `close` :56) + `LightmapRenderStateExtractor` (ctor `(GameRenderer, Minecraft)` :30; `tick` :35; `extract` :47 hard-reads `minecraft.level`/`minecraft.player` :50-51); getters `lightmap()` :661, `levelLightmap()` :665 | **CONFIRMED** | All sighted at exact lines. `GameRenderer` fields `lightmap`/`uiLightmap` private final in the :97-130 field block. |
| 37 | G5/G6: `RenderSystem.getProjectionMatrix()` (CPU), `enableDepthTest`, `depthMask`, `clear(int)` all GONE; `setProjectionMatrix(GpuBufferSlice, ProjectionType)` :180, `backupProjectionMatrix`/`restore` :186-196, `getProjectionMatrixBuffer()` :198, `outputColorTextureOverride`/`outputDepthTextureOverride` :68-69 | **CONFIRMED** | grep of `RenderSystem.java` for `enableDepthTest|depthMask|clear(` → zero hits; all replacement members at exact cited lines. |
| 38 | G7/C14: `_clearColor`/`_clearDepth` GONE; `_clearBuffer(int, Vector4fc)` :468 / `_clearBuffer(double)` :476 (glClearBufferfv-based); `_clear(int)` one-arg :460 with internal macOS error-flush; `_colorMask` CHANGED to `@WriteMask int` bitmask :441 (+ indexed :452); package moved to `com.mojang.blaze3d.opengl`; `_depthFunc` :67, `_depthMask` :75, `_enableCull` :322, `_glGenBuffers` :202, `_glGenVertexArrays` :208, `_getInteger` :536 all present | **CONFIRMED** | `com/mojang/blaze3d/opengl/GlStateManager.java` — every member at the exact cited line; `_clearBuffer` bodies use `GL33C.glClearBufferfv(6144/6145, ...)`; `_clear` has `if (MacosUtil.IS_MACOS) _getError()`. |
| 39 | G8: `RenderTarget` has no bind/FBO-id/stencil (grep `bindWrite|frameBufferId` → zero); `createBuffers` hardcodes `GpuFormat.D32_FLOAT` :86; `RenderPassDescriptor` = colorAttachments + depthAttachment only :17-19; stencil-capable formats exist `D32_FLOAT_S8_UINT`/`D24_UNORM_S8_UINT`/`S8_UINT` `GpuFormat.java:60-63`, `hasStencilAspect()` :97-98; vanilla output-redirect pattern in addAlwaysOnTopPass (set overrides → clear depth → draw → null) ~`LevelRenderer.java:498-505`; GPU queries timestamp-only (`createTimestampQueryPool` `GpuDevice.java:184-186`; repo-wide grep for `SAMPLES_PASSED`/occlusion in blaze3d → zero) | **CONFIRMED** | All sighted. The "no stencil anywhere in the GPU abstraction" and "raw-GL occlusion queries survive" conclusions hold. |
| 40 | G10: `Minecraft.cameraEntity` field GONE; `getCameraEntity()` delegates to `gameRenderer.mainCamera().entity()` :2648-2650 | **CONFIRMED** | `Minecraft.java:2648-2650` verbatim. |
| 41 | G11: `GraphicsStatus` GONE (no file); `GraphicsPreset.FABULOUS` `GraphicsPreset.java:18`; `Options.improvedTransparency` :227-228; `GameRenderState.useShaderTransparency()` :17-19 = `!isPanoramicMode && improvedTransparency` | **CONFIRMED** | All sighted at exact lines; `ls GraphicsStatus.java` → no such file. |
| 42 | G12: `LevelRenderer.setupRender` GONE; `LevelExtractor.applyFrustum(Frustum)` :373-382 (main-thread assert :374-376, `clearVisibleSections` + `sectionOcclusionGraph.addSectionsInFrustum`); spectator cull → `CameraRenderState.smartCull` (`Camera.java:122-124`, `Minecraft.smartCull` public :354) | **CONFIRMED** | All sighted verbatim at exact lines. |
| 43 | G13: `DimensionSpecialEffects` GONE (repo-wide grep → zero files); `Sheets.translucentItemSheet()` still exists :83 but not called from any LevelRenderer pass; new main-pass lambda ordered boundaries `renderGroup(OPAQUE)` :409 → `executeSolid()` :419 → depth copies :421-431 → `executeTranslucent()` :434 → `executeOutline()` :436 → `renderGroup(TRANSLUCENT)` :438 → `executeTranslucentAfterTerrain()` :440 | **CONFIRMED** | `LevelRenderer.java:389-441` read line-by-line — every boundary at the EXACT cited line number. The "between executeSolid and executeTranslucent" mapping for the 1.21.3 before-translucent-items point is correct. |
| 44 | G14: `renderHand`/`panoramicMode` fields GONE from GameRenderer; `renderItemInHand(CameraRenderState, float, Matrix4fc)` gated inline by `isPanoramicMode`/camera type/`isHudHidden`/spectator :337-341, called once at :572; panorama state on `Camera.isPanoramicMode` :79/:318/:495 | **CONFIRMED** | Field block `GameRenderer.java:97-130` has neither field; `renderItemInHand` :336+ with the four inline gates; call after HUD-projection swap + depth clear in `renderLevel`. |
| 45 | G15: `getProjectionMatrix(float)` GONE; bobbing multiplied into a per-frame projection copy in `renderLevel` (`new Matrix4f(cameraState.projectionMatrix)` … `projectionMatrix.mul(bobStack.last().pose())` :535-542) then uploaded via `levelProjectionMatrixBuffer` + `RenderSystem.setProjectionMatrix` :557; `ProjectionMatrixBuffer` fields :129-130 | **CONFIRMED** | `GameRenderer.java:525-560` — sighted verbatim, incl. `private final ProjectionMatrixBuffer levelProjectionMatrixBuffer = new ProjectionMatrixBuffer("level")`. |
| 46 | G16: `CloudRenderer.render(int, CloudStatus, float, int, Vec3, long, float)` :128; mesh rebuilt only on cell/status/relative-pos change (`prevRelativeCameraPos`/`prevCloudStatus` :54-55, gate :168-174) → vanilla now does CloudContext's caching, keyed to one world | **CONFIRMED** | `CloudRenderer.java` — all sighted at exact lines. |

## 5. 26.2 claims — CHANGED / SAME verdicts (signatures re-checked against IP call sites)

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 47 | C1: `renderLevel(DeltaTracker)` survives :525; inner call = `levelRenderer.render(resourcePool, deltaTracker, renderOutline, cameraState, modelViewMatrix, terrainFog, cameraState.fogData.color, !shouldCreateBossFog)` (~:563-565); `LevelRenderer.render` declaration :156-165 with `boolean shouldRenderSky` last param gating `addSkyPass` | **CONFIRMED** | Both files sighted verbatim; the `doRenderSky`→parameter mapping is real. |
| 48 | C2: rotation-matrix build moved into `Camera.getViewRotationMatrix(Matrix4f)` :384-392, cached behind `matrixPropertiesDirty` (:385 bit check; set only by `setRotation` :346 `|= 3` besides the ctor's -1) → a `Matrix4f.rotation` wrap fires irregularly; extracted copy `cameraState.viewRotationMatrix` written every extract :135 and threaded as `modelViewMatrix` | **CONFIRMED** | `Camera.java` grep: `matrixPropertiesDirty` touched only at :71 (init -1), :346 (set), :385/:388/:396/:400 (check/clear). Extra support: `update()` itself consumes `getViewRotationMatrix` at :106 (prepareCullFrustum), so the bit is typically already cleared before `extractRenderState` — the doc's do-not-wrap warning is if anything understated. |
| 49 | C3: `bobView(CameraRenderState, PoseStack)` :322-334 with surviving `poseStack.translate(f,f,f)` anchor; bob inputs from `entityRenderState.backwardsInterpolatedWalkDistance`/`bob` | **CONFIRMED** | `GameRenderer.java:322-334` verbatim. |
| 50 | C5/C6/C7/C16/C13: `mainCamera()` :657; `bossOverlayWorldDarkening(float)` :653-655 (consumed by extractCamera's setupFog call :637); `mainRenderTarget()` :673; `Hud.setOverlayMessage(Component, boolean)` :1225; `pollLightUpdates` unchanged `ClientLevel.java:281-293`, call site moved to `ClientLevel.update()` :496-503 (`pollLightUpdates()` + `getLightEngine().runLightUpdates()`), invoked `Minecraft.java:1287` (`this.level.update()`) | **CONFIRMED** | All sighted at exact lines. C13's lateUpdateLight cross-reference matches the mod's already-ported seam. |
| 51 | C11/C10: `addSkyPass` :314+ with `SkyRenderer` recreated on demand capturing `gameRenderer.mainRenderTarget()` at construction (~:322); pipeline cull state funnels through `GlStateManager._enableCull/_disableCull` in the GL backend (`GlCommandEncoder.java:791-795` `if (pipeline.isCull()) _enableCull(); else _disableCull();`) → existing cull-override mixin still intercepts pipeline-driven culls | **CONFIRMED** | Both sighted verbatim. |
| 52 | C12/C18/S4/S6: `allChanged()` moved to `LevelExtractor` :406-417 setting `shouldInvalidateCompiledGeometry` (:415), rebuild next extract via `invalidateCompiledGeometry` :122-124; `_glGenBuffers` reached ONLY via `DirectStateAccess` emulated `createBuffer` (:137; Core path = `ARBDirectStateAccess.glCreateBuffers` ~:60) and `_glGenVertexArrays` ONLY via vanilla's own `VertexArrayCache` :53/:127 (repo-wide caller grep); `Frustum(Matrix4fc, Matrix4f)` ctor :22-24 + copy ctor :26; `Minecraft.level` public writable :335 | **CONFIRMED** | All grep/read-verified at exact lines. |

---

## Nits (not refutations — substance verified correct)

1. api-map big-fact-2 cites `DepthStencilState.java:8` for `DEFAULT`; the record declaration is :8, the `DEFAULT` constant is :9. One-line drift in a 14-line file; claim substance exact.
2. G1 says `initialized` is "only set in `update()` (:110)"; it is also **reset to false** at `Camera.java:488` (the reset path). The port implication (a mod-built Camera must force it or `getFluidInCamera` reads NONE) is unaffected.
3. Inventory §3.2 step 2 phrases the same-frame-fetch fallback as "no prior result, frequently mispredicted, or few stalls yet"; precisely, with a prior result the fetch happens only when the prediction was *invisible* AND (`isFrequentlyMispredicted() || queryStallCounter <= 3`) — i.e. a visible last-frame prediction is always trusted (`PortalRenderInfo.java:228-251`). Slight simplification in the conservative direction (never skips a portal the prediction says visible).

## Overall

- **Checked:** 52 load-bearing claims (all geometry/sign/transform claims re-derived; every GONE verdict rename-hunted across `mc262-ref`; SAME/CHANGED signatures re-opened at the IP call sites; anchors and wiring traced; scope audited).
- **Refuted:** 0.
- **Severity: clean.** Both documents are accurate to an unusual degree — the api-map's main-pass ordering (G13) and GlStateManager member table (C14) are exact to the line. Safe to build the port plan on.
