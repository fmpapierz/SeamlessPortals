# Fresh-session prompt — paste everything below the line verbatim

---

Continue the Seamless Portals shaders-ON polish work. Work in the worktree
`C:\Users\warwa\ModDev\Portals\Portal 26.2\.claude\worktrees\is5-shadow` (branch `iris-on/is5-shadow`).

READ FIRST, in order:
1. `migration/POLISH_ROUNDS_CLOSED_HANDOFF.md` — the governing handoff: standing rules (§0 — every
   rule is binding), the shipped stack + levers (§1), the two USER-GATED commissions (§2), the
   follow-up ledger (§3), the probe kit (§4).
2. Your memory index (auto-loaded) — especially `portal-view-polish-queue`,
   `per-dest-state-commission`, `polish-rounds-lessons`.
3. `migration/POLISH_SESSION_NOTES.md` only as needed for per-item evidence detail.

STATE: polish rounds 1+2 are FULLY CLOSED — all six fixes shipped, live-confirmed by me, and pushed
(HEAD `076948b` or later). Nothing in the closed rounds needs re-work.

WHAT TO DO: I will tell you in my next message which engagement to run. The two commissioned
engagements are user-gated — per-dest iris state (ACT volume + TAA history together) and the
Motion-Blur bloom-ring polish — do NOT start either unless I explicitly say so in this session.
If my message names something else (a new bug, a §3 ledger item), do that instead.

DISCIPLINE (non-negotiable, same as always): no guessing — diagnose-first with instrumentation before
any design; logs for everything including failure paths; read the FULL latest.log every run (GL census
first); Fable for design/verify panels + Opus for mechanical recon; multi-agent panels with
adversarial verification AND a final-diff pass on the implemented code for every non-trivial
mechanism; every fix lever-gated DEFAULT-ON with `-P` passthroughs in BOTH fabric/build.gradle blocks;
A/B both directions; the 8-leg suite (`:fabric:runCrossingGametest`) before every commit; push every
stage commit; `git add` explicit file lists only; Java process cleanup only per the global CLAUDE.md
rules (own-project PIDs only, never `gradlew --stop`).
