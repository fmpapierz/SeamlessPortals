# IS5 HANDOFF — 2026-08-11 (perf arc → flicker arcs → the window-look fork)

**Branch `iris-on/is5-shadow`, worktree
`C:\Users\warwa\ModDev\Portals\Portal 26.2\.claude\worktrees\is5-shadow`, tip `d27468f`,
everything pushed. Every commit gated: `.\gradlew.bat :fabric:runCrossingGametest` GREEN.**
Read with `PERF_AND_FLICKER_2026_08_10.md` (the perf + first flicker record),
`IS5_ARRIVE_DESIGN.md`, `IS5_DEPTHFORK_DESIGN.md`, `IS5_XDIM_DESIGN.md`.

## §1 WHAT SHIPPED (this arc, in order)

| commit | what | default |
|---|---|---|
| `ba5fff4` | PERF-P1: per-stage PerfTimers + four instrument mutes | mutes ON (probes OFF) |
| `8573bcd` | PERF-P2: iris pipeline pre-warm at world join | ON |
| `ac4cfdb` | IS5-XTRACE crossing trace | OFF (`-Pis5CrossingTrace`) |
| `31dfddd` | IS5-XFLICK arrival near-plane speculative skip | ON |
| `54c2e9d` | IS5-BLINK query hysteresis (+ always-on blink detector) | ON |
| `e43d32b` | IS5-XDIM cross-dim dest-chain restore (POST_FINAL capture) | **OFF** (`-PcrossDimDestChain`) |
| `dc0c071`/`77fe2c6` | XFLICK refinements → look-into-face (portal NORMAL dot, ±0.2) | ON |
| `f181191` | IS5-ARRIVE sideways-arrival suspension withhold | ON |
| `1be3aac` | IS5-HIST history stamp writes the PREVIOUS capture | ON |
| `dce0670` | IS5-DEPTHFORK content-vs-plane depthtex1 fork | **OFF** (= PLANE) |
| `d27468f` | IS5-OUTLINE selection-outline depth-write kill (shaders-ON) | ON — **NOT live-verified** |

**User-affirmed closures:** perf "feels smoother already"; first-look freeze gone (prewarm);
"flicker gone" (BLINK, log-corroborated: 23 blinks absorbed, hys=29); sideways-arrival
gauntlet "seems good"; "swirls visible" (XDIM works, but see §2); "mb off is fixed"
(DEPTHFORK CONTENT).

## §2 THE OPEN USER DECISION — the window-look fork (BLOCKING the next step)

Measured coupling (Complementary r5.8.1, file:line): **MB velocity `composite4.glsl:90` and
TAA reprojection `composite6.glsl:39` both read depthtex1**; depthtex2 is read nowhere. The
window's depth therefore drives BOTH. Plus: the window is composited by the SOURCE chain
(the ring mandate requires the stamp to precede the main composites), so source fog/hue/VL
apply to it — keyed by that same depthtex1.

| option (lever) | MB-on look | MB-off ghost | source fog/hue in window | dest's own effects (storm) |
|---|---|---|---|---|
| **PLANE** (default, no lever) | the KEPT cool strong blur | **present** (ugly) | minimal — "glass pane at plane distance" | absent |
| **CONTENT** (`-Pis5WindowContentDepth`) | ordinary-correct | **fixed** | **stronger** — VL/fog march to content distance | absent |
| **XDIM** (`-PcrossDimDestChain`, tested with PLANE) | kept | present | dest chain runs first, then source on top | **present** |
| XDIM + CONTENT | ordinary | fixed | source on top of dest grade | present |

User verdicts so far: XDIM's double-grade = "passed but i dont like it… i want it to be
identical"; CONTENT = ghost fixed but the cool MB look lost and source fog more visible.
**The four cells above are the whole honest space** — the only way to get "cool MB + no
ghost + dest-correct look" simultaneously is the pack-coupled mid-composite restamp
(rejected as a hack in `IS5_DEPTHFORK_DESIGN.md` §4; re-open only on explicit user demand).
NEXT SESSION MUST ASK which cell ships as default; XDIM+CONTENT is the one combination never
run live.

## §3 IMMEDIATE NEXT STEPS

1. **Live-verify IS5-OUTLINE** (`d27468f`, never tested): look at a block in front of a
   portal, shaders ON — the source-terrain sliver around the selection outline must be GONE,
   the outline itself still drawn on the block and clipped at the window edge. A/B:
   `-PdisableOutlineDepthWriteFix=true` brings the sliver back. Also check the nested-window
   twin (`SecondaryWorldRenderCore.submitDestBlockOutline`) by targeting a block seen THROUGH
   a portal.
2. **Run the fork legs and take the §2 decision** (each is one launch, no build):
   `-Pis5WindowContentDepth=true`, `-PcrossDimDestChain=true`, and both together.
3. Flip whichever cell the user picks to default (same static-final + disable-row idiom).

## §4 OPEN / DEFERRED (unchanged unless noted)

- **destRender = 7.4 ms/view CPU** — THE steady-state cost (2 views ⇒ ~22-32 fps in the
  P1 census). User declined the three measured levers for now: portal-view shadow clamp,
  alternate-frame view reuse, reduced view resolution. Perf gates for GPU-side changes must
  use GL_TIME_ELAPSED or whole-frame FPS — the `is5.*` PerfTimers are CPU-side and blind to
  composite passes.
- **Pre-warm debounce** — every pack-option APPLY re-pays the end-pipeline compile
  (0.15-5 s). Named, unbuilt: warm N seconds after the last generation change.
- **2-frame blink residual** — 2 of 23 blinks lasted 2 frames; pre-registered next step is
  the hysteresis threshold 2 → 3.
- **XDIM residuals** — nested rim (rim-accept default), one-POST-per-dest-dim rule,
  DestPrevCamera coverage at the new loop position (kill-check never explicitly run).
- **IS5-OUTLINE caveat** — `linesTranslucent` has no iris SHADOW-pass mapping; irrelevant
  today (the outline never submits in the shadow pass), matters if that ever changes.
- **CONTENT-mode consumer flips** (disclosed, `IS5_DEPTHFORK_DESIGN.md` §3): `z0==z1`
  reflection gates, SSR march sees a depth hole at the window, VL/rainbow march to content
  distance (the fog observation), lava-fog `z1==1.0` repaint, DOF re-key, FXAA edge detect.
- **Parked (older)**: iris `copyPreHandDepth` GL_INVALID at pipeline creation;
  `maxPortalLayer` never synced to dedicated servers; the C2 upstream report rewrite;
  cross-view (XWIN) frames keep the OLD compositing (ring persists there, disclosed).

## §5 LEVER INVENTORY (all rows in the three run blocks of `fabric/build.gradle`)

Default-ON features, disable to reproduce the defect: `-PdisableStageConsistentComposite`
(the whole IS5-PRE path / the ring), `-PdisablePipelinePrewarm`, `-PdisableTeleportSpecSkip`,
`-PdisableQueryHysteresis`, `-PdisableSeamArrivalScope`, `-PdisableWindowHistoryPrev`,
`-PdisableOutlineDepthWriteFix`.
Default-OFF options/instruments: `-Pis5WindowContentDepth`, `-PcrossDimDestChain`,
`-Pis5CrossingTrace`, `-Pis5LiveReadbacks`, `-PflashProbe`, `-PrenderChainProbeAutoArm`,
`-PdebugStampSolid`, `-PdebugTintStamp`.
Run: `.\gradlew.bat :fabric:runClientSodium -PirisRuntime=true [levers]`.

## §6 METHOD NOTES EARNED HERE (see also the memory files)

1. **Detector-reads-raw + fix-changes-decision in ONE leg** (BLINK): the detector read the
   raw query values regardless of the fix lever, so one leg proved mechanism AND fix.
2. **A real mechanism is not automatically THE mechanism** (XFLICK): trace-proven on 29/29
   crossings, fixed, and the user still saw the flash — the headline cause was elsewhere.
   Keep such a fix if it is independently correct; do not let it close the arc.
3. **Ask the user for the SIGNATURE, not the presence** — "moves with translation, not
   panning, resets when still, window-only" identified TAA depth-reprojection outright.
4. **When two wishes touch one resource, present the fork honestly** — the depthtex1
   coupling was measured in the pack's GLSL before any design; the fork is the user's, not
   the model's.
5. **The judge's job is to catch the frozen math** — the DEPTHFORK remap chain was
   double-applying the portal transform; the correct answer was IDENTITY (the capture's
   depth already IS the main-view clip depth), which deleted an entire matrix subsystem.
6. **Instrument text can collide with your own monitors** — a census doc-string containing
   "IS5-ARRIVE" flooded the ARRIVE monitor; grep on emit-site prefixes, not on arc names.
